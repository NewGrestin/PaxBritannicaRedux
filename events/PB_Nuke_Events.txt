##################
# NUCLEAR ATTACK EVENTS
##################

add_namespace = nuclear

country_event = {
	id = nuclear.1
	title = nuclear.1.t
	desc = nuclear.1.d
	picture = GFX_report_PB_Nuclear_Attack
	is_triggered_only = yes

	trigger = {
		FROM.FROM = { NOT = { state = 299 } }
	}

	option = {
		name = nuclear.1.a

		add_political_power = -200

		every_country = {
				set_temp_variable = { superevent_temp = token:atomic_age }
				PB_Fire_Super_Event = yes
		}
	}
	after = { 
		#hidden_effect = {
		#	random = {
		#		chance = 1
		#		add_stability = 0.4
		#		add_war_support = 0.3
		#	}
		#	FROM.FROM = { 
		#		country_event = { 
		#			id = nuclear.2 days = 1
		#		}
		#	}
		#}
	}
}

country_event = { #ATMOSPHERIC IGNIITON
	id = nuclear.2
	title = nuclear.2.t
	desc = nuclear.2.d
	picture = GFX_report_PB_Nuclear_Attack
	is_triggered_only = yes

	trigger = {
		FROM.FROM = { NOT = { state = 299 } }
	}
	immediate = {
		#IDK an achievement for this?
	}
	option = { #Oh Cool...
		name = nuclear.2.a
		every_state = { 
			set_state_flag = state_nuked
			add_province_modifier = {
				static_modifiers = { nuclear_attack }
				province = {
					all_provinces = yes
				}			
			}
			add_manpower = -100000000
		}
	}
	option = { #Oh Cool... (NOT DIE)
		name = nuclear.2.b
	}
}