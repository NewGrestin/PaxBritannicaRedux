##################
# SWISS EVENTS 
##################

add_namespace = swiss

country_event = {		# 1936 Summer Olympics Leadup - Status of the German States
	id = swiss.1
	title = swiss.1.t
	desc = swiss.1.d
	picture = GFX_report_event_legislation

	fire_only_once = yes

	trigger = {
		tag = SWI
		date > 1936.2.1
	}

	mean_time_to_happen = {
		days = 7
	}

	option = { # Allow All Teams
		name = swiss.1.a
		ai_chance = {
			factor = 50
		}
	}

	option = { # Ban the GSR
		name = swiss.1.a
		ai_chance = {
			factor = 25
		}
		BAV = {
			add_opinion_modifier = { target = SWI modifier = olympic_ban }
			set_country_flag = Olympic_Ban
		}
	}

	option = { # The Independent Plan
		name = swiss.1.a
		ai_chance = {
			factor = 25
		}
		PRS = {
			add_opinion_modifier = { target = SWI modifier = outraged }
		}
		WSP = {
			add_opinion_modifier = { target = SWI modifier = outraged }
		}
		BAV = {
			add_opinion_modifier = { target = SWI modifier = outraged }
		}
	}
}

country_event = {		# 1936 Summer Olympics Leadup - Status of the USASR
	id = swiss.2
	title = swiss.2.t
	desc = swiss.2.d
	picture = GFX_report_event_legislation

	fire_only_once = yes

	trigger = {
		tag = SWI
		date > 1936.2.1
	}

	mean_time_to_happen = {
		days = 14
	}

	option = { # Ban the USASR
		name = swiss.2.a
		ai_chance = {
			factor = 50
		}
		BRA = {
			add_opinion_modifier = { target = SWI modifier = olympic_ban }
			set_country_flag = Olympic_Ban
		}
	}

	option = { # Allow them to play
		name = swiss.2.a
		ai_chance = {
			factor = 50
		}
		add_political_power = -50
		ENG = {
			add_opinion_modifier = { target = SWI modifier = recognized_USASR }
		}
		FRA = {
			add_opinion_modifier = { target = SWI modifier = recognized_USASR }
		}
		POR = {
			add_opinion_modifier = { target = SWI modifier = recognized_USASR }
		}
	}
}
