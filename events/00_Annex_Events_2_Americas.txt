###########################
# Annexation Events concerning America
###########################
# By lots of folks
# Converted from on_actions to decisions by Rylock and Dr_Njitram
# Any new post-annexation events need to have their triggers in the KR_on_actions file
###########################
###########################
# Thanks KR ;) - Pax Team
###########################

## the bottom table of context and other events need to be adjusted to Pax - Lexus
## i left one event as example for the rest of the events, if you're not sure check out KR code

# Table of Contents
#	No specific order but please keep events in the order as belowe
#
# AMERICA
# annex.2000 - Fate of Acadia
# annex.2001 - Fate of Apacheria
# annex.2002 - Fate of Tejas
# annex.2004 - Fate of Rio Grande
# annex.2005 - Fate of Rio Grande North / Fate of American Texas
# annex.2006 - Fate of Southern Rio Grande (Monclova)
# annex.2007 - Fate of Honduras
# annex.2008 - Fate of Yucatan
# annex.2009 - Fate of Guatemala
# annex.2010 - Fate of Los Altos
# annex.2011 - Fate of Nicaragua
# annex.2012 - Fate of Oregon
# annex.2013 - Fate of Gorogorun
# annex.2014 - Fate of UCA
# annex.2015 - Fate of FSA
# annex.2016 - Fate of Virginia
# annex.2017 - Fate of New Brunswick
# annex.2018 - Fate of Nova Scotia
# annex.2019 - Fate of Great Basin
# annex.2020 - Fate of the Rockies
# annex.2021 - Fate of the Great Plains
# annex.2022 - Fate of California
# annex.2023 - Fate of Baja California
# annex.2024 - Fate of East Apacheria
# annex.2025 - Fate of Santa Fe
# annex.2026 - Fate of Comanche
# annex.2027 - Fate of Quebec
# annex.2028 - Fate of Huron
# annex.2029 - Fate of Michigan
# annex.2030 - Fate of Superior
# annex.2031 - Fate of Wisconsin
# annex.2032 - Fate of Cavalier
# annex.2033 - Fate of El Salvador
# annex.2034 - Fate of New England
# annex.2035 - Fate of New Africa
# annex.2036 - Fate of Seminolia
# annex.2037 - Fate of Vermont
# annex.2038 - Fate of Ongtupqa
# annex.2039 - Fate of Labrador
# annex.2040 - Fate of Newfoundland
# annex.2041 - Fate of Peten
# annex.2042 - Fate of Angostura
# annex.2043 - Fate of Chiapas
# annex.2044 - Fate of Tabasco
# annex.2045 - Fate of Tologalpa
# annex.2046 - Fate of Honduran Miskito
# annex.2047 - Fate of Providencia
# annex.2048 - Fate of Tegucigalpa
# annex.2049 - Fate of Miskito Coast
# annex.2050 - Fate of Mexico
# annex.2051 - Fate of the Ohio River
# annex.2052 - Fate of Vandalia
# annex.2053 - Fate of Danish West Indies
# annex.2054 - Fate of Confederacy of Free Tribes
# annex.2055 - Fate of Northwestern Territory Colonial Authority (Borealia)
# annex.2056 - Fate of Rainy River
# annex.2057 - Fate of Victoria Island
# annex.2058 - Fate of Alaska
# annex.2059 - Fate of Alaska
# annex.2060 - Fate of Monterrey
# annex.2061 - Fate of Greenland

add_namespace = annex

#####################################
##  NORTH  AMERICA   ##
#####################################

country_event = { # Fate of Acadia
	id = annex.2000
	title = annexation_371
	desc = annexation_371_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		# Save UC or FSA as grant targets if ROOT can grant land
		if = {
			limit = { AME = { ROOT_can_grant_land = yes } }
			AME = { save_event_target_as = america }
		}
	}

	option = { # Release Acadia
		log = "[GetLogInfo]: event annex.2000 option annex.2000.a"
		name = annexation.2000.a
		trigger = { is_subject = no }
		ai_chance = {
			base = 65
			modifier = { factor = 0 annexations_can_annex = yes }
		}

		set_temp_variable = { tag_to_release = ACD } # Acadia tag
		every_state = {
			limit = { is_acadia = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes

		if = { limit = { ROOT = { has_monarchist_government = yes } } ACD = { set_cosmetic_tag = ACD_Occupied_Monarchy } }
		if = { limit = { ROOT = { has_republican_government = yes } } ACD = { set_cosmetic_tag = ACD_Occupied_Republic } }
	}

	option = { # Give to America
		log = "[GetLogInfo]: event annex.2000 option annex.give_to_america"
		name = annex.americas.give_to_america
		trigger = { has_event_target = america }
		ai_chance = {
			base = 100
			modifier = { factor = 0 annexations_can_annex = yes }
			modifier = { factor = 0.1 event_target:america = { is_subject = yes } }
		}

		set_temp_variable = { tag_to_release = event_target:america }
		every_state = {
			limit = { is_acadia = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Give to Overlord
		log = "[GetLogInfo]: event annex.2000 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		every_state = {
			limit = { is_acadia = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Integrate
		log = "[GetLogInfo]: event annex.2000 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		every_state = {
			limit = { is_acadia = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2000 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Apacheria
	id = annex.2001
	title = annexation_377
	desc = annexation_377_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			905 = { retain_state = yes }
			if = {
				limit = { 277 = { is_claim_or_core_of_ROOT = yes } } # Mexico City
				save_event_target_as = can_annex
			}
			if = {
				limit = {
					277 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = { ROOT_can_grant_land = yes }
					}
				}
				var:277.owner = { save_event_target_as = mexico }
			}
		}
	}

	option = { # Release Apacheria as Puppet
		log = "[GetLogInfo]: event annex.2001 option annex.2001.a"
		name = annex.2001.a
		trigger = { is_subject = no }
		ai_chance = {
			base = 100
			modifier = { factor = 0 annexations_can_annex = yes }
		}

		set_temp_variable = { tag_to_release = APA } # APA = Apacheria tag
		every_state = {
			377 = { remove_claim_and_add_core_of_target = yes }
		}
		release_targeted_tag = yes

		if = { limit = { ROOT = { has_monarchist_government = yes } } APA = { set_cosmetic_tag = APA_Occupied_Monarchy } }
		if = { limit = { ROOT = { has_republican_government = yes } } APA = { set_cosmetic_tag = APA_Occupied_Republic } }
	}

	option = { # Give Apacheria to Mexico City
		log = "[GetLogInfo]: event annex.2001 option annex.2001.b"
		name = annex.americas.give_to_mexico
		trigger = { has_event_target = mexico }
		ai_chance = {
			base = 70
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = { 
				factor = 0.1 
				event_target:mexico = { is_subject = yes } 
			}
		}
		set_temp_variable = { tag_to_release = event_target:mexico }
		377 = { transfer_if_owned = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2001 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		377 = { transfer_if_owned = yes }
		recheck_annexations = yes
	}

	option = { # Annex Apacheria
		log = "[GetLogInfo]: event annex.2001 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		377 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2001 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Tejas
	id = annex.2002
	title = annexation_383
	desc = annexation_383_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			955 = { retain_state = yes }
			
			if = {
				limit = {
					OR = {
						277 = { is_claim_or_core_of_ROOT = yes } # Mexico City
					}
				}
				save_event_target_as = can_annex
			}

			if = {
				limit = {
					277 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = { ROOT_can_grant_land = yes }
					}
				}
				var:277.owner = { save_event_target_as = mexico }
			}
		}
	}

	option = { # Release American Texas as Puppet
		log = "[GetLogInfo]: event annex.2002 option annex.2002.a"
		name = annex.2002.a
		trigger = { 
			is_subject = no 
			ROOT = {
				OR = {
					tag = ENG
					tag = AME
				}
			}
		}
		ai_chance = {
			base = 60
			modifier = { factor = 0 annexations_can_annex = yes }
		}

		set_temp_variable = { tag_to_release = ATX }
		every_state = {
			limit = { is_tejas = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes

		if = { limit = { ROOT = { has_monarchist_government = yes } } ATX = { set_cosmetic_tag = ATX_Occupied_Monarchy } }
		if = { limit = { ROOT = { has_republican_government = yes } } ATX = { set_cosmetic_tag = ATX_Occupied_Republic } }
	}

	option = { # Release Tejas as Puppet
		log = "[GetLogInfo]: event annex.2002 option annex.2002.a"
		name = annex.2002.b
		trigger = { 
			is_subject = no
			NOT = {
				ROOT = {
					OR = {
						tag = ENG
						tag = AME
					}
				}
			}
		}
		ai_chance = {
			base = 60
			modifier = { factor = 0 annexations_can_annex = yes }
		}

		set_temp_variable = { tag_to_release = TEX }
		every_state = {
			limit = { is_tejas = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes

		if = { limit = { ROOT = { has_monarchist_government = yes } } TEX = { set_cosmetic_tag = TEX_Occupied_Monarchy } }
		if = { limit = { ROOT = { has_republican_government = yes } } TEX = { set_cosmetic_tag = TEX_Occupied_Republic } }
	}

	option = { # Give Tejas to Mexico City
		log = "[GetLogInfo]: event annex.2002 option annex.americas.give_to_mexico"
		name = annex.americas.give_to_mexico
		trigger = { has_event_target = mexico }
		ai_chance = {
			base = 20
			modifier = { factor = 0.1 event_target:mexico = { is_subject = yes } }
		}
		set_temp_variable = { tag_to_release = event_target:mexico }
		every_state = {
			limit = { is_tejas = yes }
			transfer_if_owned = yes
			transfer_state_and_notify = yes
		}
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2002 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		every_owned_state = {
			limit = { is_tejas = yes }
			transfer_state_and_notify = yes
		}
		recheck_annexations = yes
	}

	option = { # Annex Tejas
		log = "[GetLogInfo]: event annex.2002 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		every_owned_state = {
			limit = { is_tejas = yes }
			add_claim_of_ROOT = yes
		}
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2002 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Llano Estacado
	id = annex.2003
	title = annexation_955
	desc = annexation_955_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			if = {
				limit = {
					OR = {
						1428 = { is_claim_or_core_of_ROOT = yes }
						964 = { is_claim_or_core_of_ROOT = yes }
					}
				}
				save_event_target_as = can_annex
			}

			if = {
				limit = {
					1428 = { #Naanapusookuni
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
						}
					}
				}
				var:1428.owner = { save_event_target_as = comanche }
			}
			if = {
				limit = {
					964 = { #Santa Anna
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:comanche }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:964.owner = { save_event_target_as = texas }
			}
		}
	}

	option = { #Give to Texas
		log = "[GetLogInfo]: event annex.2003 option annex.americas.give_to_texas"
		name = annex.americas.give_to_texas
		trigger = {
			has_event_target = texas
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = event_target:texas }
		955 = { transfer_state_and_notify = yes } #Llano Estacado
		
	}

	option = { #Give it to Comanche
		log = "[GetLogInfo]: event annex.2003 option annex.americas.give_to_comanche"
		name = annex.americas.give_to_comanche
		trigger = {
			has_event_target = comanche 
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:comanche = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:comanche }
		955 = { transfer_state_and_notify = yes } #Llano Estacado
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2003 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		955 = { add_claim_of_ROOT = yes } #Llano Estacado
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2003 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}

		set_temp_variable = { tag_to_release = overlord }

		955 = { transfer_if_owned = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2003 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Riogrande
	id = annex.2004
	title = annexation_947
	desc = annexation_947_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {		
			if = {
				limit = {
					OR = {
						277 = { is_claim_or_core_of_ROOT = yes } # Mexico City
						375 = { is_claim_or_core_of_ROOT = yes } # San Antonio
					}
				}
				save_event_target_as = can_annex
			}

			if = {
				limit = {
					277 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = { ROOT_can_grant_land = yes }
					}
				}
				var:277.owner = { save_event_target_as = mexico }
			}
			if = {
				limit = {
					375 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:mexico }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:375.owner = { save_event_target_as = texas }	
			}
		}
	}

	option = { # Release Rio Grande as Puppet
		log = "[GetLogInfo]: event annex.2004 option annex.2004.a"
		name = annex.2004.a
		trigger = { 
			is_subject = no 
		}
		ai_chance = {
			base = 60
			modifier = { factor = 0 annexations_can_annex = yes }
		}

		set_temp_variable = { tag_to_release = RGD }
		every_state = {
			limit = { is_rio_grande = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
	}

	option = { # Give Rio Grande to Mexico City
		log = "[GetLogInfo]: event annex.2004 option annex.americas.give_to_mexico"
		name = annex.americas.give_to_mexico
		trigger = { has_event_target = mexico }
		ai_chance = {
			base = 20
			modifier = { factor = 0.1 event_target:mexico = { is_subject = yes } }
		}
		set_temp_variable = { tag_to_release = event_target:mexico }
		every_state = {
			limit = { is_rio_grande = yes }
			transfer_if_owned = yes
			transfer_state_and_notify = yes
		}
	}

	option = { # Give Rio Grande to Texas
		log = "[GetLogInfo]: event annex.2004 option annex.americas.give_to_texas"
		name = annex.americas.give_to_texas
		trigger = { has_event_target = texas }
		ai_chance = {
			base = 20
			modifier = { factor = 0.1 event_target:mexico = { is_subject = yes } }
		}
		set_temp_variable = { tag_to_release = event_target:texas }
		every_state = {
			limit = { is_rio_grande = yes }
			transfer_if_owned = yes
			transfer_state_and_notify = yes
		}
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2004 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		every_owned_state = {
			limit = { is_rio_grande = yes }
			transfer_state_and_notify = yes
		}
		recheck_annexations = yes
	}

	option = { # Annex Rio Grande
		log = "[GetLogInfo]: event annex.2004 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		every_owned_state = {
			limit = { is_rio_grande = yes }
			add_claim_of_ROOT = yes
		}
	}


	option = { #Cancel
		log = "[GetLogInfo]: event annex.2004 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Hondouras
	id = annex.2007
	title = annexation_312
	desc = annexation_312_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
		779 = { retain_state = yes } # Hondouran Miskito
		1213 = { retain_state = yes } # Teguzgulpa
		147 = { retain_state = yes } # Bay Islands
			annexations_reset_central_american_tags = yes
		}
	}

	option = { #Liberate Hondouras
		log = "[GetLogInfo]: event annex.2007 option annex.2007.a"
		name = annex.2007.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		314 = {
        remove_core_of = HON
        }

		set_temp_variable = { tag_to_release = HON }
		every_state = {
			limit = { is_hondouras = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
		
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			HON = {
				set_cosmetic_tag = HON_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			HON = {
				set_cosmetic_tag = HON_Occupied_Republic
			}
		}
	}

	option = { #Liberate CEN
		log = "[GetLogInfo]: event annexation_events.2007 option annexation_events.americas.release_united_central_america"
		name = annexation_events.americas.release_united_central_america
		trigger = {
			annexations_can_release_united_central_america = yes
		}
		ai_chance = {
			base = 20
			modifier = {
				factor = 0
			}
		}
		annexations_release_united_central_america = yes
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2007 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Yucatan
	id = annex.2008
	title = annexation_474
	desc = annexation_474_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
	MEX  = { save_event_target_as = mexico }
	annexations_reset_central_american_tags = yes
    }
	option = { #Liberate Yucatan
		log = "[GetLogInfo]: event annex.2008 option annex.2008.a"
		name = annex.2008.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = YUC }
		every_state = {
			limit = { is_core_of = YUC }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			YUC = {
				set_cosmetic_tag = YUC_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			YUC = {
				set_cosmetic_tag = YUC_Occupied_Republic
			}
		}
	}

	option = { #Give it to Mexico
		log = "[GetLogInfo]: event annex.2008 option  annex.americas.give_to_mexico"
		name = annex.americas.give_to_mexico
		trigger = {
			has_event_target = mexico
			MEX = {
            exists = yes
		    }   
	    }
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = event_target:mexico }
		474 = { transfer_state_and_notify = yes }
		1407 = { transfer_state_and_notify = yes }
		transfer_targeted_territory = yes
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2008 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
	}

	after = {
		recheck_annexations = yes
	}
}	


country_event = { #Fate of Guatemala
	id = annex.2009
	title = annexation_313
	desc = annexation_313_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		1212 = { retain_state = yes } #Peten
		if = {
			limit = {
				OR = {
					778 = { is_claim_or_core_of_ROOT = yes } # Los Altos
					475 = { is_claim_or_core_of_ROOT = yes } # Chiapas
					is_central_american_tag = yes
				}
			}
			save_event_target_as = can_annex
		}
		if = {
			limit = {
				778 = { # Los Altos
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:778.owner = { save_event_target_as = los_altos }
		}
		if = {
			limit = {
				475 = { # Chiapas
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:los_altos }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:475.owner = { save_event_target_as = chiapas }
		}
		annexations_reset_central_american_tags = yes
    }
	option = { #Liberate Guatemala
		log = "[GetLogInfo]: event annex.2009 option annex.2009.a"
		name = annex.2009.a
		trigger = {
			is_subject = no
		}

		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = GUA }
		313 = { remove_claim_and_add_core_of_target = yes } # Guatemala
		1212  = { remove_claim_and_add_core_of_target = yes } # Peten
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			GUA = {
				set_cosmetic_tag = GUA_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			GUA = {
				set_cosmetic_tag = GUA_Occupied_Republic
			}
		}
	}


	option = { #Give it to Owner of Chiapas
		log = "[GetLogInfo]: event annex.2009 option annex.2009.b"
		name = annex.2009.b
		trigger = {
			has_event_target = chiapas
	    }
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_AI_wants_to_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:chiapas }
				is_ally_with = event_target:los_altos
			}
			modifier = {
				factor = 2
				NOT = { has_event_target = los_altos }
			}
			modifier = {
				factor = 0.1
				event_target:chiapas = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:chiapas }
		313 = { transfer_state_and_notify = yes } # Guatemala
		1212  = { transfer_state_and_notify = yes } # Peten
	}

	option = { #Give it to Los Altos
		log = "[GetLogInfo]: event annex.2009 option annex.americas.give_to_los"
		name =   annex.americas.give_to_los_altos
		trigger = {
			has_event_target = los_altos
	    }
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_AI_wants_to_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:los_altos }
				is_ally_with = event_target:chiapas
			}
			modifier = {
				factor = 2
				NOT = { has_event_target = chiapas }
			}
			modifier = {
				factor = 0.1
				event_target:los_altos = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:los_altos }
		313 = { transfer_state_and_notify = yes } # Guatemala
	}

	option = { #Liberate CEN
		log = "[GetLogInfo]: event annexation_events.2009 option annexation_events.americas.release_united_central_america"
		name = annexation_events.americas.release_united_central_america
		trigger = {
			annexations_can_release_united_central_america = yes
		}
		ai_chance = {
			base = 20
			modifier = {
				factor = 0
			}
		}
		annexations_release_united_central_america = yes
	}

	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2009 option name annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		every_owned_state = { 
			limit = { is_guatemala = yes }
			transfer_state_and_notify = yes
		}
	}

	option = { #Annex
		log = "[GetLogInfo]: event annex.2009 option name annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		every_state = { 
			limit = { is_guatemala = yes }
			add_claim_of_ROOT = yes
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2009 option name annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}	

country_event = { # Fate of Los Altos
	id = annex.2010
	title = annexation_778
	desc = annexation_778_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		1406 = { retain_state = yes } # Angostura

		if = {
			limit = {
				OR = {
					313 = { is_claim_or_core_of_ROOT = yes } # Guatemala
					1406 = { is_claim_or_core_of_ROOT = yes } # Angostura
					is_central_american_tag = yes
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				313 = {
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:313.owner = { save_event_target_as = guatemala }
		}

		if = {
			limit = {
				1406 = {
					owner = {
						NOT = { tag = event_target:guatemala }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:1406.owner = { save_event_target_as = angostura }
		}

		annexations_reset_central_american_tags = yes
	}

	option = { # Liberate Los Altos
		log = "[GetLogInfo]: event annex.2010 option annex.2010.a"
		name = annex.2010.a
		trigger = {
			is_subject = no
		}

		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = LOS }
		778 = { remove_claim_and_add_core_of_target = yes } # Los Altos
		1406 = { remove_claim_and_add_core_of_target = yes } # Angostura
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			LOS = { set_cosmetic_tag = LOS_Occupied_Monarchy }
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			LOS = { set_cosmetic_tag = LOS_Occupied_Republic }
		}
	}

	option = { # Give to Guatemala
		log = "[GetLogInfo]: event annex.2010 option annex.2010.b"
		name = annex.southamerica.give_to_guatemala
		trigger = {
			has_event_target = guatemala
		}

		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_AI_wants_to_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:guatemala }
				is_ally_with = event_target:angostura
			}
			modifier = {
				factor = 2
				NOT = { has_event_target = angostura }
			}
			modifier = {
				factor = 0.1
				event_target:guatemala = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:guatemala }
		778 = { transfer_state_and_notify = yes }
	}

	option = { #give to angostura
		log = "[GetLogInfo]: event annex.2010 option annex.2010.c"
		name = annex.2010.c
		trigger = {
			has_event_target = angostura
		}

		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_AI_wants_to_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:angostura }
				is_ally_with = event_target:guatemala
			}
			modifier = {
				factor = 2
				NOT = { has_event_target = guatemala }
			}
			modifier = {
				factor = 0.1
				event_target:angostura = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:angostura }
		778 = { transfer_state_and_notify = yes }
	}

	option = { #Liberate CEN
		log = "[GetLogInfo]: event annexation_events.2009 option annexation_events.americas.release_united_central_america"
		name = annexation_events.americas.release_united_central_america
		trigger = {
			annexations_can_release_united_central_america = yes
		}
		ai_chance = {
			base = 20
			modifier = {
				factor = 0
			}
		}
		annexations_release_united_central_america = yes
	}

	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2010 option name annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		every_owned_state = { 
			limit = { is_los_altos = yes }
			transfer_state_and_notify = yes
		}
	}

	option = { #Annex
		log = "[GetLogInfo]: event annex.2010 option name annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		every_state = { 
			limit = { is_los_altos = yes }
			add_claim_of_ROOT = yes
		}
	}

	option = {
		log = "[GetLogInfo]: event annex.2010 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}


country_event = { #Fate of Nicaragua
	id = annex.2011
	title = annexation_317
	desc = annexation_317_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		780 = { retain_state = yes } # Miskito Coast
		1214 = { retain_state = yes } # Tologalpa
		1050 = { retain_state = yes } # Providencia
		annexations_reset_central_american_tags = yes
    }
	option = { #Liberate Nicaragua
		log = "[GetLogInfo]: event annex.2011 option annex.2011.a"
		name = annex.2011.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = NIC }
		317 = { remove_claim_and_add_core_of_target = yes } # Managua
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			NIC = { set_cosmetic_tag = NIC_Occupied_Monarchy }
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			NIC = { set_cosmetic_tag = NIC_Occupied_Republic }
		}
	}
	option = { #Liberate CEN
		log = "[GetLogInfo]: event annexation_events.2011 option annexation_events.americas.release_united_central_america"
		name = annexation_events.americas.release_united_central_america
		trigger = {
			annexations_can_release_united_central_america = yes
		}
		ai_chance = {
			base = 20
			modifier = {
				factor = 0
			}
		}
		annexations_release_united_central_america = yes
	}

	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2011 option name annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		every_owned_state = { 
			limit = { is_nicaragua = yes }
			transfer_state_and_notify = yes
		}
	}

	option = { #Annex
		log = "[GetLogInfo]: event annex.2011 option name annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		every_state = { 
			limit = { is_nicaragua = yes }
			add_claim_of_ROOT = yes
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2011 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Oregon
	id = annex.2012
	title = annexation_836
	desc = annexation_836_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {

	}

	option = { #Create Oregon
		log = "[GetLogInfo]: event annex.2012 option annex.2012.a"
		name = annex.2012.a
		trigger = {
			is_subject = no
		}
		ai_chance = { 
			base = 10
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				has_idea = PB_Idea_Untamed_Frontier
			}
			modifier = {
				factor = 0
				original_tag = ENG
			}
		}
		set_temp_variable = { tag_to_release = NWA }
		892 = { remove_claim_and_add_core_of_target = yes } #Gasden
		895 = { remove_claim_and_add_core_of_target = yes } #East Oregon
		897 = { remove_claim_and_add_core_of_target = yes } #North Oregon
		836 = { remove_claim_and_add_core_of_target = yes } #New Virginia
		896 = { remove_claim_and_add_core_of_target = yes } #Gorogorun
		release_targeted_tag = yes
		NWA = {
			set_cosmetic_tag = NWD_Oregon_Generic
			remove_ideas = {
				PB_NWA_Ghosts_TAC
				PB_NWA_Frontier_Democracy
				PB_NWA_Trancendentalist_Vanguard
				PB_Idea_Untamed_Frontier
			}
			set_capital = {
  				state = 836
 				remember_old_capital = no
			}
			clr_country_flag = PB_Anarchy_Wild_West_active
		}
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			NWA = {
				set_cosmetic_tag = NWA_Basin_Occupied_Monarchy
			}
		}
		if = {
			limit = { ROOT = { has_republican_government = yes } }
			NWA = {
				set_cosmetic_tag = NWA_Basin_Occupied_Republic
			}
		}
		if = {
			limit = { ROOT = { tag = AME } }
			NWA = {
				retire_ideology_leader = ds
				set_cosmetic_tag = NWA_AME_Oregon_Territory
				set_politics = {
					ruling_party = ds
					elections_allowed = no
				}
				add_popularity = {
					ideology = ds
					popularity = 0.5
				}
				add_popularity = {
					ideology = cn
					popularity = 0.1
				}
				add_popularity = {
					ideology = lb
					popularity = 0.1
				}
				add_popularity = {
					ideology = jc
					popularity = 0.3
				}
				promote_character = NWA_patrick_ashley_cooper 
				set_party_name = {
					ideology = ds
					long_name = "American Governorship of Oregon"
					name = "Territorial Goverorship"
				}
			}
		}
	}

	option = { #give to the Northwest Territories
		log = "[GetLogInfo]: event annex.2012 option annex.americas.give_to_northwest_territories"
		name = annex.americas.give_to_northwest_territories
		trigger = {
			original_tag = ENG
			BCL = {
				is_subject_of = ROOT
			}
		}
		set_temp_variable = { tag_to_release = BCL }
		892 = { transfer_if_owned = yes } #Gasden
		895 = { transfer_if_owned = yes } #East Oregon
		897 = { transfer_if_owned = yes } #North Oregon
		836 = { transfer_if_owned = yes } #New Virginia
		896 = { transfer_if_owned = yes } #Gorogorun
	}

	option = { #give to overlord
		log = "[GetLogInfo]: event annex.2012 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		892 = { transfer_if_owned = yes } #Gasden
		895 = { transfer_if_owned = yes } #East Oregon
		897 = { transfer_if_owned = yes } #North Oregon
		836 = { transfer_if_owned = yes } #New Virginia
		896 = { transfer_if_owned = yes } #Gorogorun
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2012 option annex.integration"
		name = annex.integration
		trigger = {
			OR = {
				has_idea = PB_Idea_Untamed_Frontier
				annexations_can_annex = yes
			}
		}
		892 = { add_claim_of_ROOT = yes } #Gasden
		895 = { add_claim_of_ROOT = yes } #East Oregon
		897 = { add_claim_of_ROOT = yes } #North Oregon
		836 = { add_claim_of_ROOT = yes } #New Virginia
		896 = { add_claim_of_ROOT = yes } #Gorogorun
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2012 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Gorogorun
	id = annex.2013
	title = annexation_896
	desc = annexation_896_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {

	}

	option = { #Liberate Gorogorun
		log = "[GetLogInfo]: event annex.2013 option annex.2013.a"
		name = annex.2013.a
		trigger = {
			is_subject = no
		}
		ai_chance = { 
			base = 10
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				original_tag = ENG
			}
			modifier = {
				factor = 0
				has_idea = PB_Idea_Untamed_Frontier
			}
			modifier = {
				factor = 0
				NWA = { #Oregon Puppet
					is_subject_of = ROOT
				}
			}
		}
		set_temp_variable = { tag_to_release = MEI }
		896 = { remove_claim_and_add_core_of_target = yes } #Oregon Coast
		release_targeted_tag = yes
		
	}

	option = { #give to the Northwest Territories
		log = "[GetLogInfo]: event annex.2013 option annex.americas.give_to_northwest_territories"
		name = annex.americas.give_to_northwest_territories
		trigger = {
			original_tag = ENG
			BCL = {
				is_subject_of = ROOT
			}
		}
		set_temp_variable = { tag_to_release = BCL }
		896 = { transfer_if_owned = yes } #Oregon Coast
		
	}

	option = { #give to the Oregon
		log = "[GetLogInfo]: event annex.2013 option annex.2013.give_to_oregon"
		name = annex.americas.give_to_oregon
		trigger = {
			NWA = { #Oregon Puppet
				is_subject_of = ROOT
			}
		}
		set_temp_variable = { tag_to_release = NWA }
		896 = { transfer_if_owned = yes } #Oregon Coast
		
	}

	option = { #give to overlord
		log = "[GetLogInfo]: event annex.2013 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		896 = { transfer_if_owned = yes } #Oregon Coast
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2013 option annex.integration"
		name = annex.integration
		trigger = {
			OR = {
				has_idea = PB_Idea_Untamed_Frontier
				annexations_can_annex = yes
			}
		}
		896 = { add_claim_of_ROOT = yes } #Oregon Coast
		
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2013 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of UCA
	id = annex.2014
	title = annexation_360
	desc = annexation_360_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			every_state = {
				if = {
					limit = {
						OR = {
							state = 690 #North Bahamas
							state = 465 # New Brunswick
							state = 464 # Nova Scotia
							state = 950 # Cavalier
							state = 393 # Michigan
							state = 276 #Huron
							state = 394 #Ouisconsin
							state = 817 #Supperiur
						}
					} 
					remove_core_of = AME
					retain_state = yes
				}
				remove_claim_by = AME
			}
		}
	}

	option = { #Liberate Most of the UCA
		log = "[GetLogInfo]: event annex.2014 option annex.2014.a"
		name = annex.2014.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 5
				annexations_AI_country_wants_to_dismantle_america = yes
			}
		}
		set_temp_variable = { tag_to_release = AME }
		every_state = {
			limit = { is_core_of = AME}
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes		
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			AME = {
				set_cosmetic_tag = AME_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			AME = {
				set_cosmetic_tag = AME_Occupied_Republic
			}
		}
	}

	option = { #Articles of Confederation you bum
		log = "[GetLogInfo]: event annex.2014 option annex.2014.b"
		name = annex.2014.b
		trigger = {
			is_subject = no
		}
		
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 5
				annexations_AI_country_wants_to_dismantle_america = yes
			}
		}

		set_global_flag = PB_America_DisUnited
		custom_effect_tooltip = annexations_country_to_be_divided_tt
		hidden_effect = {
			every_state = {
				limit = {
					is_core_of = AME
					NOT = {
						state = 811 #Hamilton
						state = 358 #Albany
						state = 359 #New Jersey
						state = 360 #Pennsylvania
						state = 361 #Maryland
					}
				}
				remove_core_of = AME
			}
		}
		set_temp_variable = { tag_to_release = AME }
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			AME = {
				set_cosmetic_tag = AME_Occupied_Monarchy_Split
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			AME = {
				set_cosmetic_tag = AME_Occupied_Republic_Split
			}
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2014 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}	

country_event = { #Fate of FSA
	id = annex.2015
	title = annexation_368
	desc = annexation_368_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		AME  = { save_event_target_as = america }
    }
	option = { #Liberate FSA
		log = "[GetLogInfo]: event annex.2015 option annex.2015.a"
		name = annex.2015.a
		trigger = {
			is_subject = no
		}
		set_temp_variable = { tag_to_release = FSA }
		every_state = {
			limit = { is_dixieamerica = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			FSA = {
				set_cosmetic_tag = FSA_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			FSA = {
				set_cosmetic_tag = FSA_Occupied_Republic
			}
		}
	}

	option = { #Give it to America
		log = "[GetLogInfo]: event annex.2015 option annex.americas.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			AME = {
            exists = yes
		    }   
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:america = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:america }
		every_state = {
			limit = { is_dixieamerica = yes }
			transfer_if_owned = yes
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2015 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}	
country_event = { # Fate of Virginia
	id = annex.2016
	title = annexation_362
	desc = annexation_362_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					361 = { is_claim_or_core_of_ROOT = yes } # Maryland
					363 = { is_claim_or_core_of_ROOT = yes } # Albemarle
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				361 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:361.owner = { save_event_target_as = maryland }
		}

		if = {
			limit = {
				363 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:maryland }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:363.owner = { save_event_target_as = albemarle }
		}
	}

	option = { #Give it to Maryland
		log = "[GetLogInfo]: event annex.2016 option annex.2016.a"
		name = annex.2016.a
		trigger = { has_event_target = maryland }
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:maryland = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:maryland }
		362 = { transfer_state_and_notify = yes } # Virginia
	}

	option = { #Give it to Albemarle
		log = "[GetLogInfo]: event annex.2016 option annex.2016.b"
		name = annex.2016.b
		trigger = { has_event_target = albemarle }
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:albemarle = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:albemarle }
		362 = { transfer_state_and_notify = yes } # Virginia
	}

	option = { # Give to Overlord
		log = "[GetLogInfo]: event annex.2016 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		362 = { transfer_state_and_notify = yes } # Virginia
	}

	option = { #Annex
		log = "[GetLogInfo]: event annex.2016 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		362 = { add_claim_of_ROOT = yes } # Virginia
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2016 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}


country_event = { # Fate of New Brunswick
	id = annex.2017
	title = annexation_465
	desc = annexation_465_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {

			# Maine owner
			if = {
				limit = {
					357 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = { ROOT_can_grant_land = yes }
					}
				}
				var:357.owner = { save_event_target_as = maine_owner }
			}

			# Saint Lawrence owner
			if = {
				limit = {
					468 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:maine_owner }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:468.owner = { save_event_target_as = saint_lawrence_owner }
			}
		}
	}

	option = { # Give to Maine
		log = "[GetLogInfo]: event annex.2017 option annex.2017.a"
		name = annex.2017.a
		trigger = { has_event_target = maine_owner }
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:maine_owner = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:maine_owner }
		465 = { transfer_state_and_notify = yes } # New Brunswick
	}

	option = { # Give to Saint Lawrence
		log = "[GetLogInfo]: event annex.2017 option annex.2017.b"
		name = annex.2017.b
		trigger = { has_event_target = saint_lawrence_owner }
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:saint_lawrence_owner = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:saint_lawrence_owner }
		465 = { transfer_state_and_notify = yes } # New Brunswick
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2017 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		465 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2017 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}


country_event = { # Fate of Nova Scotia
	id = annex.2018
	title = annexation_464
	desc = annexation_464_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {

			# Maine owner
			if = {
				limit = {
					357 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = { ROOT_can_grant_land = yes }
					}
				}
				var:357.owner = { save_event_target_as = maine_owner }
			}

			# Saint Lawrence owner
			if = {
				limit = {
					468 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:maine_owner }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:468.owner = { save_event_target_as = saint_lawrence_owner }
			}
		}
	}

	option = { # Give to Maine
		log = "[GetLogInfo]: event annex.2018 option annex.2018.a"
		name = annex.2018.a
		trigger = { has_event_target = maine_owner }
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:maine_owner = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:maine_owner }
		464 = { transfer_state_and_notify = yes } # Nova Scotia
	}

	option = { # Give to Saint Lawrence
		log = "[GetLogInfo]: event annex.2018 option annex.2018.b"
		name = annex.2018.b
		trigger = { has_event_target = saint_lawrence_owner }
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:saint_lawrence_owner = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:saint_lawrence_owner }
		464 = { transfer_state_and_notify = yes } # Nova Scotia
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2018 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		464 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2018 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}


country_event = { # Fate of Great Basin
	id = annex.2019
	title = annexation_902
	desc = annexation_902_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		
	}

	option = { # Create Great Basin
		log = "[GetLogInfo]: event annex.2019 option annex.2019.a"
		name = annex.2019.a
		trigger = {
			is_subject = no
		}
		ai_chance = { 
			base = 10
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				has_idea = PB_Idea_Untamed_Frontier
			}
		}
		set_temp_variable = { tag_to_release = NEV }
		902 = { remove_claim_and_add_core_of_target = yes } # Imperio del Interior
		900 = { remove_claim_and_add_core_of_target = yes } # Sierra de los Pinos
		945 = { remove_claim_and_add_core_of_target = yes } # Black Foot
		385 = { remove_claim_and_add_core_of_target = yes } # South Oregon
		899 = { remove_claim_and_add_core_of_target = yes } # Ruby Mountains
		906 = { remove_claim_and_add_core_of_target = yes } # Too’ee Na’Wa
		380 = { remove_claim_and_add_core_of_target = yes } # Nuuchiu
		release_targeted_tag = yes
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			NEV = {
				set_cosmetic_tag = NEV_Basin_Occupied_Monarchy
			}
		}
		if = {
			limit = { ROOT = { has_republican_government = yes } }
			NEV = {
				set_cosmetic_tag = NEV_Basin_Occupied_Republic
			}
		}
		if = {
			limit = { ROOT = { tag = AME } }
			NEV = {
				set_cosmetic_tag = NEV_AME_Basin_Territory
				retire_ideology_leader = ds
				set_politics = {
					ruling_party = ds
					elections_allowed = no
				}
				add_popularity = {
					ideology = ds
					popularity = 0.5
				}
				add_popularity = {
					ideology = cn
					popularity = 0.1
				}
				add_popularity = {
					ideology = lb
					popularity = 0.2
				}
				add_popularity = {
					ideology = jc
					popularity = 0.1
				}
				promote_character = NEV_henry_pyrce
				set_party_name = {
					ideology = ds
					long_name = "American Governorship of the Great Basin"
					name = "Territorial Goverorship"
				}
			}
		}
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2019 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		902 = { transfer_if_owned = yes } # Imperio del Interior
		900 = { transfer_if_owned = yes } # Sierra de los Pinos
		945 = { transfer_if_owned = yes } # Black Foot
		385 = { transfer_if_owned = yes } # South Oregon
		899 = { transfer_if_owned = yes } # Ruby Mountains
		906 = { transfer_if_owned = yes } # Too’ee Na’Wa
		380 = { transfer_if_owned = yes } # Nuuchiu
	}

	option = { # Integrate directly
		log = "[GetLogInfo]: event annex.2019 option annex.integration"
		name = annex.integration
		trigger = {
			OR = {
				has_idea = PB_Idea_Untamed_Frontier
				annexations_can_annex = yes
			}
		}
		902 = { add_claim_of_ROOT = yes } # Imperio del Interior
		900 = { add_claim_of_ROOT = yes } # Sierra de los Pinos
		945 = { add_claim_of_ROOT = yes } # Black Foot
		385 = { add_claim_of_ROOT = yes } # South Oregon
		899 = { add_claim_of_ROOT = yes } # Ruby Mountains
		906 = { add_claim_of_ROOT = yes } # Too’ee Na’Wa
		380 = { add_claim_of_ROOT = yes } # Nuuchiu
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2019 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}
	after = {
		
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of the Rockies
	id = annex.2020
	title = annexation_881
	desc = annexation_881_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = { }

	option = { # Create Rockies
		log = "[GetLogInfo]: event annex.2020 option annex.2020.a"
		name = annex.2020.a
		trigger = {
			is_subject = no
		}
		ai_chance = { 
			base = 10
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				has_idea = PB_Idea_Untamed_Frontier
			}
		}
		set_temp_variable = { tag_to_release = STA }
		881 = { remove_claim_and_add_core_of_target = yes } # Rockefeller
		894 = { remove_claim_and_add_core_of_target = yes } # North Basin
		797 = { remove_claim_and_add_core_of_target = yes } # Lago Salado
		904 = { remove_claim_and_add_core_of_target = yes } # Nephia
		release_targeted_tag = yes
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			STA = {
				set_cosmetic_tag = STA_Rockies_Occupied_Monarchy
			}
		}
		if = {
			limit = { ROOT = { has_republican_government = yes } }
			STA = {
				set_cosmetic_tag = STA_Rockies_Occupied_Republic
			}
		}
		if = {
			limit = { ROOT = { tag = AME } }
			STA = {
				set_cosmetic_tag = STA_AME_Rockies_Territory
				retire_ideology_leader = ds
				set_politics = {
					ruling_party = ds
					elections_allowed = no
				}
				add_popularity = {
					ideology = ds
					popularity = 0.5
				}
				add_popularity = {
					ideology = cn
					popularity = 0.1
				}
				add_popularity = {
					ideology = lb
					popularity = 0.1
				}
				add_popularity = {
					ideology = jc
					popularity = 0.3
				}
				promote_character = STA_baron_fairhaven
				set_party_name = {
					ideology = ds
					long_name = "American Governorship of the Rocky Mountains"
					name = "Territorial Goverorship"
				}
			}
		}
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2020 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		881 = { transfer_if_owned = yes } # Rockefeller
		894 = { transfer_if_owned = yes } # North Basin
		797 = { transfer_if_owned = yes } # Lago Salado
		904 = { transfer_if_owned = yes } # Nephia
	}

	option = { # Integrate directly
		log = "[GetLogInfo]: event annex.2020 option annex.integration"
		name = annex.integration
		trigger = {
			OR = {
				has_idea = PB_Idea_Untamed_Frontier
				annexations_can_annex = yes
			}
		}
		881 = { add_claim_of_ROOT = yes } # Rockefeller
		894 = { add_claim_of_ROOT = yes } # North Basin
		797 = { add_claim_of_ROOT = yes } # Lago Salado
		904 = { add_claim_of_ROOT = yes } # Nephia
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2020 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}
	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of the Great Plains
	id = annex.2021
	title = annexation_373
	desc = annexation_373_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
	}

	option = { # Create Great Plains
		log = "[GetLogInfo]: event annex.2021 option annex.2021.a"
		name = annex.2021.a
		trigger = {
			is_subject = no
		}
		ai_chance = { 
			base = 10
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				has_idea = PB_Idea_Untamed_Frontier
			}
		}
		set_temp_variable = { tag_to_release = NDX }
		373 = { remove_claim_and_add_core_of_target = yes } # Calhoun
		1431 = { remove_claim_and_add_core_of_target = yes } # Jackson
		941 = { remove_claim_and_add_core_of_target = yes } # Kansas
		948 = { remove_claim_and_add_core_of_target = yes } # Stephens
		1410 = { remove_claim_and_add_core_of_target = yes } # Marooneria
		942 = { remove_claim_and_add_core_of_target = yes } # Freetown
		392 = { remove_claim_and_add_core_of_target = yes } # Wazhazhe
		1411 = { remove_claim_and_add_core_of_target = yes } # Ani Ozho
		944 = { remove_claim_and_add_core_of_target = yes } # Norton
		967 = { remove_claim_and_add_core_of_target = yes } # Empire
		1430 = { remove_claim_and_add_core_of_target = yes } # Americaville
		1429 = { remove_claim_and_add_core_of_target = yes } # Guntown
		384 = { remove_claim_and_add_core_of_target = yes } # Kelly
		901 = { remove_claim_and_add_core_of_target = yes } # Historia
		799 = { remove_claim_and_add_core_of_target = yes } # Agraria
		382 = { remove_claim_and_add_core_of_target = yes } # Jefferson
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			NDX = {
				set_cosmetic_tag = NDX_Great_Plains_Occupied_Monarchy
			}
		}
		if = {
			limit = { ROOT = { has_republican_government = yes } }
			NDX = {
				set_cosmetic_tag = NDX_Great_Plains_Occupied_Republic
			}
		}
		if = {
			limit = { ROOT = { tag = AME } }
			NDX = {
				set_cosmetic_tag = NDX_AME_Plains_Territory
				retire_ideology_leader = ds
				set_politics = {
					ruling_party = ds
					elections_allowed = no
				}
				add_popularity = {
					ideology = ds
					popularity = 0.5
				}
				add_popularity = {
					ideology = cn
					popularity = 0.1
				}
				add_popularity = {
					ideology = lb
					popularity = 0.1
				}
				add_popularity = {
					ideology = jc
					popularity = 0.3
				}
				promote_character = NDX_vicary_gibbs
				set_party_name = {
					ideology = ds
					long_name = "American Governorship of the Great Plains"
					name = "Territorial Goverorship"
				}
			}
		}
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2021 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		373 = { transfer_if_owned = yes } # Calhoun
		1431 = { transfer_if_owned = yes } # Jackson
		941 = { transfer_if_owned = yes } # Kansas
		948 = { transfer_if_owned = yes } # Stephens
		1410 = { transfer_if_owned = yes } # Marooneria
		942 = { transfer_if_owned = yes } # Freetown
		392 = { transfer_if_owned = yes } # Wazhazhe
		1411 = { transfer_if_owned = yes } # Ani Ozho
		944 = { transfer_if_owned = yes } # Norton
		967 = { transfer_if_owned = yes } # Empire
		1430 = { transfer_if_owned = yes } # Americaville
		1429 = { transfer_if_owned = yes } # Guntown
		384 = { transfer_if_owned = yes } # Kelly
		901 = { transfer_if_owned = yes } # Historia
		799 = { transfer_if_owned = yes } # Agraria
		382 = { transfer_if_owned = yes } # Jefferson
	}

	option = { # Integrate directly
		log = "[GetLogInfo]: event annex.2021 option annex.integration"
		name = annex.integration
		trigger = {
			OR = {
				has_idea = PB_Idea_Untamed_Frontier
				annexations_can_annex = yes
			}
		}
		373 = { add_claim_of_ROOT = yes } # Calhoun
		1431 = { add_claim_of_ROOT = yes } # Jackson
		941 = { add_claim_of_ROOT = yes } # Kansas
		948 = { add_claim_of_ROOT = yes } # Stephens
		1410 = { add_claim_of_ROOT = yes } # Marooneria
		942 = { add_claim_of_ROOT = yes } # Freetown
		392 = { add_claim_of_ROOT = yes } # Wazhazhe
		1411 = { add_claim_of_ROOT = yes } # Ani Ozho
		944 = { add_claim_of_ROOT = yes } # Norton
		967 = { add_claim_of_ROOT = yes } # Empire
		1430 = { add_claim_of_ROOT = yes } # Americaville
		1429 = { add_claim_of_ROOT = yes } # Guntown
		384 = { add_claim_of_ROOT = yes } # Kelly
		901 = { add_claim_of_ROOT = yes } # Historia
		799 = { add_claim_of_ROOT = yes } # Agraria
		382 = { add_claim_of_ROOT = yes } # Jefferson
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2021 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of CAL
	id = annex.2022
	title = annexation_378
	desc = annexation_378_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		484 = { retain_state = yes } #Baja
		hidden_effect = {
			if = {
				limit = {
					OR = {
						original_tag = MEX
					}
				}
				save_event_target_as = can_annex
			}
		}
	}

	option = { #Liberate CAL
		log = "[GetLogInfo]: event annex.2022 option annex.2022.a"
		name = annex.2022.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = CAL }
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			CAL = {
				set_cosmetic_tag = CAL_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			CAL = {
				set_cosmetic_tag = CAL_Occupied_Republic
			}
		}
	}

	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2022 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		every_owned_state = {
			limit = {
				is_allowed_annexation_state = yes
			}
			transfer_state_and_notify = yes
		}
		recheck_annexations = yes
	}

	option = { #Annex
		log = "[GetLogInfo]: event annex.2022 option name annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		every_owned_state = {
			limit = { is_core_of = CAL }
			add_claim_of_ROOT = yes
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2022 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of BAJA
	id = annex.2023
	title = annexation_484
	desc = annexation_484_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					483 = { is_claim_or_core_of_ROOT = yes } #Sonora
					378 = { is_claim_or_core_of_ROOT = yes } #Los Angeles
				}
			}
			save_event_target_as = can_annex
		}
		if = {
		ROOT = {
			tag = MEX
			}
			484 = {
				add_claim_of_ROOT = yes
			}
		}
		if = {
			limit = {
				483 = { #Sonora
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:483.owner = { save_event_target_as = mexico }
		}
		if = {
			limit = {
				378 = { #Los Angeles
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:mexico }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:378.owner = { save_event_target_as = california }
		}
	}

	option = { #Give to California
		log = "[GetLogInfo]: event annex.2023 option annex.americas.give_to_california"
		name = annex.americas.give_to_california
		trigger = {
			has_event_target = california
		}
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_AI_wants_to_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:california }
				is_ally_with = event_target:mexico
			}
			modifier = {
				factor = 2
				NOT = { has_event_target = mexico }
			}
			modifier = {
				factor = 0.1
				event_target:california = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = CAL }
		484 = { transfer_state_and_notify = yes } #Baja California
	}

	option = { #Give to Mexico
		log = "[GetLogInfo]: event annex.2023 option annex.americas.give_to_mexico"
		name = annex.americas.give_to_mexico
		trigger = {
			has_event_target = mexico
		}
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_AI_wants_to_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:mexico }
				is_ally_with = event_target:california
			}
			modifier = {
				factor = 2
				NOT = { has_event_target = california }
			}
			modifier = {
				factor = 0.1
				event_target:mexico = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = MEX }
		484 = { transfer_state_and_notify = yes } #Baja California
	}

	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2023 option name annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		484 = { transfer_state_and_notify = yes } #Baja California
	}

	option = { #Annex
		log = "[GetLogInfo]: event annex.2023 option name annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		484 = { add_claim_of_ROOT = yes } #Baja California
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2023 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of East Apacheria
	id = annex.2024
	title = annexation_903
	desc = annexation_903_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					903 = { is_claim_or_core_of_ROOT = yes } # Dzil Gozhoo (south)
					796 = { is_claim_or_core_of_ROOT = yes } # Yootoo (North)
					966 = { is_claim_or_core_of_ROOT = yes } # albaqurque
					377 = { is_claim_or_core_of_ROOT = yes } # Ndee
					277 = { is_claim_or_core_of_ROOT = yes } # Mexico City
				}
			}
			save_event_target_as = can_annex
		}

		# Ndee
		if = {
			limit = {
				377 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:377.owner = { save_event_target_as = ndee }
		}

		# Albuquerque
		if = {
			limit = {
				966 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:ndee }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:966.owner = { save_event_target_as = albuquerque }
		}

		# Mexico
		if = {
			limit = {
				277 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:ndee }
						NOT = { tag = event_target:albuquerque }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:277.owner = { save_event_target_as = mexico }
		}
	}

	option = { # Return to Ndee
		log = "[GetLogInfo]: event annex.2024 option annex.americas.give_to_apacheria"
		name = annex.americas.give_to_apacheria
		trigger = { has_event_target = ndee }

		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:ndee = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:ndee }
		903 = { transfer_state_and_notify = yes }
		796 = { transfer_state_and_notify = yes }
	}

	option = { # Return to Albuquerque
		log = "[GetLogInfo]: event annex.2024 option annex.americas.give_to_santafe"
		name = annex.americas.give_to_santafe
		trigger = { has_event_target = albuquerque }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:albuquerque = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:albuquerque }
		903 = { transfer_state_and_notify = yes }
		796 = { transfer_state_and_notify = yes }
	}

	option = { # Return to Mexico
		log = "[GetLogInfo]: event annex.2024 option annex.americas.give_to_mexico"
		name = annex.americas.give_to_mexico
		trigger = { has_event_target = mexico }

		ai_chance = {
			base = 25
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:mexico = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:mexico }
		903 = { transfer_state_and_notify = yes }
		796 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2024 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}

		set_temp_variable = { tag_to_release = overlord }
		903 = { transfer_state_and_notify = yes }
		796 = { transfer_state_and_notify = yes }
	}

	option = { # Annex directly
		log = "[GetLogInfo]: event annex.2024 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}

		903 = { add_claim_of_ROOT = yes }
		796 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2024 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Santa Fe
	id = annex.2025
	title = annexation_908
	desc = annexation_908_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		796 = { retain_state = yes } #Yootoo
		903 = { retain_state = yes } #Dzil Gozhoo
		311 = { retain_state = yes } #Belize
		881 = { retain_state = yes } #Rockefeller
		894 = { retain_state = yes } #Northbasin
		797 = { retain_state = yes } #Lago Salado
		904 = { retain_state = yes } #Nephia
		if = {
			limit = {
				OR = {
					482 = { is_claim_or_core_of_ROOT = yes } #Chihuahua
					483 = { is_claim_or_core_of_ROOT = yes } #Sonora
				}
			}
			save_event_target_as = can_annex
		}
	}

	option = { #Liberate Santa Fe
		log = "[GetLogInfo]: event annex.2025 option annex.2025.a"
		name = annex.2025.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = SFE }
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			SFE = {
				set_cosmetic_tag = SFE_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			SFE = {
				set_cosmetic_tag = SFE_Occupied_Republic
			}
		}
	}

	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2025 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		SFE = { set_transfer_target_cores = yes }
		transfer_targeted_territory = yes
	}

	option = { #Annex
		log = "[GetLogInfo]: event annex.2025 option name annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		every_owned_state = {
			limit = { is_core_of = SFE }
			add_claim_of_ROOT = yes
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2025 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Comanche
	id = annex.2026
	title = annexation_374
	desc = annexation_374_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	option = { #Liberate Comanche
		log = "[GetLogInfo]: event annex.2026 option annex.2026.a"
		name = annex.2026.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = CMC }
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			CMC = {
				set_cosmetic_tag = CMC_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			CMC = {
				set_cosmetic_tag = CMC_Occupied_Republic
			}
		}
	}

	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2026 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		CMC = { set_transfer_target_cores = yes }
		transfer_targeted_territory = yes
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2026 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2026 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Quebec
	id = annex.2027
	title = annexation_1462
	desc = annexation_1462_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			every_state = {
				if = {
					limit = {
						OR = {
							state = 943 # Rainy River
							state = 465 # New Brunswick
							state = 464 # Nova Scotia
							state = 950 # Cavalier
							state = 393 # Michigan
							state = 276 #Huron
							state = 394 #Ouisconsin
							state = 817 #Supperiur
						}
					} 
					remove_core_of = QUE
					retain_state = yes
				}
				remove_claim_by = QUE
			}
		}
	}

	option = { #Liberate Most of Quebec
		log = "[GetLogInfo]: event annex.2027 option annex.2027.a"
		name = annex.2027.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 5
				annexations_AI_country_wants_to_dismantle_america = yes
			}
		}
		set_temp_variable = { tag_to_release = QUE }
		every_state = {
			limit = { is_core_of = QUE}
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
		if = {
			limit = { 
				ROOT = { 
					AND = {
						has_monarchist_government = yes 
						NOT = { tag = ENG }
					}

				}
			}
			QUE = {
				set_cosmetic_tag = QUE_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			QUE = {
				set_cosmetic_tag = QUE_Occupied_Republic
			}
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2027 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}	

country_event = { #Fate of Huron
	id = annex.2028
	title = annexation_276
	desc = annexation_276_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					AND = {
						320 = { is_claim_or_core_of_ROOT = yes } #Upper Canada
						original_tag = QUE
					}
					393 = { is_claim_or_core_of_ROOT = yes } #Michigan
				}
			}
			save_event_target_as = can_annex
		}
		hidden_effect = {
			if = {
				limit = {
					320 = { #Upper Canada
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
						}
					}
				}
				var:320.owner = { save_event_target_as = quebec }
			}
			if = {
				limit = {
					393 = { #Michigan
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:quebec }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:393.owner = { save_event_target_as = michigan }
			}
		}
    }
	option = { #Give to America
		log = "[GetLogInfo]: event annex.2028 option annex.americas.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			has_event_target = michigan
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = event_target:michigan }
		276 = { transfer_state_and_notify = yes } #Huron
		
	}

	option = { #Give it to Quebec
		log = "[GetLogInfo]: event annex.2028 option annex.americas.give_to_quebec"
		name = annex.americas.give_to_quebec
		trigger = {
			has_event_target = quebec 
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				is_ally_with = event_target:michigan
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		276 = { transfer_state_and_notify = yes } #Huron
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2028 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		276 = { add_claim_of_ROOT = yes } #Huron
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2028 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Michigan
	id = annex.2029
	title = annexation_393
	desc = annexation_393_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					AND = {
						276 = { is_claim_or_core_of_ROOT = yes } #Huron
						original_tag = QUE
					}
					950 = { is_claim_or_core_of_ROOT = yes } #Cavallier
				}
			}
			save_event_target_as = can_annex
		}
		hidden_effect = {
			if = {
				limit = {
					276 = { #Huron
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
						}
					}
				}
				var:276.owner = { save_event_target_as = quebec }
			}
			if = {
				limit = {
					950 = { #Cavallier
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:quebec }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:950.owner = { save_event_target_as = america }
			}
		}
    }
	option = { #Give to America
		log = "[GetLogInfo]: event annex.2029 option annex.americas.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			has_event_target = america
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = event_target:america }
		393 = { transfer_state_and_notify = yes } #Michigan
		
	}

	option = { #Give it to Quebec
		log = "[GetLogInfo]: event annex.2029 option annex.americas.give_to_quebec"
		name = annex.americas.give_to_quebec
		trigger = {
			has_event_target = quebec 
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				is_ally_with = event_target:america
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		393 = { transfer_state_and_notify = yes } #Michigan
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2029 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		393 = { add_claim_of_ROOT = yes } #Huron
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2029 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Superieur
	id = annex.2030
	title = annexation_817
	desc = annexation_817_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					AND = {
						682 = { is_claim_or_core_of_ROOT = yes } #L'Acadie Du Nord
						original_tag = QUE
					}
					394 = { is_claim_or_core_of_ROOT = yes } #Ouisconsin
				}
			}
			save_event_target_as = can_annex
		}
		hidden_effect = {
			if = {
				limit = {
					682 = { #L'Acadie Du Nord
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
						}
					}
				}
				var:682.owner = { save_event_target_as = quebec }
			}
			if = {
				limit = {
					394 = { #Ouisconsin
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:quebec }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:394.owner = { save_event_target_as = america }
			}
		}
    }
	option = { #Give to America
		log = "[GetLogInfo]: event annex.2030 option annex.americas.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			has_event_target = america
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = event_target:america }
		817 = { transfer_state_and_notify = yes } #Superieur
		
	}

	option = { #Give it to Quebec
		log = "[GetLogInfo]: event annex.2030 option annex.americas.give_to_quebec"
		name = annex.americas.give_to_quebec
		trigger = {
			has_event_target = quebec 
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				is_ally_with = event_target:america
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		817 = { transfer_state_and_notify = yes } #Superiur
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2030 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		817 = { add_claim_of_ROOT = yes } #Superieur
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2030 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Ouisconsin
	id = annex.2031
	title = annexation_394
	desc = annexation_394_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					AND = {
						817 = { is_claim_or_core_of_ROOT = yes } #Superieur
						original_tag = QUE
					}
					950 = { is_claim_or_core_of_ROOT = yes } #Cavallier
				}
			}
			save_event_target_as = can_annex
		}
		hidden_effect = {
			if = {
				limit = {
					817 = { #Superieur
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
						}
					}
				}
				var:817.owner = { save_event_target_as = quebec }
			}
			if = {
				limit = {
					950 = { #Cavallier
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:quebec }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:950.owner = { save_event_target_as = america }
			}
		}
    }
	option = { #Give to America
		log = "[GetLogInfo]: event annex.2031 option annex.americas.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			has_event_target = america
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = event_target:america }
		394 = { transfer_state_and_notify = yes } #Ouisconsin
		
	}

	option = { #Give it to Quebec
		log = "[GetLogInfo]: event annex.2031 option annex.americas.give_to_quebec"
		name = annex.americas.give_to_quebec
		trigger = {
			has_event_target = quebec 
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				is_ally_with = event_target:america
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		394 = { transfer_state_and_notify = yes } #Ouisconsin
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2031 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		394 = { add_claim_of_ROOT = yes } #Ouisconsin
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2031 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Cavalier
	id = annex.2032
	title = annexation_950
	desc = annexation_950_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					AND = {
						393 = { is_claim_or_core_of_ROOT = yes } #Michigan
						original_tag = QUE
					}
					261 = { is_claim_or_core_of_ROOT = yes } #Ohio
				}
			}
			save_event_target_as = can_annex
		}
		hidden_effect = {
			if = {
				limit = {
					393 = { #Michigan
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
						}
					}
				}
				var:393.owner = { save_event_target_as = quebec }
			}
			if = {
				limit = {
					261 = { #Ohio
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:quebec }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:261.owner = { save_event_target_as = america }
			}
		}
    }
	option = { #Give to America
		log = "[GetLogInfo]: event annex.2032 option annex.americas.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			has_event_target = america
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = event_target:america }
		950 = { transfer_state_and_notify = yes } #Cavallier
		
	}

	option = { #Give it to Quebec
		log = "[GetLogInfo]: event annex.2032 option annex.americas.give_to_quebec"
		name = annex.americas.give_to_quebec
		trigger = {
			has_event_target = quebec 
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				is_ally_with = event_target:america
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		950 = { transfer_state_and_notify = yes } #Cavallier
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2032 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		950 = { add_claim_of_ROOT = yes } #Cavallier
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2032 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of El Salvador
	id = annex.2033
	title = annexation_314
	desc = annexation_314_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					312 = { is_claim_or_core_of_ROOT = yes } #Comayagua
				}
			}
			save_event_target_as = can_annex
		}
		if = {
			limit = {
				312 = { #comayagua
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:312.owner = { save_event_target_as = cuzcatlan }
		}
    }
	option = { #Liberate El Salvador
		log = "[GetLogInfo]: event annex.2033 option annex.2033.a"
		name = annex.2033.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 50
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = ELS }
		every_state = {
			limit = { is_core_of = ELS }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			ELS = {
				set_cosmetic_tag = ELS_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			ELS = {
				set_cosmetic_tag = ELS_Occupied_Republic
			}
		}
	}

	option = { #Give to Cuzcatlan
		log = "[GetLogInfo]: event annex.2033 option annex.americas.give_to_cuzcatlan"
		name = annex.americas.give_to_cuzcatlan
		trigger = {
			has_event_target = cuzcatlan
		}
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_AI_wants_to_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:cuzcatlan }
			}
			modifier = {
				factor = 0.1
				event_target:cuzcatlan = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = HON }
		314 = { transfer_state_and_notify = yes } #El Salvador
	}


	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2033 option name annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		314 = { transfer_state_and_notify = yes } #El Salvador
	}

	option = { #Annex
		log = "[GetLogInfo]: event annex.2033 option name annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		314 = { add_claim_of_ROOT = yes } #El Salvador
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2033 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
	}

	after = {
		recheck_annexations = yes
	}
}	

country_event = { # Fate of New England
	id = annex.2034
	title = annexation_813
	desc = annexation_813_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				AME = {
					ROOT_can_grant_land = yes
				}
			}
			AME = { save_event_target_as = america }
		}
		812 = { retain_state = yes }
	}

	option = { # Release New England
		log = "[GetLogInfo]: event annex.2034 option annex.2034.a"
		name = annex.2034.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 65
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = MIT }
		every_state = {
			limit = { is_new_england = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			MIT = {
				set_cosmetic_tag = MIT_New_England_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			MIT = {
				set_cosmetic_tag = MIT_New_England_Occupied_Republic
			}
		}
	}

	option = { # Give to America
		log = "[GetLogInfo]: event annex.2034 option annex.americas.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			has_event_target = america
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:america = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:america }
		every_state = {
			limit = { is_new_england = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Give to Overlord
		log = "[GetLogInfo]: event annex.2034 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		every_state = {
			limit = { is_new_england = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Integrate
		log = "[GetLogInfo]: event annex.2034 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		every_state = {
			limit = { is_new_england = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2034 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of New Africa
	id = annex.2035
	title = annexation_370
	desc = annexation_370_desc
	picture = GFX_report_PB_Peace_Deals_Africa

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				AME = {
					ROOT_can_grant_land = yes
				}
			}
			AME = { save_event_target_as = america }
		}
	}

	option = { # Release New Africa
		log = "[GetLogInfo]: event annex.2035 option annex.2035.a"
		name = annex.2035.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 65
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = NAF }
		every_state = {
			limit = { is_new_africa = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			NAF = {
				set_cosmetic_tag = NAF_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			NAF = {
				set_cosmetic_tag = NAF_Occupied_Republic
			}
		}
	}

	option = { # Give to America
		log = "[GetLogInfo]: event annex.2035 option annex.americas.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			has_event_target = america
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:america = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:america }
		every_state = {
			limit = { is_new_africa = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Give to Overlord
		log = "[GetLogInfo]: event annex.2035 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		every_state = {
			limit = { is_new_africa = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Integrate
		log = "[GetLogInfo]: event annex.2035 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		every_state = {
			limit = { is_new_africa = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2035 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Seminolia
	id = annex.2036
	title = annexation_366
	desc = annexation_366_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
		if = {
			limit = {
				1409 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:1409.owner = { save_event_target_as = clinton }
			}
		}
	}

	option = { # Puppet Seminolia
		log = "[GetLogInfo]: event annex.2036 option annex.2036.a"
		name = annex.2036.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 100
			modifier = { factor = 0 }
		}

		set_temp_variable = { tag_to_release = SML }
		366 = { remove_claim_and_add_core_of_target = yes }
		release_targeted_tag = yes

		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			SML = { set_cosmetic_tag = SML_Occupied_Monarchy }
		}
		if = {
			limit = { ROOT = { has_republican_government = yes } }
			SML = { set_cosmetic_tag = SML_Occupied_Republic }
		}
	}

	option = { # Give to owner of Clinton
		log = "[GetLogInfo]: event annex.2036 option annex.2036.b"
		name = annex.2036.b
		trigger = {
			has_event_target = clinton
		}
		ai_chance = {
			base = 70
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:clinton = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:clinton }
		366 = { transfer_if_owned = yes } # Seminolia
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2036 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		366 = { transfer_if_owned = yes }
	}

	option = { # Integrate
		log = "[GetLogInfo]: event annex.2036 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		366 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2036 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Vermont
	id = annex.2037
	title = annexation_812
	desc = annexation_812_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					358 = { is_claim_or_core_of_ROOT = yes } # Albany
					815 = { is_claim_or_core_of_ROOT = yes } # New Hampshire (North)
					468 = { is_claim_or_core_of_ROOT = yes } # Saint Lawrence
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				358 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:358.owner = { save_event_target_as = albany }
		}

		if = {
			limit = {
				815 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:albany }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:815.owner = { save_event_target_as = new_hampshire_north }
		}

		if = {
			limit = {
				468 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:albany }
						NOT = { tag = event_target:new_hampshire_north }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:468.owner = { save_event_target_as = stlawrence }
		}
	}

	option = { # Return to Albany
		log = "[GetLogInfo]: event annex.2037 option annex.2037.a"
		name = annex.2037.a
		trigger = { has_event_target = albany }
		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:albany = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:albany }
		812 = { transfer_state_and_notify = yes } # Vermont
	}

	option = { # Return to New Hampshire (North)
		log = "[GetLogInfo]: event annex.2037 option annex.2037.b"
		name = annex.2037.b
		trigger = { has_event_target = new_hampshire_north }
		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:new_hampshire_north = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:new_hampshire_north }
		812 = { transfer_state_and_notify = yes } # Vermont
	}

	option = { # Return to sint liwrince
		log = "[GetLogInfo]: event annex.2037 option annex.2037.c"
		name = annex.2037.c
		trigger = { has_event_target = stlawrence }
		ai_chance = {
			base = 25
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:stlawrence = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:stlawrence }
		812 = { transfer_state_and_notify = yes } # Vermont
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2037 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		812 = { transfer_state_and_notify = yes }
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2037 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		812 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2037 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Ongtupqa
	id = annex.2038
	title = annexation_905
	desc = annexation_905_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR =  {
					905 = { is_claim_or_core_of_ROOT = yes }
					tag = APA
				}
			}
			save_event_target_as = can_annex
		}

		# Ndee
		if = {
			limit = {
				377 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:377.owner = { save_event_target_as = ndee }
		}

		# Imperio del Interior
		if = {
			limit = {
				902 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:ndee }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:902.owner = { save_event_target_as = imperio_del_interior }
		}
	}

	option = { # Give to Ndee
		log = "[GetLogInfo]: event annex.2038 option annex.2038.a"
		name = annex.americas.give_to_apacheria
		trigger = { has_event_target = ndee }

		ai_chance = {
			base = 55
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:ndee = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:ndee }
		905 = { transfer_state_and_notify = yes }
	}

	option = { # Give to Imperio del Interior
		log = "[GetLogInfo]: event annex.2038 option annex.2038.b"
		name = annex.2038.b
		trigger = { has_event_target = imperio_del_interior }

		ai_chance = {
			base = 45
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:imperio_del_interior = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:imperio_del_interior }
		905 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2038 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}

		set_temp_variable = { tag_to_release = overlord }
		905 = { transfer_state_and_notify = yes }
	}

	option = { # Annex directly
		log = "[GetLogInfo]: event annex.2038 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}

		905 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2038 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Labrador
	id = annex.2039
	title = annexation_332
	desc = annexation_332_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = { 
					332 = { is_claim_or_core_of_ROOT = yes }
					816 = { is_claim_or_core_of_ROOT = yes }
					464 = { is_claim_or_core_of_ROOT = yes }
				}
			}
			save_event_target_as = can_annex
		}

		# Owner of 464
		if = {
			limit = {
				464 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:464.owner = { save_event_target_as = novascotia }
		}

		# Owner of 816
		if = {
			limit = {
				816 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:novascotia }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:816.owner = { save_event_target_as = quebec }
		}
	}

	option = { # Give to novascoatia
		log = "[GetLogInfo]: event annex.2039 option annex.2039.a"
		name = annex.2039.a
		trigger = { has_event_target = novascotia }

		ai_chance = {
			base = 55
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:novascotia = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:novascotia }
		332 = { transfer_state_and_notify = yes }
	}

	option = { # Give to owner of 816
		log = "[GetLogInfo]: event annex.2039 option annex.2039.b"
		name = annex.2039.b
		trigger = { has_event_target = quebec }

		ai_chance = {
			base = 45
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		332 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2039 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }

		set_temp_variable = { tag_to_release = overlord }
		332 = { transfer_state_and_notify = yes }
	}

	option = { # Annex directly
		log = "[GetLogInfo]: event annex.2039 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }

		332 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Newfoundland
	id = annex.2040
	title = annexation_331
	desc = annexation_331_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = { 
					331 = { is_claim_or_core_of_ROOT = yes }
					816 = { is_claim_or_core_of_ROOT = yes }
					464 = { is_claim_or_core_of_ROOT = yes }
				}
			}
			save_event_target_as = can_annex
		}

		# Owner of 464
		if = {
			limit = {
				464 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:464.owner = { save_event_target_as = novascotia }
		}

		# Owner of 816
		if = {
			limit = {
				816 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:novascotia }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:816.owner = { save_event_target_as = quebec }
		}
	}

	option = { # Give to Nova Scotia
		log = "[GetLogInfo]: event annex.2040 option annex.2040.a"
		name = annex.2040.a
		trigger = { has_event_target = novascotia }

		ai_chance = {
			base = 55
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:novascotia = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:novascotia }
		331 = { transfer_state_and_notify = yes }
	}

	option = { # Give to Quebec
		log = "[GetLogInfo]: event annex.2040 option annex.americas.give_to_quebec"
		name = annex.americas.give_to_quebec
		trigger = { has_event_target = quebec }

		ai_chance = {
			base = 45
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		331 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2040 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }

		set_temp_variable = { tag_to_release = overlord }
		331 = { transfer_state_and_notify = yes }
	}

	option = { # Annex directly
		log = "[GetLogInfo]: event annex.2040 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }

		331 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Peten
	id = annex.2041
	title = annexation_1212
	desc = annexation_1212_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					311 = { is_claim_or_core_of_ROOT = yes } # Belize
					313 = { is_claim_or_core_of_ROOT = yes } # Guatemala
					778 = { is_claim_or_core_of_ROOT = yes } # Los Altos
					1406 = { is_claim_or_core_of_ROOT = yes } # Angostura
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				311 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:311.owner = { save_event_target_as = belize }
		}

		if = {
			limit = {
				313 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:belize }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:313.owner = { save_event_target_as = guatemala }
		}

		if = {
			limit = {
				778 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:belize }
						NOT = { tag = event_target:guatemala }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:778.owner = { save_event_target_as = los }
		}

		if = {
			limit = {
				1406 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:belize }
						NOT = { tag = event_target:guatemala }
						NOT = { tag = event_target:los }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:1406.owner = { save_event_target_as = angostura }
		}
	}

	option = { # give to belize
		log = "[GetLogInfo]: event annex.2041 option annex.2041.a"
		name = annex.americas.give_to_belize
		trigger = { has_event_target = belize }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:belize = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:belize }
		1212 = { transfer_state_and_notify = yes } # Peten
	}

	option = { # give to guatemala
		log = "[GetLogInfo]: event annex.2041 option annex.southamerica.give_to_guatemala"
		name = annex.southamerica.give_to_guatemala
		trigger = { has_event_target = guatemala }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:guatemala = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:guatemala }
		1212 = { transfer_state_and_notify = yes }
	}

	option = { # give to los altos
		log = "[GetLogInfo]: event annex.2041 option annex.americas.give_to_los"
		name = annex.americas.give_to_los
		trigger = { has_event_target = los}

		ai_chance = {
			base = 20
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:los = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:los }
		1212 = { transfer_state_and_notify = yes }
	}

	option = { # give to angostura
		log = "[GetLogInfo]: event annex.2041 option annex.2041.d"
		name = annex.2041.d
		trigger = { has_event_target = angostura }

		ai_chance = {
			base = 15
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:angostura = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:angostura }
		1212 = { transfer_state_and_notify = yes }
	}

	option = { #give_to_overlord
		log = "[GetLogInfo]: event annex.2041 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		1212 = { transfer_state_and_notify = yes }
	}

	option = { #integrate
		log = "[GetLogInfo]: event annex.2041 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		1212 = { add_claim_of_ROOT = yes }
	}

	option = { # cancel
		log = "[GetLogInfo]: event annex.2041 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Angostura
	id = annex.2042
	title = annexation_1406
	desc = annexation_1406_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					475 = { is_claim_or_core_of_ROOT = yes } # Chiapas
					778 = { is_claim_or_core_of_ROOT = yes } # Los Altos
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				475 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:475.owner = { save_event_target_as = chiapas }
		}

		if = {
			limit = {
				778 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:chiapas }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:778.owner = { save_event_target_as = los_altos }
		}
	}

	option = {
		log = "[GetLogInfo]: event annex.2042 option annex.2042.a"
		name = annex.2042.a
		trigger = { has_event_target = chiapas }

		ai_chance = {
			base = 45
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:chiapas = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:chiapas }
		1406 = { transfer_state_and_notify = yes } # Angostura
	}

	option = {
		log = "[GetLogInfo]: event annex.2042 option annex.2042.b"
		name = annex.2042.b
		trigger = { has_event_target = los_altos }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:los_altos = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:los_altos }
		1406 = { transfer_state_and_notify = yes } # Angostura
	}

	option = {
		log = "[GetLogInfo]: event annex.2042 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		1406 = { transfer_state_and_notify = yes }
	}

	option = {
		log = "[GetLogInfo]: event annex.2042 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		1406 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2042 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Chiapas
	id = annex.2043
	title = annexation_475
	desc = annexation_475_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					476 = { is_claim_or_core_of_ROOT = yes } # Oaxaca
					1406 = { is_claim_or_core_of_ROOT = yes } # Angostura
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				476 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:476.owner = { save_event_target_as = oaxaca }
		}

		if = {
			limit = {
				1406 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:oaxaca }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:1406.owner = { save_event_target_as = angostura }
		}
	}

	option = { # Return to Oaxaca
		log = "[GetLogInfo]: event annex.2043 option annex.2043.a"
		name = annex.2043.a
		trigger = { has_event_target = oaxaca }

		ai_chance = {
			base = 45
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:oaxaca = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:oaxaca }
		475 = { transfer_state_and_notify = yes } # Chiapas
	}

	option = { # Return to Angostura
		log = "[GetLogInfo]: event annex.2043 option annex.2043.b"
		name = annex.2043.b
		trigger = { has_event_target = angostura }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:angostura = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:angostura }
		475 = { transfer_state_and_notify = yes } # Chiapas
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2043 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		475 = { transfer_state_and_notify = yes }
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2043 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		475 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2043 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}


country_event = { # Fate of Tobasco
	id = annex.2044
	title = annexation_1211
	desc = annexation_1211_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					477 = { is_claim_or_core_of_ROOT = yes } # Veracruz
					474 = { is_claim_or_core_of_ROOT = yes } # Yucatan
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				477 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:477.owner = { save_event_target_as = veracruz }
		}

		if = {
			limit = {
				474 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:veracruz }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:474.owner = { save_event_target_as = yucatan }
		}
	}

	option = { # Return to veracruz
		log = "[GetLogInfo]: event annex.2044 option annex.2044.a"
		name = annex.2044.a
		trigger = { has_event_target = veracruz }

		ai_chance = {
			base = 45
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:veracruz = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:veracruz }
		1211 = { transfer_state_and_notify = yes } # tobasco
	}

	option = { # Return to Yucatan
		log = "[GetLogInfo]: event annex.2044 option annex.2044.b"
		name = annex.2044.b
		trigger = { has_event_target = yucatan }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:yucatan = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:yucatan }
		1211 = { transfer_state_and_notify = yes } # Tobasco
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2044 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		1211 = { transfer_state_and_notify = yes }
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2044 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		1211 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2044 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Tologalpa
	id = annex.2045
	title = annexation_1214
	desc = annexation_1214_desc
	picture = GFX_report_PB_Peace_Deals_Central_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					317 = { is_claim_or_core_of_ROOT = yes } # Managua
					780 = { is_claim_or_core_of_ROOT = yes } # Miskito Coast
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				317 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:317.owner = { save_event_target_as = managua }
		}

		if = {
			limit = {
				780 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:managua }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:780.owner = { save_event_target_as = miskito }
		}
	}

	option = { # Return to Managua
		name = annex.2045.a
		trigger = { has_event_target = managua }
		ai_chance = {
			base = 55
			modifier = {
				factor = 0.1
				event_target:managua = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:managua }
		1214 = { transfer_state_and_notify = yes }
	}

	option = { # Return to Miskito Coast
		name = annex.2045.b
		trigger = { has_event_target = miskito }
		ai_chance = {
			base = 35
			modifier = {
				factor = 0.1
				event_target:miskito = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:miskito }
		1214 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		1214 = { transfer_state_and_notify = yes }
	}

	option = { # Annex
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		1214 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Honduran Miskito
	id = annex.2046
	title = annexation_779
	desc = annexation_779_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					1213 = { is_claim_or_core_of_ROOT = yes } # Teguzgulpa
					780 = { is_claim_or_core_of_ROOT = yes } # Miskito Coast
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				1213 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:1213.owner = { save_event_target_as = teguzgulpa }
		}

		if = {
			limit = {
				780 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:teguzgulpa }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:780.owner = { save_event_target_as = miskito }
		}
	}

	option = { # Return to Teguzgulpa
		log = "[GetLogInfo]: event annex.2046 option annex.2046.a"
		name = annex.2046.a
		trigger = { has_event_target = teguzgulpa }

		ai_chance = {
			base = 40
			modifier = { 
				factor = 0 
				annexations_can_annex = yes
			}
			modifier = { 
				factor = 0.1 
				event_target:teguzgulpa = { is_subject = yes } 
			}
		}

		set_temp_variable = { tag_to_release = event_target:teguzgulpa }
		779 = { transfer_state_and_notify = yes }
	}

	option = { # Return to Miskito Coast
		log = "[GetLogInfo]: event annex.2046 option annex.2046.b"
		name = annex.2046.b
		trigger = { has_event_target = miskito }

		ai_chance = {
			base = 30
			modifier = { 
				factor = 0 
				annexations_can_annex = yes 
			}
			modifier = { 
				factor = 0.1
				event_target:miskito = { is_subject = yes } 
			}
		}

		set_temp_variable = { tag_to_release = event_target:miskito }
		779 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2046 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		779 = { transfer_state_and_notify = yes }
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2046 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		779 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2046 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Providencia
	id = annex.2047
	title = annexation_1050
	desc = annexation_1050_desc
	picture = GFX_report_PB_Peace_Deals_Caribbean

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					780 = { is_claim_or_core_of_ROOT = yes } # Miskito Coast
					306 = { is_claim_or_core_of_ROOT = yes } # Cundinamarca
					317 = { is_claim_or_core_of_ROOT = yes } # Managua
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				780 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:780.owner = { save_event_target_as = miskito }
		}

		if = {
			limit = {
				306 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:miskito }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:306.owner = { save_event_target_as = cundinamarca }
		}

		if = {
			limit = {
				317 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:miskito }
						NOT = { tag = event_target:cundinamarca }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:317.owner = { save_event_target_as = managua }
		}
	}

	option = { # Return to Miskito Coast
		log = "[GetLogInfo]: event annex.2047 option annex.2047.a"
		name = annex.2047.a
		trigger = { has_event_target = miskito }
		ai_chance = {
			base = 55
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:miskito = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:miskito }
		1050 = { transfer_state_and_notify = yes }
	}

	option = { # Return to Cundinamarca
		log = "[GetLogInfo]: event annex.2047 option annex.2047.b"
		name = annex.2047.b
		trigger = { has_event_target = cundinamarca }
		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:cundinamarca = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:cundinamarca }
		1050 = { transfer_state_and_notify = yes }
	}

	option = { # Return to Managua
		log = "[GetLogInfo]: event annex.2047 option annex.2047.c"
		name = annex.2047.c
		trigger = { has_event_target = managua }
		ai_chance = {
			base = 25
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:managua = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:managua }
		1050 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2047 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		1050 = { transfer_state_and_notify = yes }
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2047 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		1050 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2047 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Tegucigalpa
	id = annex.2048
	title = annexation_1213
	desc = annexation_1213_desc
	picture = GFX_report_PB_Peace_Deals_Central_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					779 = { is_claim_or_core_of_ROOT = yes } # Miskito
					312 = { is_claim_or_core_of_ROOT = yes } # Comayagua
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				779 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:779.owner = { save_event_target_as = miskito }
		}

		if = {
			limit = {
				312 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:miskito }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:312.owner = { save_event_target_as = comayagua }
		}
	}

	option = { # Return to Miskito
		log = "[GetLogInfo]: event annex.2048 option annex.2048.a"
		name = annex.2048.a
		trigger = { has_event_target = miskito }

		ai_chance = {
			base = 40
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:miskito = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:miskito }
		1213 = { transfer_state_and_notify = yes } # Tegucigalpa
	}

	option = { # Return to Comayagua
		log = "[GetLogInfo]: event annex.2048 option annex.2048.b"
		name = annex.2048.b
		trigger = { has_event_target = comayagua }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:comayagua = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:comayagua }
		1213 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2048 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		1213 = { transfer_state_and_notify = yes }
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2048 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		1213 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2048 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Miskito Coast
	id = annex.2049
	title = annexation_780
	desc = annexation_780_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			if = {
				limit = { 317 = { is_claim_or_core_of_ROOT = yes } } # Nicaragua
				save_event_target_as = can_annex
			}

			if = {
				limit = {
					317 = {
						annexations_state_owner_is_valid_grant_target = yes
						owner = { ROOT_can_grant_land = yes }
					}
				}
				var:317.owner = { save_event_target_as = nicaragua }
			}
		}
	}

	option = { # Release Miskito
		log = "[GetLogInfo]: event annex.2049 option annex.2049.a"
		name = annex.2049.a
		trigger = { is_subject = no }
		ai_chance = { base = 100 }

		set_temp_variable = { tag_to_release = MIS } # MIS = Miskito tag
		780 = { remove_claim_and_add_core_of_target = yes }
		release_targeted_tag = yes
	}

	option = { # Give to Nicaragua
		log = "[GetLogInfo]: event annex.2049 option annex.2049.b"
		name = annex.2049.b
		trigger = { has_event_target = nicaragua }
		ai_chance = {
			base = 70
			modifier = { factor = 0 annexations_can_annex = yes }
			modifier = { factor = 0.1 event_target:nicaragua = { is_subject = yes } }
		}

		set_temp_variable = { tag_to_release = event_target:nicaragua }
		780 = { transfer_if_owned = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2049 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		780 = { transfer_if_owned = yes }
		recheck_annexations = yes
	}

	option = { # Annex Miskito Coast
		log = "[GetLogInfo]: event annex.2049 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		780 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2049 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of Mexico
	id = annex.2050
	title = annexation_277
	desc = annexation_277_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			every_state = {
				if = {
					limit = {
						OR = {
							state = 479 #San Luis Potosi
							state = 480 # Coahuila
							state = 475 # Coahuila
							state = 1211 # Coahuila
						}
					} 
					remove_core_of = MEX
					retain_state = yes
				}
				remove_claim_by = MEX
			}
		}
	}

	option = { # Release Mexico
		log = "[GetLogInfo]: event annex.2050 option annex.2050.a"
		name = annex.2050.a
		trigger = { is_subject = no }
		ai_chance = {
			base = 65
			modifier = { factor = 0 annexations_can_annex = yes }
		}

		set_temp_variable = { tag_to_release = MEX } # Acadia tag
		every_state = {
			limit = { is_mexico = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes

		if = { limit = { ROOT = { has_monarchist_government = yes } } MEX = { set_cosmetic_tag = MEX_Occupied_Monarchy } }
		if = { limit = { ROOT = { has_republican_government = yes } } MEX = { set_cosmetic_tag = MEX_Occupied_Republic } }
	}

	option = { # Give to Overlord
		log = "[GetLogInfo]: event annex.2050 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }
		set_temp_variable = { tag_to_release = overlord }
		every_state = {
			limit = { is_mexico = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Integrate
		log = "[GetLogInfo]: event annex.2050 option annex.integration"
		name = annex.integration
		trigger = { annexations_can_annex = yes }
		every_state = {
			limit = { is_mexico = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2050 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = { recheck_annexations = yes }
}

country_event = { # Fate of the Ohio River
	id = annex.2051
	title = annexation_261
	desc = annexation_261_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				AME = { ROOT_can_grant_land = yes }
			}
			AME = { save_event_target_as = america }
		}

		if = {
			limit = {
				QUE = { ROOT_can_grant_land = yes }
			}
			QUE = { save_event_target_as = quebec }
		}
	}

	option = { # Release Ohio River
		log = "[GetLogInfo]: event annex.2051 option annex.2051.a"
		name = annex.2051.a
		trigger = { is_subject = no }

		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = USA }
		every_state = {
			limit = { is_ohio_river = yes }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
		if = {
			limit = { ROOT = { has_monarchist_government = yes } }
			USA = {
				set_cosmetic_tag = USA_OhioRiver_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			USA = {
				set_cosmetic_tag = USA_OhioRiver_Occupied_Republic
			}
		}
	}

	option = { # Give to America
		log = "[GetLogInfo]: event annex.2051 option annex.give_to_america"
		name = annex.americas.give_to_america
		trigger = {
			has_event_target = america
		}

		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:america = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:america }
		every_state = {
			limit = { is_ohio_river = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Give to Quebec
		log = "[GetLogInfo]: event annex.2051 option annex.give_to_quebec"
		name = annex.americas.give_to_quebec
		trigger = {
			has_event_target = quebec
		}

		ai_chance = {
			base = 70
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		every_state = {
			limit = { is_ohio_river = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Give to Overlord
		log = "[GetLogInfo]: event annex.2051 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}

		set_temp_variable = { tag_to_release = overlord }
		every_state = {
			limit = { is_ohio_river = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Integrate
		log = "[GetLogInfo]: event annex.2051 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}

		every_state = {
			limit = { is_ohio_river = yes }
			transfer_if_owned = yes
		}
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2051 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Vandalia
	id = annex.2052
	title = annexation_810
	desc = annexation_810_desc
	picture = GFX_report_PB_Peace_Deals_North_America

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					261 = { is_claim_or_core_of_ROOT = yes } # Ohio
					362 = { is_claim_or_core_of_ROOT = yes } # Virginia
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				261 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { ROOT_can_grant_land = yes }
				}
			}
			var:261.owner = { save_event_target_as = ohio }
		}

		if = {
			limit = {
				362 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:ohio }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:362.owner = { save_event_target_as = virginia }
		}
	}

	option = { # Return to Ohio
		log = "[GetLogInfo]: event annex.2052 option annex.2052.a"
		name = annex.2052.a
		trigger = { has_event_target = ohio }

		ai_chance = {
			base = 45
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:ohio = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:ohio }
		810 = { transfer_state_and_notify = yes } # Vandalia
		369 = { transfer_state_and_notify = yes }
	}

	option = { # Return to Virginia
		log = "[GetLogInfo]: event annex.2052 option annex.2052.b"
		name = annex.2052.b
		trigger = { has_event_target = virginia }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:virginia = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:virginia }
		810 = { transfer_state_and_notify = yes }
		369 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2052 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		810 = { transfer_state_and_notify = yes }
		369 = { transfer_state_and_notify = yes }
	}

	option = { # Annex Vandalia
		log = "[GetLogInfo]: event annex.2052 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		810 = { add_claim_of_ROOT = yes }
		369 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2052 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { # Fate of Epstein Island
	id = annex.2053
	title = annexation_824
	desc = annexation_824_desc
	picture = GFX_report_PB_Peace_Deals_Caribbean

	is_triggered_only = yes

	immediate = {
		annexations_create_west_indies_federation_event_target = yes

		if = {
			limit = {
				OR = {
					1369 = { is_claim_or_core_of_ROOT = yes } # Puerto Rico
					686 = { is_claim_or_core_of_ROOT = yes } # Dutch Antilles
					original_tag = WIS
				}
			}
			save_event_target_as = can_annex
		}

		if = {
			limit = {
				1369 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = { 
						ROOT_can_grant_land = yes 
						NOT = { tag = event_target:west_indies }
					}
				}
			}
			var:1369.owner = { save_event_target_as = puerto_rico }
		}

		if = {
			limit = {
				686 = {
					annexations_state_owner_is_valid_grant_target = yes
					owner = {
						NOT = { tag = event_target:puerto_rico }
						NOT = { tag = event_target:west_indies }
						ROOT_can_grant_land = yes
					}
				}
			}
			var:686.owner = { save_event_target_as = dutch_antilles }
		}
	}

	option = { # Give to West Indies
        log = "[GetLogInfo]: event annex.2053 option annex.southamerica.give_to_westindies"
        name = annex.southamerica.give_to_westindies
        trigger = { has_event_target = west_indies }
        ai_chance = {
            base = 0
            modifier = {
                add = 100
                has_subject = event_target:west_indies
                annexations_can_annex = no
            }
        }
        set_temp_variable = { tag_to_release = event_target:west_indies }
        824 = { transfer_state_and_notify = yes } # Epstein island
    }

	option = { # Return to Puerto Rico
		log = "[GetLogInfo]: event annex.2053 option annex.2053.a"
		name = annex.2053.a
		trigger = { has_event_target = puerto_rico }

		ai_chance = {
			base = 45
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:puerto_rico = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:puerto_rico }
		824 = { transfer_state_and_notify = yes }
	}

	option = { # Return to Dutch Antilles
		log = "[GetLogInfo]: event annex.2053 option annex.2053.b"
		name = annex.2053.b
		trigger = { has_event_target = dutch_antilles }

		ai_chance = {
			base = 35
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:dutch_antilles = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:dutch_antilles }
		824 = { transfer_state_and_notify = yes }
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2053 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }
		824 = { transfer_state_and_notify = yes }
	}

	option = { # Annex
		log = "[GetLogInfo]: event annex.2053 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		824 = { add_claim_of_ROOT = yes }
	}

	option = { # Cancel
		log = "[GetLogInfo]: event annex.2053 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Condeferacy of Free Tribes
	id = annex.2054
	title = annexation_388
	desc = annexation_388_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		
	}

	option = { #Liberate All of Confederacy of Free Tribes
		log = "[GetLogInfo]: event annex.2054 option annex.2054.a"
		name = annex.2054.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = CFT }
		every_state = {
			limit = { is_core_of = CFT }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
	}

	option = { # Give to overlord
		log = "[GetLogInfo]: event annex.2054 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = { annexations_should_give_lands_to_overlord = yes }

		set_temp_variable = { tag_to_release = overlord }
		388 = { transfer_state_and_notify = yes }
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2054 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}	

country_event = { #Northwest Territory Colonial Authority
	id = annex.2055
	title = annexation_891
	desc = annexation_891_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			every_state = {
				if = {
					limit = {
						OR = {
							state = 943 # Rainy River
							state = 740 # Victoria Island
						}
					} 
					remove_core_of = BCL
					retain_state = yes
				}
				remove_claim_by = BCL
			}
		}
	}

	option = { #Liberate Most of Northwest Territory Colonial Authority
		log = "[GetLogInfo]: event annex.2055 option annex.2055.a"
		name = annex.2055.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = BCL }
		every_state = {
			limit = { is_core_of = BCL }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
		if = {
			limit = { 
				ROOT = { 
					AND = {
						has_monarchist_government = yes 
						NOT = { tag = ENG }
					}

				}
			}
			BCL = {
				set_cosmetic_tag = BCL_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			BCL = {
				set_cosmetic_tag = BCL_Occupied_Republic
			}
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2055 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}	

country_event = { #Fate of Rainy River
	id = annex.2056
	title = annexation_943
	desc = annexation_943_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					AND = {
						682 = { is_claim_or_core_of_ROOT = yes } #L'Acadie du Nord
						original_tag = QUE
					}
					683 = { is_claim_or_core_of_ROOT = yes } #Keewatin
				}
			}
			save_event_target_as = can_annex
		}
		hidden_effect = {
			if = {
				limit = {
					682 = { #L'Acadie du Nord
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
						}
					}
				}
				var:682.owner = { save_event_target_as = quebec }
			}
			if = {
				limit = {
					683 = { #Keewatin
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:quebec }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:683.owner = { save_event_target_as = borealia }
			}
		}
    }
	option = { #Give to Borealia
		log = "[GetLogInfo]: event annex.2056 option annex.americas.give_to_northwest_territories"
		name = annex.americas.give_to_northwest_territories
		trigger = {
			has_event_target = borealia
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = event_target:borealia }
		943 = { transfer_state_and_notify = yes } #Rainy River
		
	}

	option = { #Give it to Quebec
		log = "[GetLogInfo]: event annex.2056 option annex.americas.give_to_quebec"
		name = annex.americas.give_to_quebec
		trigger = {
			has_event_target = quebec 
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				is_ally_with = event_target:quebec
			}
			modifier = {
				factor = 0.1
				event_target:quebec = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:quebec }
		943 = { transfer_state_and_notify = yes } #Rainy River
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2056 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		943 = { add_claim_of_ROOT = yes } #Rainy River
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2056 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Victoria Island
	id = annex.2057
	title = annexation_740
	desc = annexation_740_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		if = {
			limit = {
				OR = {
					AND = {
						473 = { is_claim_or_core_of_ROOT = yes } #British Columbia
						original_tag = BCL
					}
					1006 = { is_claim_or_core_of_ROOT = yes } #Novo-Arkhangelsk
				}
			}
			save_event_target_as = can_annex
		}
		hidden_effect = {
			if = {
				limit = {
					473 = { #British Columbia
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
						}
					}
				}
				var:473.owner = { save_event_target_as = borealia }
			}
			if = {
				limit = {
					1006 = { #Novo-Arkhangelsk
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							NOT = { tag = event_target:borealia }
							ROOT_can_grant_land = yes
						}
					}
				}
				var:1006.owner = { save_event_target_as = alaska }
			}
		}
    }
	option = { #Give to Borealia
		log = "[GetLogInfo]: event annex.2057 option annex.americas.give_to_northwest_territories"
		name = annex.americas.give_to_northwest_territories
		trigger = {
			has_event_target = borealia
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}

		set_temp_variable = { tag_to_release = event_target:borealia }
		740 = { transfer_state_and_notify = yes } #Victoria Island
		
	}

	option = { #Give it to Alaska
		log = "[GetLogInfo]: event annex.2057 option annex.americas.give_to_alaska"
		name = annex.americas.give_to_alaska
		trigger = {
			has_event_target = alaska
	    }
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				is_ally_with = event_target:alaska
			}
			modifier = {
				factor = 0.1
				event_target:alaska = { is_subject = yes }
			}
		}

		set_temp_variable = { tag_to_release = event_target:alaska }
		740 = { transfer_state_and_notify = yes } #Victoria Island
		
	}

	option = { #Integrate
		log = "[GetLogInfo]: event annex.2057 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		740 = { add_claim_of_ROOT = yes } #Victoria Island
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2057 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Alaska
	id = annex.2058
	title = annexation_463
	desc = annexation_463_desc
	picture = GFX_report_event_Generic_Art_Gallery

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			every_state = {
				if = {
					limit = {
						state = 740 # Victoria Island
					} 
					remove_core_of = ASK
					retain_state = yes
				}
				remove_claim_by = ASK
			}
		}
	}

	option = { #Liberate Most of Alaska
		log = "[GetLogInfo]: event annex.2058 option annex.2058.a"
		name = annex.2058.a
		trigger = {
			is_subject = no
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
		}
		set_temp_variable = { tag_to_release = ASK }
		every_state = {
			limit = { is_core_of = ASK }
			remove_claim_and_add_core_of_target = yes
		}
		release_targeted_tag = yes
		if = {
			limit = { 
				ROOT = { 
					AND = {
						has_monarchist_government = yes 
						NOT = { tag = RUS }
					}

				}
			}
			ASK = {
				set_cosmetic_tag = ASK_Occupied_Monarchy
			}
		}

		if = {
			limit = { ROOT = { has_republican_government = yes } }
			ASK = {
				set_cosmetic_tag = ASK_Occupied_Republic
			}
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2058 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }	
	}

	after = {
		recheck_annexations = yes
	}
}	

country_event = { #Fate of Monterrey
	id = annex.2060
	title = annexation_480
	desc = annexation_480_desc
	picture = GFX_report_PB_Peace_Deals_Europe

	is_triggered_only = yes
	
	immediate = {
		if = {
			limit = { MEX = { ROOT_can_grant_land = yes } }
			MEX = { save_event_target_as = mexico }
		}
		if = {
			limit = { MEX = { ROOT_can_grant_land = yes } }
			RGD = { save_event_target_as = riogrande }
		}		
	}	
	
	option = { #give to Mexico
		log = "[GetLogInfo]: event annex.2060 option annex.americas.give_to_mexico"
		name = annex.americas.give_to_mexico
		trigger = {
			has_event_target = mexico
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:mexico = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:mexico }
		every_state = {
			limit = { is_monterrey = yes }
			transfer_if_owned = yes
		}
	}
	option = { #give to Rio
		log = "[GetLogInfo]: event annex.2060 option annex.americas.give_to_rio"
		name = annex.americas.give_to_rio
		trigger = {
			has_event_target = riogrande
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0.1
				event_target:riogrande = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:riogrande }
		every_state = {
			limit = { is_monterrey = yes }
			transfer_if_owned = yes
		}
	}	
	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2060 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }		
		every_state = {
			limit = { is_monterrey = yes }
			transfer_if_owned = yes
		}
	}

	option = { #integration
		log = "[GetLogInfo]: event annex.2060 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		every_owned_state = {
			limit = { is_monterrey = yes}
			add_claim_of_ROOT = yes
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2060 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
    }

	after = {
		recheck_annexations = yes
	}
}

country_event = { #Fate of Greenland
	id = annex.2061
	title = annexation_101
	desc = annexation_101_desc
	picture = GFX_report_PB_Peace_Deals_Europe

	is_triggered_only = yes
	
	immediate = {
		hidden_effect = {
			if = {
				limit = {
					OR = {
						is_nordic_tag = yes
						original_tag = ENG
						original_tag = AME
						original_tag = SCA
						original_tag = QUE
					}
				}
				save_event_target_as = can_annex
			}
			
			#Greater Montreal
			if = {
				limit = {
					1462 = { 
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
							tag = QUE
							tag = AME
							tag = SCA
						}
					}
				}
				var:1462.owner = { save_event_target_as = montreal }
			}
			
			#New England / Maine
			if = {
				limit = {
					357 = { 
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
							tag = QUE
							tag = AME
							tag = SCA
						}
					}
				}
				var:357.owner = { save_event_target_as = new_england }
			}
			
			#Denmark
			if = {
				limit = {
					37 = { 
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
							tag = DEN
						}
					}
				}
				var:37.owner = { save_event_target_as = denmark }
			}
			
			#Norway
			if = {
				limit = {
					110 = { 
						annexations_state_owner_is_valid_grant_target = yes
						owner = {
							ROOT_can_grant_land = yes
							tag = NOR
							tag = SWE
						}
					}
				}
				var:110.owner = { save_event_target_as = norway }
			}
		}
	}
	
	option = { #Give to Greater Montreal
		log = "[GetLogInfo]: event annex.2061 option annex.2061.a"
		name = annex.2061.a
		trigger = {
			has_event_target = montreal
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:montreal }
				OR = {
					is_ally_with = event_target:new_england
					is_ally_with = event_target:denmark
					is_ally_with = event_target:norway
				}
			}
			modifier = {
				factor = 0.1
				event_target:montreal = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:montreal }
		every_owned_state = {
			limit = { is_greenland = yes }
			transfer_state_and_notify = yes
		}
	}
	
	option = { #Give to New England
		log = "[GetLogInfo]: event annex.2061 option annex.2061.b"
		name = annex.2061.b
		trigger = {
			has_event_target = new_england
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:new_england }
				OR = {
					is_ally_with = event_target:montreal
					is_ally_with = event_target:denmark
					is_ally_with = event_target:norway
				}
			}
			modifier = {
				factor = 0.1
				event_target:new_england = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:new_england }
		every_owned_state = {
			limit = { is_greenland = yes }
			transfer_state_and_notify = yes
		}
	}
	
	option = { #Give to Denmark
		log = "[GetLogInfo]: event annex.2061 option annex.2061.c"
		name = annex.2061.c
		trigger = {
			has_event_target = denmark
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:denmark }
				OR = {
					is_ally_with = event_target:montreal
					is_ally_with = event_target:new_england
					is_ally_with = event_target:norway
				}
			}
			modifier = {
				factor = 0.1
				event_target:denmark = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:denmark }
		every_owned_state = {
			limit = { is_greenland = yes }
			transfer_state_and_notify = yes
		}
	}
	
	option = { #Give to Norway
		log = "[GetLogInfo]: event annex.2061 option annex.2061.e"
		name = annex.2061.e
		trigger = {
			has_event_target = norway
		}
		ai_chance = {
			base = 60
			modifier = {
				factor = 0
				annexations_can_annex = yes
			}
			modifier = {
				factor = 0
				NOT = { is_ally_with = event_target:norway }
				OR = {
					is_ally_with = event_target:montreal
					is_ally_with = event_target:new_england
					is_ally_with = event_target:denmark
				}
			}
			modifier = {
				factor = 0.1
				event_target:norway = { is_subject = yes }
			}
		}
		set_temp_variable = { tag_to_release = event_target:norway }
		every_owned_state = {
			limit = { is_greenland = yes }
			transfer_state_and_notify = yes
		}
	}
	
	option = { #Give to overlord
		log = "[GetLogInfo]: event annex.2061 option annex.give_to_overlord"
		name = annex.give_to_overlord
		trigger = {
			annexations_should_give_lands_to_overlord = yes
		}
		set_temp_variable = { tag_to_release = overlord }		
		every_owned_state = {
			limit = { is_greenland = yes }
			transfer_state_and_notify = yes
		}
	}

	option = { #integration
		log = "[GetLogInfo]: event annex.2061 option annex.integration"
		name = annex.integration
		trigger = {
			annexations_can_annex = yes
		}
		ai_chance = { base = 100 }
		
		every_owned_state = {
			limit = { is_greenland = yes}
			add_claim_of_ROOT = yes
		}
	}

	option = { #Cancel
		log = "[GetLogInfo]: event annex.2061 option annex.cancel"
		name = annex.cancel
		ai_chance = { base = 0 }
		
    }

	after = {
		recheck_annexations = yes
	}
}