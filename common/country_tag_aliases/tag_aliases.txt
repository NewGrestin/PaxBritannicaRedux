SWC = {
	original_tag = SWI
	has_country_flag = SWI_is_catholic_SWI
}

DSA = {
	original_tag = DAS
	has_country_flag = DAS_is_ZIT
}

# operative operation target
# only set for majors who has agency
# set on on_weekly
MOT = {
	variable = generic_operation_target
}

AHP = {
	targets = { SER GRE MOL }
	is_faction_leader = yes
	is_in_faction_with = SER
}
LON = {
	global_event_target = league_of_nations
	fallback = DGG
}

HNG = { 
	targets = { HWA HUN }
	has_country_flag = HNG_true_hungary
}

HRV = { 
	targets = { SLA CRO }
	is_subject_of = HNG
}
# Imperial Powers Leader
IMP = { 
	global_event_target = IMP_leader
	fallback = ENG
}
NMX = { 
	global_event_target = NMX_leader
	fallback = NMX
}
SMX = { 
	global_event_target = SMX_leader
	fallback = YUC
}
GBC = { #Great Balkan war Coalitioners -Bulgarian inclusion unsure
	targets = { SER GRE MOL BUL }
	is_faction_leader = yes
	is_in_faction_with = SER
}
GBE = { #Great Balkan war Ententeers
	targets = { HUN BUL ALB }
	is_faction_leader = yes
	is_in_faction_with = HUN
}
# Belgium
BLG = {
	targets = { WAL FLA }
	has_country_flag = BLG_formed
	fallback = BEL
}
#Central America
CEN = {
	targets = { NIC }
	has_country_flag = CEN_formed
	fallback = CEN
}