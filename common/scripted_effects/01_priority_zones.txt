### thanks KR, those are ambition areas ;P
### Priority Zones
###
### Each country has a list of states/regions which it will take in a peace conference even if it
### doesn't have claims/cores. Other countries in their faction will let them take these areas unless there's a conflict.
### If they're a minor, or they're in a faction but not the leader, they won't expand outside of this ambition area unless
### there's no-one involved in the peace conference who can take the land.
###
### If a country is NOT in this list, then it only takes cores/claims.
###

create_priority_zones = {
	# FRA - France
	if = {
		limit = {
			OR = {
				is_core_of = FRA
				is_claimed_by = FRA
				is_rhineland = yes
				is_netherlands = yes
				state = 6
			}
		}
		set_variable = { priority_zone_@FRA = 1 }
	}
	# RUS - Russia
	if = {
		limit = {
			OR = {
				is_core_of = RUS
				is_claimed_by = RUS
				is_ukraine = yes
				is_poland = yes 
				is_baltic = yes
			}
		}
		set_variable = { priority_zone_@RUS = 1 }
	}
	# BLR - Belarus
	if = {
		limit = {
			OR = {
				is_core_of = BLR
				is_claimed_by = BLR
			}
		}
		set_variable = { priority_zone_@BLR = 1 }
	}
	# UKR - Ukraine
	if = {
		limit = {
			OR = {
				is_core_of = UKR
				is_claimed_by = UKR
			}
		}
		set_variable = { priority_zone_@BLR = 1 }
	}
	# GER - Germany
	if = {
		limit = {
			OR = {
				is_core_of = GER
				is_claimed_by = GER
				region = 22
				is_prussia = yes
			}
		}
		set_variable = { priority_zone_@GER = 1 }
	}
	# SPR - Spain
	if = {
		limit = {
			OR = {
				is_core_of = SPR
				is_claimed_by = SPR
				is_morocco = yes
				is_portugal = yes
			}
		}
		set_variable = { priority_zone_@SPR = 1 }
	}
	# HUN - Hungary
	if = {
		limit = {
			OR = {
				is_core_of = HUN
				is_claimed_by = HUN
				is_croatia = yes
			}
		}
		set_variable = { priority_zone_@HUN = 1 }
	}
	# CRO - Croatia
	if = {
		limit = {
			OR = {
				is_core_of = CRO
				is_claimed_by = CRO
				is_bosnia = yes
				is_dalmatia = yes
			}
		}
		set_variable = { priority_zone_@CRO = 1 }
	}
	# BAL - Baltic
	if = {
		limit = {
			OR = {
				is_core_of = BAL
				is_claimed_by = BAL
				region = 37
			}
		}
		set_variable = { priority_zone_@BAL = 1 }
	}
	# UKR - Ukraine
	if = {
		limit = {
			OR = {
				is_core_of = UKR
				is_claimed_by = UKR
				region = 130
				state = 225
				state = 220
				state = 240
				state = 260
				state = 324
				state = 1390
				state = 1387
				state = 218
			}
		}
		set_variable = { priority_zone_@BAL = 1 }
	}
	# RAJ - Raj
	if = {
		limit = {
			OR = {
				is_core_of = BAL
				is_claimed_by = BAL
				is_india = yes
				is_burma = yes
				region = 379
			}
		}
		set_variable = { priority_zone_@RAJ = 1 }
	}
	# CHI - China
	if = {
		limit = {
			OR = {
				is_core_of = CHI
				is_claimed_by = CHI
				is_china = yes
			}
		}
		set_variable = { priority_zone_@CHI = 1 }
	}
	# MAN - China
	if = {
		limit = {
			OR = {
				is_core_of = MAN
				is_claimed_by = MAN
				is_china = yes
			}
		}
		set_variable = { priority_zone_@MAN = 1 }
	}
	# ROC - China
	if = {
		limit = {
			OR = {
				is_core_of = ROC
				is_claimed_by = ROC
				is_china = yes
			}
		}
		set_variable = { priority_zone_@ROC = 1 }
	}
}
