update_custom_state_pop_growth = {
	every_state = { 
		limit = { 
			OR = {
				medical_center > 0
				has_dynamic_modifier = {
					modifier = PB_state_focus_modifiers_8 #population investments
				}
			}
		}
		set_temp_variable = { modified_pop_growth = state_population_k } 
		set_temp_variable = { custom_pop_growth = 1 }
		add_to_temp_variable = { custom_pop_growth = modifier@state_monthly_population } 
		multiply_temp_variable = { custom_pop_growth = 0.00125 } 
		multiply_temp_variable = { modified_pop_growth = custom_pop_growth }
		multiply_temp_variable = { modified_pop_growth = 1 }
		add_manpower = var:modified_pop_growth
	}
}

save_current_factory_count = {
	hidden_effect = {
		if = {
			limit = { industrial_complex > 0 }
			set_variable = { previous_industrial_complex_count = building_level@industrial_complex }
		}
		if = {
			limit = { arms_factory > 0 }
			set_variable = { previous_arms_factory_count = building_level@arms_factory }
		}
		if = {
			limit = { dockyard > 0 }
			set_variable = { previous_dockyard_count = building_level@dockyard }
		}
		if = {
			limit = { synthetic_refinery > 0 }
			set_variable = { previous_synthetic_refinery_count = building_level@synthetic_refinery }
		}
		if = {
			limit = { fuel_silo > 0 }
			set_variable = { previous_fuel_silo_count = building_level@fuel_silo }
		}
		if = {
			limit = { rocket_site > 0 }
			set_variable = { previous_rocket_site_count = building_level@rocket_site }
		}
		if = {
			limit = { nuclear_reactor > 0 }
			set_variable = { previous_nuclear_reactor_count = building_level@nuclear_reactor }
		}
		if = {
			limit = { nuclear_reactor_heavy_water > 0 }
			set_variable = { previous_nuclear_reactor_heavy_water_count = building_level@nuclear_reactor_heavy_water }
		}
		if = {
			limit = { commercial_nuclear_reactor  > 0 }
			set_variable = { previous_commercial_nuclear_reactor_count = building_level@commercial_nuclear_reactor }
		}
		if = {
			limit = { agroponics_complex > 0 }
			set_variable = { previous_agroponics_complex_count = building_level@agroponics_complex }
		}
		if = {
			limit = { mineral_fabricator > 0 }
			set_variable = { previous_mineral_fabricator_count = building_level@mineral_fabricator }
		}
		if = {
			limit = { medical_center > 0 }
			set_variable = { previous_medical_center_count = building_level@medical_center }
		}
		if = {
			limit = { research_center > 0 }
			set_variable = { previous_research_center_count = building_level@research_center }
		}
	}
}

restore_previous_factory_count = {
	hidden_effect = {
		if = {
			limit = { has_variable = previous_industrial_complex_count }
			subtract_from_variable = { previous_industrial_complex_count = building_level@industrial_complex }
			if = {
				limit = { check_variable = { previous_industrial_complex_count > 0 } }
				add_building_construction = { type = industrial_complex level = previous_industrial_complex_count instant_build = yes }
			}
			clear_variable = previous_industrial_complex_count
		}
		if = {
			limit = { has_variable = previous_arms_factory_count }
			subtract_from_variable = { previous_arms_factory_count = building_level@arms_factory }
			if = {
				limit = { check_variable = { previous_arms_factory_count > 0 } }
				add_building_construction = { type = arms_factory level = previous_arms_factory_count instant_build = yes }
			}
			clear_variable = previous_arms_factory_count
		}
		if = {
			limit = { has_variable = previous_dockyard_count }
			subtract_from_variable = { previous_dockyard_count = building_level@dockyard }
			if = {
				limit = { check_variable = { previous_dockyard_count > 0 } }
				add_building_construction = { type = dockyard level = previous_dockyard_count instant_build = yes }
			}
			clear_variable = previous_dockyard_count
		}
		if = {
			limit = { has_variable = previous_synthetic_refinery_count }
			subtract_from_variable = { previous_synthetic_refinery_count = building_level@synthetic_refinery }
			if = {
				limit = { check_variable = { previous_synthetic_refinery_count > 0 } }
				add_building_construction = { type = synthetic_refinery level = previous_synthetic_refinery_count instant_build = yes }
			}
			clear_variable = previous_synthetic_refinery_count
		}
		if = {
			limit = { has_variable = previous_fuel_silo_count }
			subtract_from_variable = { previous_fuel_silo_count = building_level@fuel_silo }
			if = {
				limit = { check_variable = { previous_fuel_silo_count > 0 } }
				add_building_construction = { type = fuel_silo level = previous_fuel_silo_count instant_build = yes }
			}
			clear_variable = previous_fuel_silo_count
		}
		if = {
			limit = { has_variable = previous_rocket_site_count }
			subtract_from_variable = { previous_rocket_site_count = building_level@rocket_site }
			if = {
				limit = { check_variable = { previous_rocket_site_count > 0 } }
				add_building_construction = { type = rocket_site level = previous_rocket_site_count instant_build = yes }
			}
			clear_variable = previous_rocket_site_count
		}
		if = {
			limit = { has_variable = previous_nuclear_reactor_count }
			subtract_from_variable = { previous_nuclear_reactor_count = building_level@nuclear_reactor }
			if = {
				limit = { check_variable = { previous_nuclear_reactor_count > 0 } }
				add_building_construction = { type = nuclear_reactor level = previous_nuclear_reactor_count instant_build = yes }
			}
			clear_variable = previous_nuclear_reactor_count
		}
		if = {
			limit = { has_variable = previous_nuclear_reactor_heavy_water_count }
			subtract_from_variable = { previous_nuclear_reactor_heavy_water_count = building_level@nuclear_reactor_heavy_water }
			if = {
				limit = { check_variable = { previous_nuclear_reactor_heavy_water_count > 0 } }
				add_building_construction = { type = nuclear_reactor_heavy_water level = previous_nuclear_reactor_heavy_water_count instant_build = yes }
			}
			clear_variable = previous_nuclear_reactor_heavy_water_count
		}
		if = {
			limit = { has_variable = previous_commercial_nuclear_reactor_count }
			subtract_from_variable = { previous_commercial_nuclear_reactor_count = building_level@commercial_nuclear_reactor }
			if = {
				limit = { check_variable = { previous_commercial_nuclear_reactor_count > 0 } }
				add_building_construction = { type = commercial_nuclear_reactor level = previous_commercial_nuclear_reactor_count instant_build = yes }
			}
			clear_variable = previous_commercial_nuclear_reactor_count
		}
		if = {
			limit = { has_variable = previous_agroponics_complex_count }
			subtract_from_variable = { previous_agroponics_complex_count = building_level@agroponics_complex }
			if = {
				limit = { check_variable = { previous_agroponics_complex_count > 0 } }
				add_building_construction = { type = agroponics_complex level = previous_agroponics_complex_count instant_build = yes }
			}
			clear_variable = previous_agroponics_complex_count
		}
		if = {
			limit = { has_variable = previous_mineral_fabricator_count }
			subtract_from_variable = { previous_mineral_fabricator_count = building_level@mineral_fabricator }
			if = {
				limit = { check_variable = { previous_mineral_fabricator_count > 0 } }
				add_building_construction = { type = mineral_fabricator level = previous_mineral_fabricator_count instant_build = yes }
			}
			clear_variable = previous_mineral_fabricator_count
		}
		if = {
			limit = { has_variable = previous_medical_center_count }
			subtract_from_variable = { previous_medical_center_count = building_level@medical_center }
			if = {
				limit = { check_variable = { previous_medical_center_count > 0 } }
				add_building_construction = { type = medical_center level = previous_medical_center_count instant_build = yes }
			}
			clear_variable = previous_medical_center_count
		}
		if = {
			limit = { has_variable = previous_research_center_count }
			subtract_from_variable = { previous_research_center_count = building_level@research_center }
			if = {
				limit = { check_variable = { previous_research_center_count > 0 } }
				add_building_construction = { type = research_center level = previous_research_center_count instant_build = yes }
			}
			clear_variable = previous_research_center_count
		}
	}
}

embargo_ROOT = {
	if = {
		limit = {
			NOT = { has_country_flag = embargoed_@ROOT }
			NOT = { tag = ROOT } #sanity check
		}
		set_country_flag = embargoed_@ROOT
		ROOT = { set_country_flag = embargoed_@PREV }
		if = {
			limit = { has_dlc = "By Blood Alone" }
			if = {
				limit = { NOT = { is_embargoing = ROOT } }
				diplomatic_relation = {
					country = ROOT
					relation = embargo
				}
			}
			if = {
				limit = { NOT = { is_embargoed_by = ROOT } }
				ROOT = {
					diplomatic_relation = {
						country = PREV
						relation = embargo
					}
				}
			}
		}
		else = {
			add_opinion_modifier = {
				modifier = embargo
				target = ROOT
			}
			reverse_add_opinion_modifier = {
				modifier = embargo
				target = ROOT
			}
		}
	}
}

clear_embargo_ROOT = {
	if = {
		limit = { has_country_flag = embargoed_@ROOT }
		clr_country_flag = embargoed_@ROOT
		effect_tooltip = {
			if = {
				limit = { has_dlc = "By Blood Alone" }
				diplomatic_relation = {
					country = ROOT
					relation = embargo
					active = no
				}
			}
			else = {
				remove_opinion_modifier = {
					modifier = embargo
					target = ROOT
				}
			}
		}
		hidden_effect = {
			diplomatic_relation = {
				country = ROOT
				relation = embargo
				active = no
			}
			remove_opinion_modifier = {
				modifier = embargo
				target = ROOT
			}
		}
		ROOT = {
			clr_country_flag = embargoed_@PREV
			effect_tooltip = {
				if = {
					limit = { has_dlc = "By Blood Alone" }
					diplomatic_relation = {
						country = PREV
						relation = embargo
						active = no
					}
				}
				else = {
					remove_opinion_modifier = {
						modifier = embargo
						target = PREV
					}
				}
			}
			hidden_effect = {
				diplomatic_relation = {
					country = PREV
					relation = embargo
					active = no
				}
				remove_opinion_modifier = {
					modifier = embargo
					target = PREV
				}
			}
		}
	}
}

embargo_PREV = {
	if = {
		limit = {
			NOT = { has_country_flag = embargoed_@PREV }
			NOT = { tag = PREV } #sanity check
		}
		set_country_flag = embargoed_@PREV
		PREV = { set_country_flag = embargoed_@PREV }
		if = {
			limit = { has_dlc = "By Blood Alone" }
			if = {
				limit = { NOT = { is_embargoing = PREV } }
				diplomatic_relation = {
					country = PREV
					relation = embargo
				}
			}
			if = {
				limit = { NOT = { is_embargoed_by = PREV } }
				PREV = {
					diplomatic_relation = {
						country = PREV
						relation = embargo
					}
				}
			}
		}
		else = {
			add_opinion_modifier = {
				modifier = embargo
				target = PREV
			}
			reverse_add_opinion_modifier = {
				modifier = embargo
				target = PREV
			}
		}
	}
}

clear_embargo_PREV = {
	if = {
		limit = { has_country_flag = embargoed_@PREV }
		clr_country_flag = embargoed_@PREV
		effect_tooltip = {
			if = {
				limit = { has_dlc = "By Blood Alone" }
				diplomatic_relation = {
					country = PREV
					relation = embargo
					active = no
				}
			}
			else = {
				remove_opinion_modifier = {
					modifier = embargo
					target = PREV
				}
			}
		}
		hidden_effect = {
			diplomatic_relation = {
				country = PREV
				relation = embargo
				active = no
			}
			remove_opinion_modifier = {
				modifier = embargo
				target = PREV
			}
		}
		PREV = {
			clr_country_flag = embargoed_@PREV
			effect_tooltip = {
				if = {
					limit = { has_dlc = "By Blood Alone" }
					diplomatic_relation = {
						country = PREV
						relation = embargo
						active = no
					}
				}
				else = {
					remove_opinion_modifier = {
						modifier = embargo
						target = PREV
					}
				}
			}
			hidden_effect = {
				diplomatic_relation = {
					country = PREV
					relation = embargo
					active = no
				}
				remove_opinion_modifier = {
					modifier = embargo
					target = PREV
				}
			}
		}
	}
}

embargo_FROM = {
	if = {
		limit = {
			NOT = { has_country_flag = embargoed_@FROM }
			NOT = { tag = FROM } #sanity check
		}
		set_country_flag = embargoed_@FROM
		FROM = { set_country_flag = embargoed_@PREV }
		if = {
			limit = { has_dlc = "By Blood Alone" }
			if = {
				limit = { NOT = { is_embargoing = FROM } }
				diplomatic_relation = {
					country = FROM
					relation = embargo
				}
			}
			if = {
				limit = { NOT = { is_embargoed_by = FROM } }
				FROM = {
					diplomatic_relation = {
						country = PREV
						relation = embargo
					}
				}
			}
		}
		else = {
			add_opinion_modifier = {
				modifier = embargo
				target = FROM
			}
			reverse_add_opinion_modifier = {
				modifier = embargo
				target = FROM
			}
		}
	}
}

clear_embargo_FROM = {
	if = {
		limit = { has_country_flag = embargoed_@FROM }
		clr_country_flag = embargoed_@FROM
		effect_tooltip = {
			if = {
				limit = { has_dlc = "By Blood Alone" }
				diplomatic_relation = {
					country = FROM
					relation = embargo
					active = no
				}
			}
			else = {
				remove_opinion_modifier = {
					modifier = embargo
					target = FROM
				}
			}
		}
		hidden_effect = {
			diplomatic_relation = {
				country = FROM
				relation = embargo
				active = no
			}
			remove_opinion_modifier = {
				modifier = embargo
				target = FROM
			}
		}
		FROM = {
			clr_country_flag = embargoed_@PREV
			effect_tooltip = {
				if = {
					limit = { has_dlc = "By Blood Alone" }
					diplomatic_relation = {
						country = PREV
						relation = embargo
						active = no
					}
				}
				else = {
					remove_opinion_modifier = {
						modifier = embargo
						target = PREV
					}
				}
			}
			hidden_effect = {
				diplomatic_relation = {
					country = PREV
					relation = embargo
					active = no
				}
				remove_opinion_modifier = {
					modifier = embargo
					target = PREV
				}
			}
		}
	}
}

open_opening_screen = {
	if = {
		limit = { is_ai = no }
		set_variable = { PB_opening_screen_var = 1 }
		set_variable_to_random = PB_opening_screen_reload
	}
}

transfer_state_during_war = {
	if = {
		limit = { is_fully_controlled_by = owner }
		transfer_state_to = var:target
	}
	else = {
		set_state_owner_to = var:target
		set_state_province_controller = {
			controller = owner
			limit = { NOT = { has_war_with = PREV.owner } }
		}
	}
}

