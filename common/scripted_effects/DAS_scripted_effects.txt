DAS_check_zitronella_influence_dynmod_vars = {
	if = {
		limit = {
			DAS = {
				check_variable = { zitronella_shares < 26 }
				check_variable = { zitronella_shares > 0 }
			} 
		}
		set_variable = { DAS.DAS_consumer_goods_factor_zitronella = 0.1 }
		set_variable = { DAS.DAS_production_factory_efficiency_gain_factor_zitronella = 0.1 }
		set_variable = { DAS.DAS_research_speed_factor_zitronella = 0 }
		set_variable = { DAS.DAS_party_popularity_stability_factor_zitronella = 0 }
		set_variable = { DAS.DAS_an_drift_zitronella = 0 }
		set_variable = { DAS.DAS_drift_defence_factor_zitronella = 0 }
	}
	else_if = {
		limit = {
			DAS = {
				check_variable = { zitronella_shares < 51 }
				check_variable = { zitronella_shares > 25 }
			}
		}
		set_variable = { DAS.DAS_consumer_goods_factor_zitronella = 0.2 }
		set_variable = { DAS.DAS_production_factory_efficiency_gain_factor_zitronella = 0.2 }
		set_variable = { DAS.DAS_research_speed_factor_zitronella = 0.1 }
		set_variable = { DAS.DAS_party_popularity_stability_factor_zitronella = 0 }
		set_variable = { DAS.DAS_an_drift_zitronella = 0 }
		set_variable = { DAS.DAS_drift_defence_factor_zitronella = 0 }
	}	
	else_if = {
		limit = {
			DAS = {
				check_variable = { zitronella_shares < 81 }
				check_variable = { zitronella_shares > 50 }
			}
		}
		set_variable = { DAS.DAS_consumer_goods_factor_zitronella = 0.35 }
		set_variable = { DAS.DAS_production_factory_efficiency_gain_factor_zitronella = 0.35 }
		set_variable = { DAS.DAS_research_speed_factor_zitronella = 0.2 }
		set_variable = { DAS.DAS_party_popularity_stability_factor_zitronella = -0.3 }
		set_variable = { DAS.DAS_an_drift_zitronella = 0.04 }
		set_variable = { DAS.DAS_drift_defence_factor_zitronella = -0.2 }
	}
	else_if = {
		limit = {
			DAS = {
				check_variable = { zitronella_shares < 101 }
				check_variable = { zitronella_shares > 80 }
			}
		}
		set_variable = { DAS.DAS_consumer_goods_factor_zitronella = 0.5 }
		set_variable = { DAS.DAS_production_factory_efficiency_gain_factor_zitronella = 0.4 }
		set_variable = { DAS.DAS_research_speed_factor_zitronella = 0.25 }
		set_variable = { DAS.DAS_party_popularity_stability_factor_zitronella = -0.4 }
		set_variable = { DAS.DAS_an_drift_zitronella = 0.06 }
		set_variable = { DAS.DAS_drift_defence_factor_zitronella = -0.3 }
	}
}