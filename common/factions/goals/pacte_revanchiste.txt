## MANIFESTOS ##
faction_manifest_new_world_order = {
	name = faction_manifest_new_world_order_name
	description = faction_manifest_new_world_order_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_states
		completed_amount_collection = collection:faction_owned_states
		scale = {
		}

		progress_sections = {
			manifest_fulfilled = {
				min = 0.75

				modifier = {

				}
			}
		}
	}
}

## GOALS ##
faction_goal_renounce_treaty_of_tours = {
	name = renounce_treaty_of_tours_goal_name
	description = renounce_treaty_of_tours_goal_desc
	category = medium_term
	group = FOCUS_FILTER_WAR_SUPPORT
	visible = {
		FROM = { original_tag = FRA }
	}
	completed = {
		OR = {
			AND = {
				original_tag = FRA
				has_army_manpower = {
					size > 800000
				}
				controls_state = 14 #Brittany
				controls_state = 34 #Hainaut
				controls_state = 788 #Walloon Luxemburg
				controls_state = 855 #Dunkerque
			}
			any_allied_country = {
				original_tag = FRA
				has_army_manpower = {
					size > 800000
				}
				controls_state = 14 #Brittany
				controls_state = 34 #Hainaut
				controls_state = 788 #Walloon Luxemburg
				controls_state = 855 #Dunkerque
			}
		}
		17 = { is_demilitarized_zone = no } #Meurthe
		685 = { is_demilitarized_zone = no } #Champagne-Ardenne
		972 = { is_demilitarized_zone = no } #Neuchatel
		1206 = { is_demilitarized_zone = no } #Moselle
		1207 = { is_demilitarized_zone = no } #Verdun
	}
	complete_effect = {
		add_faction_power_projection = 100
		every_faction_member = {
			limit = {
				original_tag = FRA
			}
			add_faction_initiative = 1
		}
		custom_effect_tooltip = generic_skip_one_line_tt
		every_faction_member = { 
			tooltip = INFLUENCE_ABOVE_30_PERCENT
			visible_when_empty = yes
			limit = {
				faction_influence_ratio > 0.3
			}
			add_war_support = 0.05
			add_political_power = 50
		}
	}
	ai_will_do = {
		base = 10
	}
}