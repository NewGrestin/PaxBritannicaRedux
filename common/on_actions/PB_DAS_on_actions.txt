on_actions = {
	on_startup = {
		effect = {
			add_to_array = { DAS.phillipine_states = 327.id }
			add_to_array = { DAS.phillipine_states = 623.id }
			add_to_array = { DAS.phillipine_states = 624.id }
			add_to_array = { DAS.phillipine_states = 625.id }
			add_to_array = { DAS.phillipine_states = 626.id }
			add_to_array = { DAS.phillipine_states = 627.id }
			add_to_array = { DAS.phillipine_states = 1352.id }
			add_to_array = { DAS.phillipine_states = 1414.id }
			add_to_array = { DAS.phillipine_states = 1575.id }
			add_to_array = { DAS.phillipine_states = 1576.id }
			add_to_array = { DAS.phillipine_states = 1577.id }
			add_to_array = { DAS.phillipine_states = 1578.id }
			add_to_array = { DAS.phillipine_states = 1579.id }
			add_to_array = { DAS.fernost_states = 737.id }
			add_to_array = { DAS.fernost_states = 634.id }
			add_to_array = { DAS.fernost_states = 725.id }
			add_to_array = { DAS.fernost_states = 684.id }
			add_to_array = { DAS.fernost_states = 647.id }
			add_to_array = { DAS.fernost_states = 646.id }
			add_to_array = { DAS.fernost_states = 638.id }
			add_to_array = { DAS.fernost_states = 633.id }
			add_to_array = { DAS.fernost_states = 1148.id }
			add_to_array = { DAS.fernost_states = 726.id }
			PHI = { 
				country_event = {
					id = philippines.intro.8
					days = 58
				}
			}
		}
	}
	on_monthly_DAS = {
		effect = {
			update_DAS_zitronella_influence = yes
		}
	}
	on_monthly_PHI = {
		effect = {
			update_PHI_kkk_strength = yes
		}
		if = {
			limit = {
				PHI = {
					stability < 0.1
				}
			}
			DAS = {
				DAS_bypass_peace = yes
			}
			DAS = { 
				annex_country = PHI
			}
		}
	}
	on_capitulation_immediate = {
		effect = {
			if = {
				limit = {
					original_tag = SWK
					AND = {
						BOR = {
							has_country_flag = BOR_backed_by_DAS
							NOT = {
								has_country_flag = BOR_Finished_War
							}
						}
						country_exists = BOR
						BOR = {
							controls_state = 333
						}
					}
				}
				BOR = {
					set_country_flag = BOR_Finished_War
				}
				DAS = {
					set_autonomy = {
						target = BOR
						autonomous_state = autonomy_puppet
					}
				}
				#news_event = {
				#	id = PBNews.31
				#	days = 1
				#}
			}
		}
	}
}