ideas = {
	alert_tokens = {
		### TEMPLATE
		#<alert token> = { allowed = { always = no } on_add = { <optional effect block for alerts that should have a click effect> } }
		debug_mode_enabled = { allowed = { always = no } on_add = { log = "Click effect for debug_mode_enabled executed" } }

		pax_tension_test_enabled = { allowed = { always = no } }
	}
}