scripted_gui = {
	PB_stronghold_network_blocker_gui = {
		window_name = "PB_no_dlc_stronghold_network_window"
		context_type = selected_state_context
		parent_window_token = selected_state_view
		#dirty = PB_convert_progress
	
		visible = {
			if = {
				limit = {
					NOT = {
						has_dlc = "Gotterdammerung"
					}
				 }
				always = yes
			}
			else = {
				always = no
			}
		}
	
	}
}