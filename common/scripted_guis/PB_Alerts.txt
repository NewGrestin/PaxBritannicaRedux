scripted_gui = {

	PB_Alerts_gui= {
		window_name = "PB_Alerts_Window"
		context_type = player_context
		parent_window_token = top_bar
		dirty = active_alerts^num
		visible = {
			has_variable = active_alerts^num
		}

		properties = {
			Alert_Button = {
				image = "GFX_alerticon_[?alert_v.GetTokenKey]"
			}
		}

		effects = {
			Alert_Button_right_click = {
				remove_from_array = { active_alerts = alert_v}
			}
			
		}	

		dynamic_lists = {
			alerts_gridbox = {
				array = active_alerts
				value = alert_v
				entry_container = "alerticon_entry_container"
			}
		}
	}	
}

