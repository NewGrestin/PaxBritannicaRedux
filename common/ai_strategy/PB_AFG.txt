AFG_ENG_Send_Volunteers_to_Afghan_Separatists = {
 	allowed = { 
 		original_tag = ENG
 	}

    enable = {
		has_global_flag = AFG_civil_war
		OR = {
			BLC = {
				exists = yes
			}
			HZT = {
				exists = yes
			}
		}
    }

 	abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
 		id = HZT
        value = 1000
    }
	ai_strategy = {
        type = send_volunteers_desire
 		id = BLC
        value = 1000
    }
}

AFG_GER_Send_Volunteers_to_Afghan_Separatists = {
 	allowed = { 
 		original_tag = GER
 	}

    enable = {
		has_global_flag = AFG_civil_war
		OR = {
			BLC = {
				exists = yes
			}
			HZT = {
				exists = yes
			}
		}
    }

 	abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
 		id = HZT
        value = 200
    }
	ai_strategy = {
        type = send_volunteers_desire
 		id = BLC
        value = 200
    }
}

AFG_RUS_Send_Volunteers_to_Afghan_Loyalists = {
 	allowed = {
 		original_tag = RUS
 	}

    enable = {
		has_global_flag = AFG_civil_war
		AFG = {
			exists = yes
		}
    }

 	abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
 		id = AFG
        value = 1000
    }
}

AFG_PER_Send_Volunteers_to_Afghan_Republicans = {
 	allowed = { 
 		original_tag = PER
 	}

    enable = {
		has_global_flag = AFG_civil_war
		PAK = {
			exists = yes
		}
    }

 	abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
 		id = PAK
        value = 1000
    }
}