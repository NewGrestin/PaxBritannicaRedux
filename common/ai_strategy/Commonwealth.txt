join_commonwealth_pact = {
	
	enable = {
	
		NOT = { tag = ENG }
		ENG = { is_in_faction = yes }
		
		OR = {		#These countries should always desire alliance
			tag = IRE
			tag = CAN
			tag = AME
			tag = EGY
			tag = JOR
			tag = AST
			tag = MAL
			tag = SAF
			has_country_flag = join_commonwealth
		}
		
		NOT = { has_country_flag = left_commonwealth }

		OR = {
			has_government = cn
			has_government = lb
			has_government = ss
			has_government = jc
			has_government = ds
			has_government = rc
			has_government = ni
		}
		
		OR = {
			ENG = { has_government = cn }
			ENG = { has_government = lb }
			ENG = { has_government = ss }
			ENG = { has_government = jc }
			ENG = { has_government = ds }
			ENG = { has_government = rc }
			ENG = { has_government = ni }
		}
		
	}
	
	abort = {
		OR = {
			has_war_with = ENG
			ENG = { is_in_faction = no }
			ENG = { exists = no }
		}
	}
	
	ai_strategy = {
		type = befriend
		id = "ENG"
		value = 50
	}
	ai_strategy = {
		type = alliance
		id = "ENG"
		value = 125
	}
}