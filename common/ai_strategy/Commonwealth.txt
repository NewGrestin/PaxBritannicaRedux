join_commonwealth_pact = {
	
	enable = {
	
		NOT = { tag = ENG }
		ENG = { is_in_faction = yes }
		
		OR = {		#These countries should always desire alliance
			tag = IRE
			tag = CAN
			tag = AME
			tag = EGY
			tag = JOR
			tag = AST
			tag = MAL
			tag = SAF
			has_country_flag = join_commonwealth
		}
		
		NOT = { has_country_flag = left_commonwealth }

		OR = {
			has_government = conservative
			has_government = social_liberal
			has_government = social_democrat
			has_government = socialist
			has_government = monarchist
			has_government = ultranationalist
			has_government = authoritarianism
		}
		
		OR = {
			ENG = { has_government = conservative }
			ENG = { has_government = social_liberal }
			ENG = { has_government = social_democrat }
			ENG = { has_government = socialist }
			ENG = { has_government = monarchist }
			ENG = { has_government = ultranationalist }
			ENG = { has_government = authoritarianism }
		}
		
	}
	
	abort = {
		OR = {
			has_war_with = ENG
			ENG = { is_in_faction = no }
			ENG = { exists = no }
		}
	}
	
	ai_strategy = {
		type = befriend
		id = "ENG"
		value = 50
	}
	ai_strategy = {
		type = alliance
		id = "ENG"
		value = 125
	}
}