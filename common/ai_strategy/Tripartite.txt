join_tripartite_pact = {
	
	enable = {
	
		NOT = { tag = AUS }
		AUS = { is_in_faction = yes }
		
		OR = {		#These countries should always desire alliance
			tag = TUR
			tag = SAR
			tag = PRS
			tag = FIN
			tag = MEX
			tag = JAP
			tag = TAC
			has_country_flag = join_tripartite
		}
		
		NOT = { has_country_flag = left_tripartite }

		OR = {
			has_government = conservative
			has_government = social_liberal
			has_government = social_democrat
			has_government = socialist
			has_government = monarchist
			has_government = ultranationalist
			has_government = authoritarianism
		}
		
		OR = {
			AUS = { has_government = conservative }
			AUS = { has_government = social_liberal }
			AUS = { has_government = social_democrat }
			AUS = { has_government = socialist }
			AUS = { has_government = monarchist }
			AUS = { has_government = ultranationalist }
			AUS = { has_government = authoritarianism }
		}
		
	}
	
	abort = {
		OR = {
			has_war_with = AUS
			AUS = { is_in_faction = no }
			AUS = { exists = no }
		}
	}
	
	ai_strategy = {
		type = befriend
		id = "AUS"
		value = 50
	}
	ai_strategy = {
		type = alliance
		id = "AUS"
		value = 500
	}
}