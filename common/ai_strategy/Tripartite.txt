join_tripartite_pact = {
	
	enable = {
	
		NOT = { tag = AUS }
		AUS = { is_in_faction = yes }
		
		OR = {		#These countries should always desire alliance
			tag = TUR
			tag = SAR
			tag = PRS
			tag = FIN
			tag = MEX
			tag = JAP
			tag = TAC
			has_country_flag = join_tripartite
		}
		
		NOT = { has_country_flag = left_tripartite }

		OR = {
			has_government = cn
			has_government = lb
			has_government = ss
			has_government = jc
			has_government = ds
			has_government = rc
			has_government = ni
		}
		
		OR = {
			AUS = { has_government = cn }
			AUS = { has_government = lb }
			AUS = { has_government = ss }
			AUS = { has_government = jc }
			AUS = { has_government = ds }
			AUS = { has_government = rc }
			AUS = { has_government = ni }
		}
		
	}
	
	abort = {
		OR = {
			has_war_with = AUS
			AUS = { is_in_faction = no }
			AUS = { exists = no }
		}
	}
	
	ai_strategy = {
		type = befriend
		id = "AUS"
		value = 50
	}
	ai_strategy = {
		type = alliance
		id = "AUS"
		value = 500
	}
}