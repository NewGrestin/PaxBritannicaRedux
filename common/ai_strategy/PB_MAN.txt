MAN_Send_Volunteers_to_Mongolia = {
	allowed = { 
		original_tag = MAN 
	}

    enable = {
        MON = {
			has_war_with = GAN
		}
    }

	abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
        id = MON
        value = 1000
    }
}

MAN_Send_Volunteers_to_Xinjiang = {
	allowed = { 
		original_tag = MAN 
	}

    enable = {
        SIK = {
			OR = {
                has_war_with = KUM
                has_war_with = ETR
				has_war_with = XSM
            }
		}
    }

	abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
        id = SIK
        value = 1000
    }
}