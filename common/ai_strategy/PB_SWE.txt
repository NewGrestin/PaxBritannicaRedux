FFI_push_against_FIN = {
    allowed = {
        original_tag = FFI
    }
    enable = {
        has_war_with = FIN
    }
    abort_when_not_enabled = yes

	abort = { 
		has_idea = FIN_ceasefire
	}

	ai_strategy = {
        type = conquer
        id = FIN        
        value = 100
    }

	ai_strategy = {
		type = front_control
		tag = FIN

		ratio = 0.9
		
		priority = 250

		ordertype = front
		execution_type = careful
		execute_order = yes
	}

	ai_strategy = {
        type = front_unit_request

        tag = FIN

        value = 35
    }

	ai_strategy = {
		type = garrison
		value = -10000
	}
}

SWE_Send_Volunteers_to_Finland = {
	allowed = { 
		original_tag = SWE 
	}

    enable = {
        FIN = {
			has_war_with = FFI
		}
    }

	abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
        id = FFI
        value = 1000
    }
}


FFI_ceasefire_freeze_front = {
    allowed = {
        original_tag = FFI
    }
    enable = {
        has_idea = FIN_ceasefire
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = front_control
        country_trigger = { tag = FIN }   
        ordertype = front
        execute_order = no                    
        manual_attack = no                    
        priority = 100    
    }
    ai_strategy = {
        type = diplo_action_acceptance
        id = SWE
        value = -100
        target = send_volunteers
    }
    ai_strategy = {
        type = garrison
        value = -100
    }
    ai_strategy = {
        type = front_control
        country_trigger = { tag = FIN }
        ordertype = invasion
        execute_order = no                    
        priority = 100
    }
}
FIN_duma_prevents_volounteers = { 
    allowed = {
        original_tag = FIN
    }
    enable = {
        NOT = { 
            RUS = { 
                has_completed_focus = RUS_commit_to_finland
            }
        }
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = diplo_action_acceptance
        id = RUS
        value = -1000
        target = send_volunteers
    }
}
FIN_ceasefire_freeze_front = {
    allowed = {
        original_tag = FIN
    }
    enable = {
        has_idea = FIN_ceasefire
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = front_control
        country_trigger = { tag = FFI }   
        ordertype = front
        execute_order = no                    
        manual_attack = no                    
        priority = 100    
    }
    ai_strategy = {
        type = diplo_action_acceptance
        id = RUS
        value = -100
        target = send_volunteers
    }
    ai_strategy = {
        type = garrison
        value = -100
    }
    ai_strategy = {
        type = front_control
        country_trigger = { tag = FFI }
        ordertype = invasion
        execute_order = no                    
        priority = 100
    }
}
SWE_ceasefire = { 
    allowed = { 
        original_tag = SWE
    }
    enable = { 
        has_idea = SWE_ceasefire
    }
    abort_when_not_enabled = yes
    ai_strategy = {
        type = send_volunteers_desire
        id = FFI
        value = -100
    }
}