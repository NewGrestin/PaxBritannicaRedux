CHR_Focus_on_PGP_and_NWG = {
    allowed = { 
        original_tag = CHR
    }

    enable = {
        NWG = {
            exists = yes
            has_war_with = CHR
        }
        SPR = {
            NOT = { has_war_with = NWG }
        }
    }

    abort_when_not_enabled = yes

    # These are state IDs you want CHR to focus on - replace with accurate ones
    ai_strategy = {
        type = theatre_distribution_demand_increase
        value = 50
        id = 649
    }

    ai_strategy = {
        type = theatre_distribution_demand_increase
        value = 50
        id = 1215
    }

    ai_strategy = {
        type = theatre_distribution_demand_increase
        value = 50
        id = 491
    }

    ai_strategy = {
        type = naval_invasion_focus
        value = 250
    }

	ai_strategy = { 
		type = dont_defend_ally_borders id = "RLP" 
		value = 500 
	}
		ai_strategy = {
		type = garrison
		value = -100	
	}

	ai_strategy = {
		type = garrison_reinforcement_priority
		value = -100
	}
	
	ai_strategy = {
    type = naval_mission_threshold
    id = MISSION_CONVOY_RAIDING
    value = -100
}

}

SPR_Supports_NWG = {
    allowed = {
        original_tag = SPR
    }

    enable = {
        NWG = {
            exists = yes
            has_war_with = CHR
        }
        SPR = {
            NOT = { has_war_with = NWG }
        }
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = send_volunteers_desire
        id = NWG
        value = 100
    }

    ai_strategy = {
        type = send_lend_lease_desire
        id = NWG
        value = 80
    }

}
