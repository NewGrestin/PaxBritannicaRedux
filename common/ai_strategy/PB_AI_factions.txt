### Faction Initiative Spending Priorities ###

### Facilities ###
faction_add_facility = {
	allowed = { has_dlc = "No Compromise, No Surrender" }
	enable = {
		is_actual_major = yes # Only majors should ever assign their facilities
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = spent_faction_initiative_priority
		id = program
		value = 10 # Moderate Priority as not that important
	}
}

### Military Operations ###
faction_create_faction_theatres = {
	allowed = { has_dlc = "No Compromise, No Surrender" }
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = spent_faction_initiative_priority
		id = unlock_faction_commander
		value = 30 #AI will heavily prioritise creating faction theatres. Makes doctrine sharing more powerful.
	}
}

faction_doctrine_sharing_focus = {
	allowed = { has_dlc = "No Compromise, No Surrender" }
	enable = {
		always = yes
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = spent_faction_initiative_priority
		id = unlock_doctrine_sharing
		value = 20 #AI will often prioritise doctrine sharing
	}
}

default_become_spymaster_major = {
	allowed = {
			has_dlc = "La Resistance"
	}
	enable = {
		is_major = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = become_spymaster
		value = 30
	}
}

default_become_spymaster_minor = {
	allowed = {
			has_dlc = "La Resistance"
	}
	enable = {
		is_major = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = become_spymaster
		value = 2
	}
}

default_become_head_of_crypto = {
	allowed = {
		has_dlc = "No Compromise, No Surrender"
			has_dlc = "La Resistance"
	}
	enable = { always = yes }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = become_head_of_crypto
		value = 2
	}
}

default_become_head_of_operations = {
	allowed = {
		has_dlc = "No Compromise, No Surrender"
			has_dlc = "La Resistance"
	}
	enable = { always = yes }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = become_head_of_operations
		value = 2
	}
}

default_become_counter_intel = {
	allowed = {
		has_dlc = "No Compromise, No Surrender"
		has_dlc = "La Resistance"
	}
	enable = { always = yes }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = become_head_of_counter_intel
		value = 2
	}
}

default_become_faction_leader = {
	enable = { always = yes }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = pp_spend_priority
		id = become_faction_leader
		value = 100
	}
}
