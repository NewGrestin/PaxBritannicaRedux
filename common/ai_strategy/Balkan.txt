join_commonwealth_pact = {
	
	enable = {
	
		NOT = { tag = SER }
		SER = { is_in_faction = yes }
		
		OR = {		#These countries should always desire alliance
			tag = CRO
			tag = BUL
			tag = BOS
			tag = ALB
			tag = GRE
			has_country_flag = join_balkan
		}
		
		NOT = { has_country_flag = left_balkan }

		OR = {
			has_government = conservative
			has_government = social_liberal
			has_government = social_democrat
			has_government = socialist
			has_government = monarchist
			has_government = ultranationalist
			has_government = authoritarianism
		}
		
		OR = {
			SER = { has_government = conservative }
			SER = { has_government = social_liberal }
			SER = { has_government = social_democrat }
			SER = { has_government = socialist }
			SER = { has_government = monarchist }
			SER = { has_government = ultranationalist }
			SER = { has_government = authoritarianism }
		}
		
	}
	
	abort = {
		OR = {
			has_war_with = SER
			SER = { is_in_faction = no }
			SER = { exists = no }
		}
	}
	
	ai_strategy = {
		type = befriend
		id = "SER"
		value = 50
	}
	ai_strategy = {
		type = alliance
		id = "SER"
		value = 125
	}
}