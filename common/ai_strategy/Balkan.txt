join_commonwealth_pact = {
	
	enable = {
	
		NOT = { tag = SER }
		SER = { is_in_faction = yes }
		
		OR = {		#These countries should always desire alliance
			tag = CRO
			tag = BUL
			tag = BOS
			tag = ALB
			tag = GRE
			has_country_flag = join_balkan
		}
		
		NOT = { has_country_flag = left_balkan }

		OR = {
			has_government = cn
			has_government = lb
			has_government = ss
			has_government = jc
			has_government = ds
			has_government = rc
			has_government = ni
		}
		
		OR = {
			SER = { has_government = cn }
			SER = { has_government = lb }
			SER = { has_government = ss }
			SER = { has_government = jc }
			SER = { has_government = ds }
			SER = { has_government = rc }
			SER = { has_government = ni }
		}
		
	}
	
	abort = {
		OR = {
			has_war_with = SER
			SER = { is_in_faction = no }
			SER = { exists = no }
		}
	}
	
	ai_strategy = {
		type = befriend
		id = "SER"
		value = 50
	}
	ai_strategy = {
		type = alliance
		id = "SER"
		value = 125
	}
}