PB_ENG_British_Civil_War = {

	PB_Focus_Secure_Greater_London = {

		icon = generic_industry

		available = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy

					NOT = {
						has_country_flag = ENG_Securing_State
					}
				}
			}
		}

		visible = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy
				}
			}
		}

		highlight_states = {
			state = 126
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 15

		remove_effect = {
			custom_effect_tooltip = ENG_England_Control_Increase.tt
			modify_country_flag = { flag = ENG_England_Controlled value = 1 }

			clr_country_flag = ENG_Securing_state
		}

		complete_effect = {
			custom_effect_tooltip = ENG_Securing_state

			set_country_flag = ENG_Securing_State
		}
	}

	PB_Focus_East_Anglia = {

		icon = generic_industry

		available = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy

					NOT = {
						has_country_flag = ENG_Securing_State
					}
				}
			}
		}

		visible = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy
				}
			}
		}

		highlight_states = {
			state = 125
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 15

		remove_effect = {
			custom_effect_tooltip = ENG_England_Control_Increase.tt
			modify_country_flag = { flag = ENG_England_Controlled value = 1 }

			clr_country_flag = ENG_Securing_state
		}

		complete_effect = {
			custom_effect_tooltip = ENG_Securing_state

			set_country_flag = ENG_Securing_State
		}
	}

	PB_Focus_East_Midlands = {

		icon = generic_industry

		available = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy

					NOT = {
						has_country_flag = ENG_Securing_State
					}
				}
			}
		}

		visible = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy
				}
			}
		}

		highlight_states = {
			state = 129
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 15

		remove_effect = {
			custom_effect_tooltip = ENG_England_Control_Increase.tt
			modify_country_flag = { flag = ENG_England_Controlled value = 1 }

			clr_country_flag = ENG_Securing_state
		}

		complete_effect = {
			custom_effect_tooltip = ENG_Securing_state

			set_country_flag = ENG_Securing_State
		}
	}

	PB_Focus_West_Midlands = {

		icon = generic_industry

		available = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy

					NOT = {
						has_country_flag = ENG_Securing_State
					}
				}
			}
		}

		visible = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy
				}
			}
		}

		highlight_states = {
			state = 128
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 15

		remove_effect = {
			custom_effect_tooltip = ENG_England_Control_Increase.tt
			modify_country_flag = { flag = ENG_England_Controlled value = 1 }

			clr_country_flag = ENG_Securing_state
		}

		complete_effect = {
			custom_effect_tooltip = ENG_Securing_state

			set_country_flag = ENG_Securing_State
		}
	}

	PB_Focus_Wales = {

		icon = generic_industry

		available = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy

					NOT = {
						has_country_flag = ENG_Securing_State
					}
				}
			}
		}

		visible = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy
				}
			}
		}

		highlight_states = {
			state = 122
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 15

		remove_effect = {
			custom_effect_tooltip = ENG_Wales_Control_Increase.tt
			modify_country_flag = { flag = ENG_Wales_Controlled value = 1 }

			clr_country_flag = ENG_Securing_state
		}

		complete_effect = {
			custom_effect_tooltip = ENG_Securing_state

			set_country_flag = ENG_Securing_State
		}
	}

	PB_Focus_Lothian = {

		icon = generic_industry

		available = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy

					NOT = {
						has_country_flag = ENG_Securing_State
					}
				}
			}
		}

		visible = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy
				}
			}
		}

		highlight_states = {
			state = 121
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 15

		remove_effect = {
			custom_effect_tooltip = ENG_Scotland_Control_Increase.tt
			modify_country_flag = { flag = ENG_Scotland_Controlled value = 1 }

			clr_country_flag = ENG_Securing_state
		}

		complete_effect = {
			custom_effect_tooltip = ENG_Securing_state

			set_country_flag = ENG_Securing_State
		}
	}

	PB_Focus_Lanark = {

		icon = generic_industry

		available = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy

					NOT = {
						has_country_flag = ENG_Securing_State
					}
				}
			}
		}

		visible = {
			tag = ENG

			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy
				}
			}
		}

		highlight_states = {
			state = 133
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 15

		remove_effect = {
			custom_effect_tooltip = ENG_Scotland_Control_Increase.tt
			modify_country_flag = { flag = ENG_Scotland_Controlled value = 1 }

			clr_country_flag = ENG_Securing_state
		}

		complete_effect = {
			custom_effect_tooltip = ENG_Securing_state

			set_country_flag = ENG_Securing_State
		}
	}


	PB_English_Insurrection = {

		icon = revolt

		available = {
			NOT = { has_idea = ENG_Anarchy }
		}

		days_mission_timeout = 5

		activation = {
			has_idea = ENG_Anarchy

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_English_Anarchy
				}
			}
		}

		visible = {
			
		}	

		fixed_random_seed = no

		fire_only_once = yes

		is_good = no

		timeout_effect = {

			set_global_flag = Dissolution_of_Britain

			hidden_effect = {

				every_country = {
					limit = {
						is_subject_of = ENG
					}
					country_event = { id = britain.89 }
					ENG = {
						end_puppet = PREV
					}
				}

				every_country = {
					limit = {
						exists = yes
						NOT = {
							tag = ENG
						}
					}
					PREV = {
						country_event = { id = britain.91 }
					}
				}
						
				if = {
					limit = {
						ENG = {
							has_country_flag = { flag = ENG_England_Controlled value > 4 }
						}
					}
					ENG = {
						country_event = { id = britain.84 }
					}
				}

				if = {
					limit = {
						ENG = {
							has_country_flag = { flag = ENG_England_Controlled value < 4 }
						}
					}
					ENG = {
						country_event = { id = britain.85 }
					}
				}

				if = {
					limit = {
						ENG = {
							has_country_flag = { flag = ENG_Scotland_Controlled value < 2 }
						}
					}
					ENG = {
						country_event = { id = britain.86 }
					}
				}

				if = {
					limit = {
						ENG = {
							NOT = {
								has_country_flag = { flag = ENG_Wales_Controlled value = 2 }
							}
						}
					}
					ENG = {
						country_event = { id = britain.87 }
					}
				}

				ENG = { 
					country_event = { id = britain.88 } 
				}
			}
		}
	}
}