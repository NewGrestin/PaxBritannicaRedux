generic_monarchist_coup = {

	generic_monarchist_propaganda = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = monarchism
				tag = ENG
				tag = AUS
				tag = DEN
			}
			monarchism > 0.2

		}

		visible = {
			always = yes
		}

		fire_only_once = no

		modifier = {
			monarchism_drift = 0.10
		}

		days_remove = 100
		days_re_enable = 200

		cost = 150

		complete_effect = {

		}
	}

	generic_empower_monarchy = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = monarchism
				tag = ENG
				tag = AUS
				tag = DEN
			}
			monarchism > 0.4

		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 100

		cost = 150

		complete_effect = {
			add_popularity = {
				ideology = monarchism popularity = 0.2
			}
		}
	}

	generic_monarchist_ally_military = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = monarchism
				tag = ENG
				tag = AUS
				tag = DEN
			}
			monarchism > 0.5

		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 100

		cost = 150

		complete_effect = {
			add_popularity = {
				ideology = monarchism popularity = 0.1
			}

			hidden_effect = {
				set_country_flag = Generic_Monarchist_Coup_Military_Support
			}
		}
	}

	generic_execute_monarchist_coup = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = monarchism
				tag = ENG
				tag = AUS
				tag = DEN
			}
			monarchism > 0.6
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 25

		cost = 150

		complete_effect = {
			hidden_effect = {
				if = {
					limit = {
						has_country_flag = Generic_Monarchist_Coup_Military_Support
					}
					country_event = { days = 26 id = political.1 }
				}

				if = {
					limit = {
						NOT = {
							has_country_flag = Generic_Monarchist_Coup_Military_Support
						}
					}
					country_event = { days = 26 id = political.2 }
				}
			}
		}
	}
}

