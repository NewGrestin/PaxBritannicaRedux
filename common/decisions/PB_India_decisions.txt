PB_India_War = {

	IND_Declare_national_government = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			always =yes
		}
		available = {
			num_of_controlled_states > 6
		}
        cost = 20
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 1
    	}
		remove_effect = {
			set_country_flag = PB_Indian_Unifier
		}
	}

	PB_attack_India = {
		icon = GFX_decision_generic_operation
		cost = 20

		allowed = {
			OR = {
				original_tag = MUG
				original_tag = AWD
				original_tag = MAR
				original_tag = SIN
				original_tag = BDK
				original_tag = FOM
				original_tag = BAN
				original_tag = HYD
				original_tag = BHP
				original_tag = NEP
				original_tag = ORI
				original_tag = MDR
				original_tag = MYS
				original_tag = JKM
			}
		}
		available = {
			has_war = no

			hidden_trigger = {
				OR = { 
					date > 1934.02.01
					has_country_flag = India_Debug
				}
			}
			custom_trigger_tooltip = {
				tooltip = PNK_Recently_Declared_War_flag_tt
				NOT = {
					has_country_flag = {
						flag = INDIA_Recently_Declared_War_flag
						days < 70
					}
				}
			}
    	}
		visible = {
			is_neighbor_of = FROM			
		}

		targets = { JKM MYS MDR ORI NEP BHP FOM BAN HYD SIN MAR AWD MUG BDK FOM }
    	targets_dynamic = yes
		war_with_target_on_remove = yes
		complete_effect = {
			set_country_flag = INDIA_Recently_Declared_War_flag
		}
		remove_effect = {
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
		days_remove = 15
		cancel_trigger = {
			FROM = {
				OR = {
					exists = no
					has_war_with = ROOT
				}
			}
		}
	}
}