PB_Domestic_Decisions = {

	PB_Rally_Public_Support = {

		icon = generic_decision

		cost = 100

		ai_will_do = {
			factor = 1000
		}

		modifier = {
			political_power_factor = -0.1

			stability_weekly = 0.05
		}

		days_remove = 50
		days_re_enable = 100
	}

	PB_Grant_Local_Autonomy = {

		icon = generic_decision

		cost = 50

		ai_will_do = {
			factor = 1000
		}

		available = {
			custom_trigger_tooltip = {
				tooltip = centralization_below_limit_tt
				OR = {
					AND = {
						has_government = ni
						check_variable = { centralization < 1 }
					}

					AND = {
						has_government = ds
						check_variable = { centralization < 1 }
					}

					AND = {
						has_government = rc
						check_variable = { centralization < 0.8 }
					}

					AND = {
						has_government = an
						check_variable = { centralization < 0.3 }
					}

					AND = {
						has_government = cn
						check_variable = { centralization < 0.5 }
					}

					AND = {
						has_government = lb
						check_variable = { centralization < 0.5 }
					}

					AND = {
						has_government = ss
						check_variable = { centralization < 0.6 }
					}

					AND = {
						has_government = jc
						check_variable = { centralization < 1 }
					}
				}	
			}
		}

		modifier = {
			political_power_factor = -0.1

			stability_weekly = 0.05
		}

		days_remove = 70
		days_re_enable = 100

		remove_effect = {
			PB_increase_centralization_moderate = yes
		}
	}
}