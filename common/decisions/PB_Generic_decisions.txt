PB_Generic_Balkan_War = {
	Generic_Join_Hungary = {
		icon = GFX_decision_generic_upgrade_colony

		allowed = {
			OR = {
				tag = SLO
				tag = SLA
				tag = CRO
				tag = DAL
				tag = SER
				tag = MOL
				tag = ROM
				tag = BUL
				tag = ALB
				tag = GRE
			}
		}

		available = {
			is_in_faction = no
			BUL = {
				has_war_with = ROM
			}
		}

        	cost = 20

		ai_will_do = {
			factor = 2
		}

		fire_only_once = yes

		selectable_mission = yes
		days_remove = 0

		is_good = yes

		remove_effect = {
			HUN = {
				add_to_faction = ROOT
			}
		}
	}

	Generic_Join_Romania = {

		icon = GFX_decision_generic_upgrade_colony

		allowed = {
			OR = {
				tag = SLO
				tag = SLA
				tag = CRO
				tag = DAL
				tag = SER
				tag = MOL
				tag = ROM
				tag = BUL
				tag = ALB
				tag = GRE
			}
		}
		visible = {
			is_in_faction = no
		}

		available = {
			is_in_faction = no
			BUL = {
				has_war_with = ROM
			}
		}

        	cost = 20

		ai_will_do = {
			factor = 2
		}

		fire_only_once = yes

		selectable_mission = yes
		days_remove = 0

		is_good = yes

		remove_effect = {
			ROM = {
				add_to_faction = ROOT
			}
		}
	}	
}
PB_Generic_Arabian_Unification = {
	ARAB_attack_target = {
		name = "Strike [FROM.GetNameDef]"
		icon = GFX_decision_generic_operation
		allowed = {
			tag_is_eligible_for_arabian_generic_tree = yes
		}
		target_root_trigger = {
			has_war = no
		}
		target_trigger = {
			is_owner_neighbor_of = FROM
		}
		targets = { JBS SAU IKW NAN SDT YEM OMA }
		on_map_mode = map_and_decisions_view
		cost = 25
		days_remove = 15
		ai_will_do = {
			base = 100
			modifier = {
				has_war = no
				factor = 10
			}
		}
		war_with_target_on_remove = yes
		remove_effect = {
			ROOT = {
				declare_war_on = {
					target = FROM
					type = annex_everything
				}
			}
		}
		highlight_states = {
			highlight_states_trigger = {
				is_owned_by = FROM
			}
			highlight_color_while_active = 3
			highlight_color_before_active = 2
		}
	}
	SAU_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = SAU
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = SAU_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
			else_if = {
				limit = { has_country_flag = SAU_Tier_1_Formed }
				PB_Arabian_Unification_States = yes
			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 3
    	}
		remove_effect = {
			if = {
				limit = { NOT = { has_country_flag = SAU_Tier_1_Formed } }
				set_cosmetic_tag = SAU_1
				set_country_flag = SAU_Tier_1_Formed
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = SAU
						}
					}
					add_core_of = SAU
				}
				add_research_slot = 1
			}
			if = {
				limit = {
					has_country_flag = SAU_Tier_1_Formed 
					NOT = { has_country_flag = united_arabia }
				}
				set_cosmetic_tag = SAU_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = SAU
						}
					}
					add_core_of = SAU
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}

	IKW_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = IKW
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = IKW_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
			else_if = {
				limit = { has_country_flag = IKW_Tier_1_Formed }
				PB_Arabian_Unification_States = yes
			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 3
    	}
		remove_effect = {
			if = {
				limit = { NOT = { has_country_flag = IKW_Tier_1_Formed } }
				set_cosmetic_tag = IKW_1
				set_country_flag = IKW_Tier_1_Formed
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = IKW
						}
					}
					add_core_of = IKW
				}
				add_research_slot = 1
			}
			if = {
				limit = {
					has_country_flag = IKW_Tier_1_Formed 
					NOT = { has_country_flag = united_arabia }
				}
				set_cosmetic_tag = IKW_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = IKW
						}
					}
					add_core_of = IKW
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}

	JBS_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = JBS
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = JBS_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
			else_if = {
				limit = { has_country_flag = JBS_Tier_1_Formed }
				PB_Arabian_Unification_States = yes
			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 3
    	}
		remove_effect = {
			if = {
				limit = { NOT = { has_country_flag = JBS_Tier_1_Formed } }
				set_cosmetic_tag = JBS_1
				set_country_flag = JBS_Tier_1_Formed
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = JBS
						}
					}
					add_core_of = JBS
				}
				add_research_slot = 1
			}
			if = {
				limit = {
					has_country_flag = JBS_Tier_1_Formed 
					NOT = { has_country_flag = united_arabia }
				}
				set_cosmetic_tag = JBS_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = JBS
						}
					}
					add_core_of = JBS
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}

	NAN_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = NAN
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = NAN_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
			else_if = {
				limit = { has_country_flag = NAN_Tier_1_Formed }
				PB_Arabian_Unification_States = yes
			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 3
    	}
		remove_effect = {
			if = {
				limit = { NOT = { has_country_flag = NAN_Tier_1_Formed } }
				set_cosmetic_tag = NAN_1
				set_country_flag = NAN_Tier_1_Formed
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = NAN
						}
					}
					add_core_of = NAN
				}
				add_research_slot = 1
			}
			if = {
				limit = {
					has_country_flag = NAN_Tier_1_Formed 
					NOT = { has_country_flag = united_arabia }
				}
				set_cosmetic_tag = NAN_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = NAN
						}
					}
					add_core_of = NAN
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}
}
PB_Krakatoa_Indonesia_Unification = {
	KRA_attack_target = {
		name = "Attack [FROM.GetNameDef]"
		icon = GFX_decision_generic_operation
		allowed = {
			tag = KRA
		}
		target_root_trigger = {
			has_war = no
		}
		target_trigger = {
			is_owner_neighbor_of = FROM
		}
		targets = { ACE HAR DEL INS USS CCO LAN KAL MKS SUL PNG }
		on_map_mode = map_and_decisions_view
		cost = 25
		days_remove = 15
		ai_will_do = {
			base = 100
			modifier = {
				has_war = no
				factor = 10
			}
		}
		war_with_target_on_remove = yes
		remove_effect = {
			ROOT = {
				declare_war_on = {
					target = FROM
					type = annex_everything
				}
			}
		}
		highlight_states = {
			highlight_states_trigger = {
				is_owned_by = FROM
			}
			highlight_color_while_active = 3
			highlight_color_before_active = 2
		}
	}
	KRA_unite_indonesia = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = KRA
		}
		available = {
			num_of_controlled_states > 29
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 0
		remove_effect = {
			if = {
				set_cosmetic_tag = KRA_indonesia
				set_country_flag = KRA_united
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = KRA
						}
					}
					add_core_of = KRA
				}
				add_research_slot = 1
			}
		}
	}
}
