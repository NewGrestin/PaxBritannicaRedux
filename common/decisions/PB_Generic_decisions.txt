PB_Generic_Balkan_War = {
	Generic_Join_Hungary = {
		icon = GFX_decision_generic_upgrade_colony

		visible = {
			OR = {
				tag = SLO
				tag = SLA
				tag = CRO
				tag = DAL
				tag = SER
				tag = MOL
				tag = ROM
				tag = BUL
				tag = ALB
				tag = GRE
			}
		}

		available = {
			is_in_faction = no
			BUL = {
				has_war_with = ROM
			}
		}

        	cost = 20

		ai_will_do = {
			factor = 2
		}

		fire_only_once = yes

		selectable_mission = yes
		days_remove = 0

		is_good = yes

		remove_effect = {
			HUN = {
				add_to_faction = ROOT
			}
		}
	}

	Generic_Join_Romania = {

		icon = GFX_decision_generic_upgrade_colony

		visible = {
			is_in_faction = no
			OR = {
				tag = SLO
				tag = SLA
				tag = CRO
				tag = DAL
				tag = SER
				tag = MOL
				tag = ROM
				tag = BUL
				tag = ALB
				tag = GRE
			}
		}

		available = {
			is_in_faction = no
			BUL = {
				has_war_with = ROM
			}
		}

        	cost = 20

		ai_will_do = {
			factor = 2
		}

		fire_only_once = yes

		selectable_mission = yes
		days_remove = 0

		is_good = yes

		remove_effect = {
			ROM = {
				add_to_faction = ROOT
			}
		}
	}	
}
PB_Generic_Arabian_Unification = {
	WWW_TAB_Tier_One = {

		icon = GFX_decision_generic_form_nation

		available = {
			num_of_controlled_states > 4
		}

		visible = {
			original_tag = TAB
		}

        cost = 100

		ai_will_do = {
			factor = 4000
		}

		fire_only_once = yes

		selectable_mission = yes
		days_remove = 20

		is_good = yes

		modifier = {
       	 	civilian_factory_use = 3
    	}

		remove_effect = {
			custom_effect_tooltip = TAB_Great_Basin_Republic
			hidden_effect = {
				set_cosmetic_tag = TAB_1
				set_country_flag = TAB_Tier_1_Formed
			}
			every_owned_state = { 
				limit = {
					NOT = {
						is_core_of = TAB
					}
				}
				add_core_of = TAB
			}
			add_research_slot = 1
		}
	}	
}

