PB_Generic_Balkan_War = {
	Generic_GRE_Peace_Out_Hungary = {
		icon = GFX_decision_generic_upgrade_colony

		allowed = {
			tag = GRE
		}

		available = {
			has_global_flag = PB_GBW_Bulgaria_Defeated
			has_war_with = HUN
			gbw_started = yes
		}
		
        cost = 20

		ai_will_do = {
			factor = 2
		}

		fire_only_once = yes

		selectable_mission = yes
		days_remove = 0

		is_good = yes

		remove_effect = {
			HUN = { 
				country_event = balkwar.12
			}
		}
	}
}

PB_Generic_Arabian_Unification = {
	ARAB_attack_target = {
		name = "Strike [FROM.GetNameDef]"
		icon = GFX_decision_generic_operation
		allowed = {
			tag_is_eligible_for_arabian_generic_tree = yes
		}
		target_root_trigger = {
			has_war = no
		}
		target_trigger = {
			is_owner_neighbor_of = FROM
		}
		targets = { JBS SAU IKW NAN SDT }
		on_map_mode = map_and_decisions_view
		cost = 25
		days_remove = 15
		ai_will_do = {
			base = 100
			modifier = {
				has_war = no
				factor = 10
			}
		}
		war_with_target_on_remove = yes
		remove_effect = {
			ROOT = {
				every_state = {
					limit = {
						is_core_of = FROM
					}
					add_claim_by = ROOT
				}
				declare_war_on = {
					target = FROM
					type = annex_everything
				}
			}
		}
		highlight_states = {
			highlight_states_trigger = {
				is_owned_by = FROM
			}
			highlight_color_while_active = 3
			highlight_color_before_active = 2
		}
	}
	SAU_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = SAU
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = SAU_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
			else_if = {
				limit = { has_country_flag = SAU_Tier_1_Formed }

			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 		civilian_factory_use = 3
    		}
		remove_effect = {
			set_cosmetic_tag = SAU_1
			set_country_flag = SAU_Tier_1_Formed
			every_owned_state = { 
				limit = {
					NOT = {
						is_core_of = SAU
					}
				}
				add_core_of = SAU
			}
			add_research_slot = 1
		}
	}

	IKW_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = IKW
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = IKW_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 3
    	}
		remove_effect = {
			if = {
				limit = { NOT = { has_country_flag = IKW_Tier_1_Formed } }
				set_cosmetic_tag = IKW_1
				set_country_flag = IKW_Tier_1_Formed
				every_owned_state = {
					if = {
						limit = {
							NOT = {
								is_core_of = IKW
							}
						}
						add_core_of = IKW
					}
				}
				add_research_slot = 1
			}
		}
	}

	JBS_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = JBS
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = JBS_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 3
    	}
		remove_effect = {
			if = {
				limit = { NOT = { has_country_flag = JBS_Tier_1_Formed } }
				set_cosmetic_tag = JBS_1
				set_country_flag = JBS_Tier_1_Formed
				every_owned_state = {
					if = {
						limit = {
							NOT = {
								is_core_of = JBS
							}
						}
						add_core_of = JBS
					}
				}
				add_research_slot = 1
			}
		}
	}

	NAN_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = NAN
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = NAN_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
			else_if = {
				limit = { has_country_flag = NAN_Tier_1_Formed }
				PB_Arabian_Unification_States = yes
			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 3
    	}
		remove_effect = {
			set_cosmetic_tag = NAN_1
			set_country_flag = NAN_Tier_1_Formed
			every_owned_state = { 
				limit = {
					NOT = {
						is_core_of = NAN
					}
				}
				add_core_of = NAN
			}
			add_research_slot = 1
		}
	}
	SDT_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = SDT
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = SDT_Tier_1_Formed } }
				num_of_controlled_states > 4
			}
		}
		visible = {
			NOT = { has_country_flag = united_arabia }
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = no
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 3
    	}
		remove_effect = {
			set_cosmetic_tag = SDT_1
			set_country_flag = SDT_Tier_1_Formed
			every_owned_state = { 
				limit = {
					NOT = {
						is_core_of = SDT
					}
				}
				add_core_of = SDT
			}
			add_research_slot = 1
		}
	}
	SAU_unite_arabia = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = SAU
		}
		available = {
			num_of_controlled_states > 10
			has_country_flag = SAU_Tier_1_Formed 
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 0
		remove_effect = {
			if = {
				set_cosmetic_tag = SAU_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = SAU
						}
					}
					add_core_of = SAU
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}
	IKW_unite_arabia = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = IKW
		}
		available = {
			num_of_controlled_states > 10
			has_country_flag = IKW_Tier_1_Formed 
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 0
		remove_effect = {
			if = {
				set_cosmetic_tag = IKW_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = IKW
						}
					}
					add_core_of = IKW
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}
	NAN_unite_arabia = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = NAN
		}
		available = {
			num_of_controlled_states > 10
			has_country_flag = NAN_Tier_1_Formed 
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 0
		remove_effect = {
			if = {
				set_cosmetic_tag = NAN_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = NAN
						}
					}
					add_core_of = NAN
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}
	SDT_unite_arabia = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = SDT
		}
		available = {
			num_of_controlled_states > 10
			has_country_flag = SDT_Tier_1_Formed 
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 0
		remove_effect = {
			if = {
				set_cosmetic_tag = SDT_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = SDT
						}
					}
					add_core_of = SDT
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}
	JBS_unite_arabia = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = JBS
		}
		available = {
			num_of_controlled_states > 10
			has_country_flag = JBS_Tier_1_Formed 
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 0
		remove_effect = {
			if = {
				set_cosmetic_tag = JBS_2
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = JBS
						}
					}
					add_core_of = JBS
				}
				add_research_slot = 1
				set_country_flag = united_arabia
			}
		}
	}
}
PB_Krakatoa_Indonesia_Unification = {
	KRA_attack_target = {
		name = "Strike [FROM.GetNameDef]"
		icon = GFX_decision_generic_operation
		target_root_trigger = {
			has_war = no
		}
		target_trigger = {
			is_owner_neighbor_of = FROM
		}
		targets = { ACE HAR USS CCO LAN KAL MKS PNG INS DEL }
		on_map_mode = map_and_decisions_view
		cost = 25
		days_remove = 15
		ai_will_do = {
			base = 100
			modifier = {
				has_war = no
				factor = 10
			}
		}
		war_with_target_on_remove = yes
		remove_effect = {
			ROOT = {
				every_state = {
					limit = {
						is_core_of = FROM
					}
					add_claim_by = ROOT
				}
				declare_war_on = {
					target = FROM
					type = annex_everything
				}
			}
		}
		highlight_states = {
			highlight_states_trigger = {
				is_owned_by = FROM
			}
			highlight_color_while_active = 3
			highlight_color_before_active = 2
		}
	}
	KRA_unite_indonesia = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = KRA
		}
		available = {
			num_of_controlled_states > 29
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 0
		remove_effect = {
			if = {
				set_cosmetic_tag = KRA_indonesia
				set_country_flag = KRA_united
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = KRA
						}
					}
					add_core_of = KRA
				}
				add_research_slot = 1
			}
		}
	}
}

PB_Dutch_Indonesia_Unification = {
	INS_attack_target = {
		name = "Strike [FROM.GetNameDef]"
		icon = GFX_decision_generic_operation
		target_root_trigger = {
			has_war = no
		}
		target_trigger = {
			is_owner_neighbor_of = FROM
		}
		targets = { ACE HAR KRA USS CCO LAN KAL MKS PNG }
		on_map_mode = map_and_decisions_view
		cost = 25
		days_remove = 15
		ai_will_do = {
			base = 100
			modifier = {
				has_war = no
				factor = 10
			}
		}
		war_with_target_on_remove = yes
		remove_effect = {
			ROOT = {
				every_state = {
					limit = {
						is_core_of = FROM
					}
					add_claim_by = ROOT
				}
				declare_war_on = {
					target = FROM
					type = annex_everything
				}
			}
		}
		highlight_states = {
			highlight_states_trigger = {
				is_owned_by = FROM
			}
			highlight_color_while_active = 3
			highlight_color_before_active = 2
		}
	}
	INS_unite_indonesia = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = INS
		}
		available = {
			num_of_controlled_states > 29
		}
        cost = 100
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 0
		remove_effect = {
			if = {
				set_cosmetic_tag = INS_indonesia
				set_country_flag = INS_united
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = INS
						}
					}
					add_core_of = INS
				}
				add_research_slot = 1
			}
		}
	}
}
