RUS_power_struggle = {


###monarch
	RUS_tolstoy_speech = {

		icon = generic_decision

		visible = {
			tag = SOV
			OR = {
				has_country_flag = power_struggle
				has_global_flag = kara_boga
			}
		}
		ai_will_do = {
			factor = 0
		}

		fire_only_once = yes

		modifier = {
			monarchism_drift = 0.10
		}

		days_remove = 30

		cost = 50

		complete_effect = {
			country_event = { id = russia_fl.6 hours = 6 }
			hidden_effect = {
				add_to_variable = { var = monarchy value = 1 }
				add_to_variable = { var = bolshevism value = 1 }
			}
		}
	}

	RUS_succession_law = {

		icon = generic_decision

		visible = {
			tag = SOV
			OR = {
				has_country_flag = power_struggle
				has_global_flag = kara_boga
			}
		}

		ai_will_do = {
			factor = 0
		}

		fire_only_once = yes

		modifier = {
			monarchism_drift = 0.10
		}

		days_remove = 30

		cost = 50

		complete_effect = {
			country_event = { id = russia_fl.6 hours = 6 }
			hidden_effect = {
				add_to_variable = { var = monarchy value = 1 }
			}
		}
	}

###kolchak
	RUS_bribe_denikin = {
		icon = generic_decision
		fire_only_once = yes

		visible = {
			tag = SOV
			OR = {
				has_country_flag = power_struggle
				has_global_flag = kara_boga
			}
		}

		cost = 25
		complete_effect = {
			add_command_power = -10
			set_country_flag = denikin_bribed
			country_event = { id = russia_fl.2 hours = 6 }
		}
	}	

	RUS_bribe_kappel = {
		icon = generic_decision
		fire_only_once = yes

		visible = {
			tag = SOV
			OR = {
				has_country_flag = power_struggle
				has_global_flag = kara_boga
			}
		}

		cost = 25
		complete_effect = {
			add_command_power = -10
			set_country_flag = kappel_bribed
			country_event = { id = russia_fl.3 hours = 6 }
		}
	}	

	RUS_bribe_wrangel = {
		icon = generic_decision
		fire_only_once = yes

		visible = {
			tag = SOV
			OR = {
				has_country_flag = power_struggle
				has_global_flag = kara_boga
			}
		}

		cost = 25
		complete_effect = {
			add_command_power = -10
			set_country_flag = wrangel_bribed
			country_event = { id = russia_fl.4 hours = 6 }
		}
	}

###democracy
	RUS_meet_chernov = {

		icon = generic_decision

		visible = {
			tag = SOV
			OR = {
				has_country_flag = power_struggle
				has_global_flag = kara_boga
			}
		}
		ai_will_do = {
			factor = 0
		}

		fire_only_once = yes

		modifier = {
			monarchism_drift = 0.10
		}

		days_remove = 30

		cost = 50

		complete_effect = {
			country_event = { id = russia_fl.6 hours = 6 }
			hidden_effect = {
				add_to_variable = { var = democracy value = 1 }
				subtract_from_variable = { var = bolshevism value = 1 }
			}
		}
	}

	RUS_consult_britain = {

		icon = generic_decision

		visible = {
			tag = SOV
			OR = {
				has_country_flag = power_struggle
				has_global_flag = kara_boga
			}
		}

		ai_will_do = {
			factor = 0
		}

		fire_only_once = yes

		modifier = {
			monarchism_drift = 0.10
		}

		days_remove = 30

		cost = 50

		complete_effect = {
			country_event = { id = russia_fl.6 hours = 6 }
			hidden_effect = {
				add_to_variable = { var = democracy value = 1 }
			}
		}
	}



###communist uprising
	RUS_hunt_tukh = {
		icon = generic_nationalism
		target_trigger = {
			FROM = {	
				tag = KOR
			}
		}
		ai_will_do = {
			factor = 1.5
		}
		fire_only_once = yes
		modifier = 
			political_power_gain = -.25
		}
		days_remove = 1000
		cost = 100
		complete_effect = {
			hidden_effect = {
				SOV = {
					country_event = { id = tbd days = 250 random = 1000 }
				}
			}
		}
	}










}