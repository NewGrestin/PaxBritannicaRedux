PB_POL_Hunt = {

    PB_POL_Collect_Evidences= {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			country_event = {
				id = pol.8
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				POL = {
					set_country_flag = POL_evidences_collected
				}
			}
		}
	}

	PB_POL_Crossexamine_Winesses = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			country_event = {
				id = pol.9
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				POL = {
					set_country_flag = POL_witnesses_examined
				}
			}
		}
	}

	PB_POL_Analyse_All_Materials = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
			has_country_flag = POL_evidences_collected
			has_country_flag = POL_witnesses_examined
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			country_event = {
				id = pol.10
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				POL = {
					set_country_flag = POL_materials_analysed
				}
			}
		}
	}

	PB_POL_Focus_On_Moscicki = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_materials_analysed
			NOT = {
					has_country_flag = POL_moscicki_escaped
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
				has_country_flag = POL_focus_on_niewiadomski
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 2

		remove_effect = {
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				POL = {
					set_country_flag = POL_focus_on_moscicki
				}
			}
		}
	}

	PB_POL_Examine_Lviv_Academics = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_moscicki
			NOT = {
					has_country_flag = POL_moscicki_escaped
					has_country_flag = POL_academics_examined
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 10

		remove_effect = {
			random_list = {
				90 = {
					country_event = {
						id = pol.11
					}
				}
				10 = {
					country_event = {
						id = pol.15
					}
				}
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_academics_examined
			}
		}
	}

	PB_POL_Torture_Lviv_Academics = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_moscicki
			NOT = {
					has_country_flag = POL_moscicki_escaped
					has_country_flag = POL_academics_examined
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			random_list = {
				65 = {
					country_event = {
						id = pol.11
					}
				}
				35 = {
					country_event = {
						id = pol.15
					}
				}
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_academics_examined
			}
		}
	}

	PB_POL_Search_In_Lukasiewicz_Chemicals = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_moscicki
			NOT = {
					has_country_flag = POL_moscicki_escaped
				}
			NOT = {
					has_country_flag = POL_search_in_luksiewicz
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 10

		remove_effect = {
			random_list = {
				80 = {
					country_event = {
						id = pol.12
					}
				}
				20 = {
					country_event = {
						id = pol.14
					}
				}
			}
			hidden_effect = {
				set_country_flag = POL_search_in_luksiewicz
				clr_country_flag = POL_ongoing_investigation
			}
		}
	}

	PB_POL_Send_Black_Ops_To_Lukasiewicz = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_moscicki
			NOT = {
					has_country_flag = POL_moscicki_escaped
				}
			NOT = {
					has_country_flag = POL_search_in_luksiewicz
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			random_list = {
				50 = {
					country_event = {
						id = pol.12
					}
				}
				50 = {
					country_event = {
						id = pol.14
					}
				}
			}
			hidden_effect = {
				set_country_flag = POL_search_in_luksiewicz
				clr_country_flag = POL_ongoing_investigation
			}
		}
	}

	PB_POL_Look_In_Hospitals = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_moscicki
			NOT = {
					has_country_flag = POL_moscicki_escaped
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 10

		remove_effect = {
			country_event = {
				id = pol.13
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_look_in_hospitals
			}
		}
	}

	PB_POL_Arrest_Moscicki = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_moscicki
			NOT = {
					has_country_flag = POL_moscicki_escaped
				}
			
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
			has_country_flag = POL_found_moscicki

		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 10

		remove_effect = {
			if = {
				limit = {
						AND = {
							has_country_flag = POL_search_in_luksiewicz_success
							has_country_flag = POL_examine_academics_success
						}
				}
				random_list = {
					90 = {
						country_event = {
							id = pol.22
						}
					}
					10 = {
						country_event = {
							id = pol.23
						}
					}
				}
				else = {
					if = {
						limit = {
								OR = {
									has_country_flag = POL_search_in_luksiewicz_success
									has_country_flag = POL_examine_academics_success
								}
						}
						random_list = {
							50 = {
								country_event = {
									id = pol.22
								}
							}
							50 = {
								country_event = {
									id = pol.23
								}
							}
						}
						else = {
							random_list = {
								35 = {
									country_event = {
										id = pol.22
									}
								}
								65 = {
									country_event = {
										id = pol.23
									}
								}
							}
						}
					}
				}
				
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				clr_country_flag = POL_focus_on_moscicki
			}
		}
	}

	PB_POL_Focus_On_Niewiadomski = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_materials_analysed
			NOT = {
					has_country_flag = POL_niewiadomski_escaped
				}

		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
				has_country_flag = POL_focus_on_moscicki
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 2

		remove_effect = {
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				POL = {
					set_country_flag = POL_focus_on_niewiadomski
				}
			}
		}
	}

	PB_POL_Follow_The_Footsteps = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_niewiadomski
			NOT = {
					has_country_flag = POL_niewiadomski_escaped
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 7

		remove_effect = {
			random_list = {
				20 = {
					country_event = {
						id = pol.31
					}
				}
				80 = {
					country_event = {
						id = pol.32
					}
				}
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_follow_the_footsteps
			}
		}
	}

	PB_POL_Engage_The_Crimial_Underworld = {
		icon = generic_decision

		cost = 25
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_niewiadomski
			NOT = {
					has_country_flag = POL_niewiadomski_escaped
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 12

		remove_effect = {
			random_list = {
				20 = {
					country_event = {
						id = pol.33
					}
				}
				80 = {
					country_event = {
						id = pol.34
					}
				}
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_engage_the_crimial_underworld
			}
		}
	}

	PB_POL_Mass_Arrests = {
		icon = generic_decision

		cost = 7
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_niewiadomski
			NOT = {
					has_country_flag = POL_niewiadomski_escaped
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 10

		remove_effect = {
			random_list = {
				15 = {
					country_event = {
						id = pol.35
					}
				}
				85 = {
					country_event = {
						id = pol.36
					}
				}
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_mass_arrests
			}
		}
	}

	PB_POL_Ask_Okhrana_For_Help = {
		icon = generic_decision

		cost = 20
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_niewiadomski
			NOT = {
					has_country_flag = POL_niewiadomski_escaped
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 15

		remove_effect = {
			random_list = {
				25 = {
					country_event = {
						id = pol.37
					}
				}
				75 = {
					country_event = {
						id = pol.38
					}
				}
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_ask_okhrana_for_help
			}
		}
	}

	PB_POL_Predict_Future_With_Computer = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_niewiadomski
			NOT = {
					has_country_flag = POL_niewiadomski_escaped
				}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			random_list = {
				10 = {
					country_event = {
						id = pol.39
					}
				}
				90 = {
					country_event = {
						id = pol.40
					}
				}
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_predict_future_with_computer
			}
		}
	}

	PB_POL_Arrest_Niewiadomski = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_focus_on_niewiadomski
			NOT = {
					has_country_flag = POL_niewiadomski_escaped
				}
			
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
			has_country_flag = POL_found_niewiadomski

		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 2

		remove_effect = {
			random_list = {
				65 = {
					country_event = {
						id = pol.24
					}
				}
				35 = {
					country_event = {
						id = pol.25
					}
				}
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				clr_country_flag = POL_focus_on_niewiadomski
			}
		}
	}

	PB_POL_Proposition_For_Moscicki = {
		icon = generic_decision

		cost = 5
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_moscicki_captured
		}

		available = {
			has_country_flag = POL_moscicki_captured
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 2

		remove_effect = {
			country_event = {
				id = pol.501
			}
		}
	}

	PB_POL_Streghten_Border_Controls = {
		icon = generic_decision

		cost = 25
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_materials_analysed
			NOT = {
				AND = {
					has_country_flag = POL_moscicki_end
					has_country_flag = POL_niewiadomski_end
				}
			}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			add_days_mission_timeout = {
				mission = POL_Moscicki_Escapes
				days = 20
			}
			add_days_mission_timeout = {
				mission = POL_Niewiadomski_Escapes
				days = 10
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				
			}
		}
	}

	PB_POL_Warrants_Of_Capture = {
		icon = generic_decision

		cost = 15
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			has_country_flag = POL_materials_analysed
			NOT = {
				AND = {
					has_country_flag = POL_moscicki_end
					has_country_flag = POL_niewiadomski_end
				}
			}
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			add_days_mission_timeout = {
				mission = POL_Moscicki_Escapes
				days = 15
			}
			add_days_mission_timeout = {
				mission = POL_Niewiadomski_Escapes
				days = 7
			}
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				
			}
		}
	}

	PB_POL_Arrest = {
		icon = generic_decision

		cost = 15
		ai_will_do = {
			factor = 100000
			
		}

		visible = {
			is_ai = yes
			NOT = {
				AND = {
					has_country_flag = POL_moscicki_end
					has_country_flag = POL_niewiadomski_end
				}
			}
			has_country_flag = POL_materials_analysed
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_ongoing_investigation
				}
			}
		}

		fire_only_once = yes

		days_remove = 5

		remove_effect = {
			hidden_effect = {
				clr_country_flag = POL_ongoing_investigation
				set_country_flag = POL_moscicki_captured
				set_country_flag = POL_niewiadomski_captured
				set_country_flag = POL_moscicki_end
				set_country_flag = POL_niewiadomski_end
			}
		}
	}

PB_POL_Make_A_Show_Trial = {
		icon = generic_decision

		cost = 20
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			NOT = {
				has_country_flag = POL_quick_trail
			}
			has_country_flag = POL_materials_analysed
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
			AND = {
				has_country_flag = POL_moscicki_end
				has_country_flag = POL_niewiadomski_end
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_show_trail
				}
			}
		}

		fire_only_once = yes

		days_remove = 70

		remove_effect = {
			add_manpower = -20
			add_political_power = 100
			if = {
				limit = {
						AND = {
							has_country_flag = POL_moscicki_captured
							has_country_flag = POL_niewiadomski_captured
						}
				}
				country_event = {
					id = pol.26
				}
				else = {
					if = {
						limit = {
								OR = {
									AND = {
										has_country_flag = POL_moscicki_captured
										has_country_flag = POL_niewiadomski_killed
									}
									AND = {
										has_country_flag = POL_moscicki_killed
										has_country_flag = POL_niewiadomski_killed
									}
									AND = {
										has_country_flag = POL_moscicki_killed
										has_country_flag = POL_niewiadomski_captured
									}
								}
						}
						country_event = {
							id = pol.27
						}
						else = {
							if = {
							limit = {
									OR = {
										AND = {
											has_country_flag = POL_moscicki_captured
											has_country_flag = POL_niewiadomski_escaped
										}
										AND = {
											has_country_flag = POL_moscicki_escaped
											has_country_flag = POL_niewiadomski_captured
										}
									}
							}
							country_event = {
								id = pol.28
							}
							else = {
								if = {
									limit = {
											OR = {
												AND = {
													has_country_flag = POL_moscicki_killed
													has_country_flag = POL_niewiadomski_escaped
												}
												AND = {
													has_country_flag = POL_moscicki_escaped
													has_country_flag = POL_niewiadomski_killed
												}
											}
									}
									country_event = {
										id = pol.29
									}
									else = {
										country_event = {
											id = pol.30
										}
									}
								}
							}
						}
					}
				}	
			}
		}
	}
}

	PB_POL_Make_A_Quick_Trial = {
		icon = generic_decision

		cost = 10
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			NOT = {
				has_country_flag = POL_show_trail
			}
			has_country_flag = POL_materials_analysed
		}

		available = {
			NOT = {
				has_country_flag = POL_ongoing_investigation
			}
			AND = {
				has_country_flag = POL_moscicki_end
				has_country_flag = POL_niewiadomski_end
			}
		}

		complete_effect = {
			hidden_effect = {
				POL = {
					set_country_flag = POL_quick_trail
				}
			}
		}

		fire_only_once = yes

		days_remove = 30

		remove_effect = {
			if = {
				limit = {
						AND = {
							has_country_flag = POL_moscicki_captured
							has_country_flag = POL_niewiadomski_captured
						}
				}
				country_event = {
					id = pol.26
				}
				else = {
					if = {
						limit = {
								OR = {
									AND = {
										has_country_flag = POL_moscicki_captured
										has_country_flag = POL_niewiadomski_killed
									}
									AND = {
										has_country_flag = POL_moscicki_killed
										has_country_flag = POL_niewiadomski_killed
									}
									AND = {
										has_country_flag = POL_moscicki_killed
										has_country_flag = POL_niewiadomski_captured
									}
								}
						}
						country_event = {
							id = pol.27
						}
						else = {
							if = {
							limit = {
									OR = {
										AND = {
											has_country_flag = POL_moscicki_captured
											has_country_flag = POL_niewiadomski_escaped
										}
										AND = {
											has_country_flag = POL_moscicki_escaped
											has_country_flag = POL_niewiadomski_captured
										}
									}
							}
							country_event = {
								id = pol.28
							}
							else = {
								if = {
									limit = {
											OR = {
												AND = {
													has_country_flag = POL_moscicki_killed
													has_country_flag = POL_niewiadomski_escaped
												}
												AND = {
													has_country_flag = POL_moscicki_escaped
													has_country_flag = POL_niewiadomski_killed
												}
											}
									}
									country_event = {
										id = pol.29
									}
									else = {
										country_event = {
											id = pol.30
										}
									}
								}
							}
						}
					}
				}	
			}
		}
	}
}
	POL_Moscicki_Escapes = {

		icon = GFX_decision_generic_civil_support
		
		activation = { 
			original_tag = POL
			has_country_flag = POL_materials_analysed            
		}

		available = {

			OR = {
				date > 1933.07.01
				has_country_flag = POL_moscicki_captured
				has_country_flag = POL_moscicki_killed
			}
		}  

		selectable_mission = no

		is_good = no
		fire_only_once = yes
				
		days_mission_timeout = 30

		complete_effect = {

			if = {
				limit = {
					NOT = {
						OR = {
							has_country_flag = POL_moscicki_captured
							has_country_flag = POL_moscicki_killed
						}
					}
				}
				country_event = {
					id = pol.20
				}
				hidden_effect = {
					POL = {
						set_country_flag = POL_moscicki_escaped
						set_country_flag = POL_moscicki_end
						clr_country_flag = POL_focus_on_moscicki
					}
				}
			}
		}

		timeout_effect = {
			country_event = {
				id = pol.20
			}
			hidden_effect = {
				POL = {
					set_country_flag = POL_moscicki_escaped
					set_country_flag = POL_moscicki_end
					clr_country_flag = POL_focus_on_moscicki
				}
			}
		}
	}

	POL_Niewiadomski_Escapes = {

		icon = GFX_decision_generic_civil_support
		
		activation = { 
			original_tag = POL
			has_country_flag = POL_materials_analysed            
		}

		available = {

			OR = {
				date > 1933.06.01
				has_country_flag = POL_niewiadomski_captured
				has_country_flag = POL_niewiadomski_killed
			}
		}  

		selectable_mission = no

		is_good = no
		fire_only_once = yes
				
		days_mission_timeout = 20

		complete_effect = {

			if = {
				limit = {
					NOT = {
						OR = {
							has_country_flag = POL_niewiadomski_captured
							has_country_flag = POL_niewiadomski_killed
						}
					}
				}
				country_event = {
					id = pol.21
				}
				hidden_effect = {
					POL = {
						set_country_flag = POL_niewiadomski_escaped
						set_country_flag = POL_niewiadomski_end
						clr_country_flag = POL_focus_on_niewiadomski
					}
				}
			}
		}

		timeout_effect = {
			country_event = {
				id = pol.21
			}
			hidden_effect = {
				POL = {
					set_country_flag = POL_niewiadomski_escaped
					set_country_flag = POL_niewiadomski_end
					clr_country_flag = POL_focus_on_niewiadomski
				}
			}
		}
	}

}