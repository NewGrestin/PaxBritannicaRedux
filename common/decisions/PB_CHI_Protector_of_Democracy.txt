CHI_Protector_of_Democracy = {

	PB_CHI_Detente_with_Japan = {

		icon = jap_pacific_guardian

		available = {
			tag = CHI
			JAP = {
				exists = yes
				NOT = {
					has_war_with = CHI
				}
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 7

		cost = 50

		remove_effect = {
			JAP = {
				country_event = { days = 3 id = ChinaExternal.38 } 
			}
		}
	}

	PB_CHI_Condemn_Japan = {

		icon = jap_pacific_guardian

		available = {
			tag = CHI
			JAP = {
				exists = yes
				NOT = {
					has_war_with = CHI
				}
				NOT = {
					has_government = conservative
					has_government = social_liberal
					has_government = social_democracy
				}
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 7

		cost = 50

		remove_effect = {
			JAP = {
				country_event = { days = 3 id = ChinaExternal.40 } 
			}
		}
	}

	PB_CHI_Liberate_Korea = {

		icon = jap_pacific_guardian

		available = {
			tag = CHI
			JAP = {
				exists = yes
				NOT = {
					has_war_with = CHI
				}
				NOT = {
					has_government = conservative
					has_government = social_liberal
					has_government = social_democracy
				}
			}
			KOR = {
				exists = yes
				is_puppet_of = JAP
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 25

		cost = 50

		remove_effect = {
			create_wargoal = { type = annex_everything target = KOR }
		}
	}

	PB_CHI_Promote_Mongolian_Democracy = {

		icon = generic_political_discourse

		available = {
			tag = CHI
			MON = {
				exists = yes
				NOT = {
					has_government = conservative
					has_government = social_liberal
					has_government = social_democracy
				}
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 25

		cost = 100

		remove_effect = {
			MON = {
				country_event = { days = 3 id = ChinaExternal.39 } 
			}
		}
	}

	PB_CHI_Promote_Anti_Colonialism = {

		icon = generic_political_discourse

		available = {
			tag = CHI
			FID = {
				exists = yes
				is_puppet = yes
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 50

		remove_effect = {
			FID = {
				add_opinion_modifier = { target = CHI modifier = anti_colonialism }
			}
			FRA = {
				add_opinion_modifier = { target = CHI modifier = anti_colonialism }
			}
		}
	}
}
