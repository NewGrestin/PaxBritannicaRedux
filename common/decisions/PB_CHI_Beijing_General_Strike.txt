PB_CHI_Beijing_General_Strike = {

	PB_CHI_Beijing_General_Strike_Approach_the_Reds = {

		icon = generic_political_actions

		available = {
			OR = {
				has_idea = CHI_Beijing_General_Strike
				has_idea = CHI_National_Strike
			}
		}

		visible = {
			OR = {
				has_idea = CHI_Beijing_General_Strike
				has_idea = CHI_National_Strike
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 0
		}

		modifier = {

		}

		days_remove = 3

		remove_effect = {
			add_political_power = -300
			country_event = { days = 5 id = china.49 }
		}
	}

	PB_CHI_Beijing_General_Strike_Crackdown_Beijing = {

		icon = generic_political_actions

		available = {
			tag = CHI

			CHI = {
				has_idea = CHI_Beijing_General_Strike
			}

			NOT = {
				has_idea = CHI_National_Strike
			}

			divisions_in_state = {
				state = 608	
				size > 5
			}
		}

		visible = {
			has_idea = CHI_Beijing_General_Strike
		}

		cancel_trigger = {
			divisions_in_state = {
				state = 610	
				size < 5
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 0
			modifier = {
				add = 2000
				divisions_in_state = {
					state = 610	
					size < 6
				}
			}
		}

		modifier = {

		}

		days_remove = 3

		remove_effect = {
			army_experience = 10
			hidden_effect = {
				country_event = { days = 30 id = china.54 }
			}
		}
	}

	PB_CHI_Beijing_General_Strike_Timeout = {

		icon = generic_civil_support

		available = {
			NOT = { 
				has_idea = CHI_Beijing_General_Strike 
			}
		}

		days_mission_timeout = 365

		activation = {
			has_idea = CHI_Beijing_General_Strike
		}

		visible = {
			has_idea = CHI_Beijing_General_Strike			
		}		

		fixed_random_seed = no

		fire_only_once = no

		is_good = no

		timeout_effect = {
			CHI = {
				country_event = { days = 5 id = china.48 }
			}
		}
	}

	PB_CHI_Beijing_National_Strike_Timeout = {

		icon = generic_civil_support

		available = {
			NOT = { has_idea = CHI_National_Strike }
		}

		days_mission_timeout = 200

		activation = {
			has_idea = CHI_National_Strike
		}

		visible = {
			has_idea = CHI_National_Strike			
		}		

		fixed_random_seed = no

		fire_only_once = no

		is_good = no

		timeout_effect = {
			add_political_power - 200
			CHI = {
				set_country_flag = CHI_Civil_War_Incoming
			}
		}
	}
}