PB_Military_Decisions = {

	PB_Study_Great_War_Tactics = {

		icon = generic_operation

		available = {
			NOT = {
				has_idea = PB_Fully_Modern_Military
			}
		}

		ai_will_do = {
			factor = 100
		}

		modifier = {
			political_power_gain = -0.1
		}

		days_remove = 30

		days_re_enable = 30

		custom_cost_trigger = { command_power > 4 }
		custom_cost_text = decision_cost_5_cp

		complete_effect = {
			command_power = -5
		}

		remove_effect = {
			custom_effect_tooltip = PB_Small_Modernization_Increase

			if = {
				limit = {
					check_variable = { PB_Military_Reform < 100 }
				}
				add_to_variable = {
					var = PB_Military_Reform
					value = 5
				}
			}
		}
	}

	PB_Training_Exercise = {

		icon = generic_prepare_civil_war

		available = {
			NOT = {
				has_idea = PB_Fully_Modern_Military
			}
		}

		ai_will_do = {
			factor = 100
		}

		modifier = {
			political_power_gain = -0.1
		}

		days_remove = 30

		days_re_enable = 30

		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 500 }
		}
		custom_cost_text = decision_cost_500_infequip

		complete_effect = {
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = -500
				producer = ROOT
			}
		}

		remove_effect = {
			custom_effect_tooltip = PB_Moderate_Modernization_Increase

			if = {
				limit = {
					check_variable = { PB_Military_Reform < 100 }
				}
				add_to_variable = {
					var = PB_Military_Reform
					value = 5
				}
			}
		}
	}

	PB_Generic_Officers_Recruitment = {

		icon = generic_army_support

		available = {
			NOT = {
				has_idea = PB_Fully_Modern_Military
			}
		}

		ai_will_do = {
			factor = 100
		}

		modifier = {
			experience_gain_army_factor = -0.1
		}

		days_remove = 30

		days_re_enable = 30

		custom_cost_trigger = { command_power > 9 }
		custom_cost_text = decision_cost_10_cp

		complete_effect = {
			command_power = -10
		}

		remove_effect = {
			custom_effect_tooltip = PB_Small_Modernization_Increase

			if = {
				limit = {
					check_variable = { PB_Military_Reform < 100 }
				}
				add_to_variable = {
					var = PB_Military_Reform
					value = 5
				}
			}
		}
	}

	PB_Examine_Wartime_Landships = {

		icon = generic_tank

		available = {
			NOT = {
				has_idea = PB_Fully_Modern_Military
			}
		}

		ai_will_do = {
			factor = 100
		}

		modifier = {
			civilian_factory_use = 5
		}

		days_remove = 30

		days_re_enable = 60

		custom_cost_trigger = { command_power > 9 }
		custom_cost_text = decision_cost_10_cp

		complete_effect = {
			command_power = -20
		}

		remove_effect = {
			custom_effect_tooltip = PB_Large_Modernization_Increase

			if = {
				limit = {
					check_variable = { PB_Military_Reform < 100 }
				}
				add_to_variable = {
					var = PB_Military_Reform
					value = 10
				}
			}
		}
	}

	PB_Expand_Military_Academies = {

		icon = generic_army_support

		available = {
			NOT = {
				has_idea = PB_Fully_Modern_Military
			}
		}

		ai_will_do = {
			factor = 100
		}

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 30

		days_re_enable = 30

		custom_cost_trigger = { command_power > 19 }
		custom_cost_text = decision_cost_20_cp

		complete_effect = {
			command_power = -10
		}

		remove_effect = {
			custom_effect_tooltip = PB_Large_Modernization_Increase

			if = {
				limit = {
					check_variable = { PB_Military_Reform < 100 }
				}
				add_to_variable = {
					var = PB_Military_Reform
					value = 15
				}
			}
		}
	}

	PB_Expand_Research_Facilities = {

		icon = generic_construction

		available = {
			NOT = {
				has_idea = PB_Fully_Modern_Military
			}
		}

		ai_will_do = {
			factor = 100
		}

		modifier = {
			civilian_factory_use = 5
		}

		days_remove = 30

		days_re_enable = 60

		custom_cost_trigger = { command_power > 19 }
		custom_cost_text = decision_cost_20_cp

		complete_effect = {
			command_power = -20
		}

		remove_effect = {
			custom_effect_tooltip = PB_Very_Large_Modernization_Increase

			if = {
				limit = {
					check_variable = { PB_Military_Reform < 100 }
				}
				add_to_variable = {
					var = PB_Military_Reform
					value = 25
				}
			}
		}
	}

	PB_Military_Reform_Timeout = {
		icon = generic_operation

		available = {
			NOT = {
				ROOT = {
					check_variable = { PB_Military_Reform > 99 }
				} 
			}
		}

		activation = {                         
			ROOT = {
				check_variable = { PB_Military_Reform > 99 }
			}                 
		}

		fire_only_once = yes

		is_good = yes

		cost = 0

		ai_will_do = {
			factor = 10
		}

		days_mission_timeout = 15

		timeout_effect = {
			ROOT = {
				country_event = { id = milmod.1 } 
			}
		}
	}

