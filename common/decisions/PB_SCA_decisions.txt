PB_SCA_The_Sleeping_Giant = {

    SCA_With_Open_Eyes = {

		icon = GFX_decision_infiltrate_state

		activation = { 
			tag = SCA
			has_country_flag = SCA_In_Exile
			NOT = { has_country_flag = SCA_Hiding has_war_with = AME } 
			has_completed_focus = SCA_The_Dead_March_West                
		}

		available = {
			OR = {
				SCA = { 
					OR = { 
						has_country_flag = SCA_Hiding 
						has_country_flag = SCA_AME_PEACE 
						has_war_with = AME  
					} 
				}	
			}     
		}  

		selectable_mission = no

		is_good = no
		fire_only_once = no
				
		days_mission_timeout = 100

		timeout_effect = {
			AME = {
				country_event = { id = SCA.32 days = 3 }
			}
			custom_effect_tooltip = SCA29tooltip
		}
	}

	SCA_Hide_From_The_Giant = {

		icon = GFX_decision_generic_operation

		available = { OR = { has_active_mission = SCA_With_Open_Eyes has_active_mission = SCA_With_Open_Eyes_2 } }

		visible = {
			has_country_flag = SCA_In_Exile
			has_completed_focus = SCA_The_Dead_March_West
		}

		cancel_if_invalid = yes

		cost = num_battalions

		ai_will_do = {
			factor = 4000
		}

		days_renewable = 40

		complete_effect = {
			set_country_flag = {
				flag = SCA_Hiding
				days = 30
				value = 1
			}
			custom_effect_tooltip = SCA28tooltip
			hidden_effect = {
				#if = {
				#	limit = { 
				#		has_completed_focus = SCA_Hush_Campaigns
				#	}
				#	activate_mission = SCA_With_Open_Eyes_2
				#}
				#if = {
				#	limit = { 
				#		NOT = { has_completed_focus = SCA_Hush_Campaigns }
				#	}
				#	activate_mission = SCA_With_Open_Eyes
				#}
			}
		}
	}

	SCA_Stabilize_The_Nation = {

		icon = GFX_decision_generic_political_discourse

		visible = {
			has_country_flag = SCA_In_Exile
			has_completed_focus = SCA_The_First_Seeds
			NOT = {
				has_stability > 0.5
			}
		}

		cancel_if_invalid = yes

		cost = 50

		ai_will_do = {
			factor = 4000
		}

		days_renewable = 40

		complete_effect = {
			add_stability = 0.05
		}
	}

	SCA_Recruit_Cowboys = {

		icon = GFX_decision_generic_civil_support

		visible = {
			has_country_flag = SCA_In_Exile
			has_completed_focus = SCA_Outlaw_Country
		}

		available = {
			has_manpower < 5000
		}

		cancel_if_invalid = yes

		cost = 10

		ai_will_do = {
			factor = 4000
		}

		days_renewable = 40

		complete_effect = {
			add_war_support = -0.025
			add_manpower = 5000
		}
	}


	SCA_Hide_From_The_Giant_2 = {

		icon = GFX_decision_generic_operation

		#available = { has_active_mission = SCA_With_Open_Eyes }

		visible = {
			has_country_flag = SCA_In_Exile
			has_completed_focus = SCA_Safe_And_Sound 
		}

		cancel_if_invalid = yes

		cost = num_armies

		ai_will_do = {
			factor = 4000
		}

		days_renewable = 40

		complete_effect = {
			set_country_flag = {
				flag = SCA_Hiding
				days = 30
				value = 1
			}
			custom_effect_tooltip = SCA28tooltip
			hidden_effect = {
				if = {
					limit = { 
						has_completed_focus = SCA_Hush_Campaigns
					}
					activate_mission = SCA_With_Open_Eyes_2
				}
				if = {
					limit = { 
						NOT = { has_completed_focus = SCA_Hush_Campaigns }
					}
					activate_mission = activate_mission = SCA_With_Open_Eyes
				}
			}
		}
	}

	SCA_Buy_Time = {

		icon = GFX_decision_generic_special_forces

		available = { 
			num_divisions > 5
			has_manpower > 2000
			has_active_mission = SCA_With_Open_Eyes
		}

		visible = {
			has_country_flag = SCA_In_Exile
		}

		custom_cost_trigger = { has_stability > 0.045 }
		custom_cost_text = decision_cost_5_stability

		ai_will_do = {
			factor = 3000
		}

		days_renewable = 50

		complete_effect = {
			add_stability = -0.05
			add_manpower = -2000
			add_days_mission_timeout = {
				mission = SCA_With_Open_Eyes
				days = 30
			}
		}
	}
}

PB_SCA_The_Final_Few = {

	SCA_The_Final_Few = {

		icon = GFX_decision_generic_upgrade_colony

		available = {
			has_country_flag = All_Thats_Left
		}  

		activation = { 
			tag = SCA
			has_country_flag = SCA_In_Exile
			has_completed_focus = SCA_The_Dead_March_West
		}

		selectable_mission = no
		days_mission_timeout = 30

		is_good = no
		fire_only_once = yes

		timeout_effect = {
			if = {
				limit = {
					has_country_flag = SCA_Final_Few_1
				}
				country_event = { id = SCA.2 days = 0 }
			}
			if = {
				limit = {
					has_country_flag = SCA_Final_Few_2
				}
				country_event = { id = SCA.3 days = 0 }
			}
			if = {
				limit = {
					has_country_flag = SCA_Final_Few_3
				}
				country_event = { id = SCA.4 days = 0 }
			}
			if = {
				limit = {
					has_country_flag = SCA_Final_Few_4
				}
				country_event = { id = SCA.5 days = 0 }
			}
			if = {
				limit = {
					has_country_flag = SCA_Final_Few_5
				}
				country_event = { id = SCA.6 days = 0 }
			}
			if = {
				limit = {
					has_country_flag = SCA_Final_Few_6
				}
				country_event = { id = SCA.7 days = 0 }
			}
			hidden_effect = {
				if = {
					limit = {
						NOT = { has_country_flag = All_Thats_Left }
					}
					activate_mission = SCA_The_Final_Few
				}
			}
		}
	}
}

