PB_SCA_The_Final_Few = {

        SCA_The_Final_Few = {

		icon = GFX_decision_generic_upgrade_colony

		available = {
                        has_country_flag = All_Thats_Left
		}  

                activation = { 
                       tag = SCA
                       has_country_flag = SCA_In_Exile
                 }

		selectable_mission = no
		days_mission_timeout = 30

		is_good = no
		fire_only_once = yes

		timeout_effect = {
                if = {
                     limit = {
                             has_country_flag = SCA_Final_Few_1
                     }
                     country_event = { id = SCA.2 days = 0 }
                }
                if = {
                     limit = {
                             has_country_flag = SCA_Final_Few_2
                     }
                     country_event = { id = SCA.3 days = 0 }
                }
                if = {
                     limit = {
                             has_country_flag = SCA_Final_Few_3
                     }
                     country_event = { id = SCA.4 days = 0 }
                }
                if = {
                     limit = {
                             has_country_flag = SCA_Final_Few_4
                     }
                     country_event = { id = SCA.5 days = 0 }
                }
                if = {
                     limit = {
                             has_country_flag = SCA_Final_Few_5
                     }
                     country_event = { id = SCA.6 days = 0 }
                }
                if = {
                     limit = {
                             has_country_flag = SCA_Final_Few_6
                     }
                     country_event = { id = SCA.7 days = 0 }
                }
                hidden_effect = {
                if = {
                     limit = {
                             NOT = { has_country_flag = All_Thats_Left }
                     }
                     activate_mission = SCA_The_Final_Few
                }
                }
                }
         }
}

PB_SCA_The_Northern_Menace = {

        SCA_The_Crow_Offensive = {

		icon = GFX_decision_infiltrate_state

		available = {
                        has_war_with = LAK
		}  

                activation = { 
                       tag = SCA
                       has_country_flag = SCA_In_Exile
                 }

		selectable_mission = no
		days_mission_timeout = 365

		is_good = no
		fire_only_once = yes

		timeout_effect = {
                       country_event = { id = SCA.8 days = 0 }
                       SCA = {
                             declare_war_on = {
                                   target = LAK
                                   type = annex_everything
                             }
                       }
                }
         }

         SCA_Emergency_Draft = {

		icon = GFX_decision_generic_militia

		available = { 
                           has_manpower > 3000
                           NOT = { has_country_flag = SCA_LAK_WAR_OVER }
		}

		visible = {
                has_country_flag = SCA_In_Exile
                NOT = { has_country_flag = SCA_LAK_WAR_OVER }
                }

                cancel_if_invalid = yes

		cost = 50

		ai_will_do = {
			factor = 100
		}

                days_remove = 15

                days_renable = 30

		remove_effect = { 
                custom_effect_tooltip = SCA3tooltip
                        add_manpower = -3000
			hidden_effect = {
				random_owned_controlled_state = {
                                        limit = {
                                             is_in_home_area = yes
                                        }
					create_unit = {
						division = "name = \"Butlerite Militia\" division_template = \"Butlerite Militia\" start_experience_factor = 1.0 start_equipment_factor = 0.3"
						owner = SCA
					}
				}
			}
                }
	}

}