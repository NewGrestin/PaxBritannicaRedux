PB_African_Pacification = {

	PB_Pacify_Kenya = {

		icon = generic_ignite_civil_war

		available = {
			tag = ENG

			ENG = {
				owns_state = 547
				controls_state = 547
				has_equipment = {
				    infantry_equipment_1 > 500
				}
			}

			divisions_in_state = {
				state = 547	
				size > 3
			}

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_African_Imperialism
				}
			}
		}

		visible = {
			tag = ENG

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_African_Imperialism
				}
			}
		}

		modifier = {
			stability_weekly = -0.01
		}

		highlight_states = {
			state = 547
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 0
			modifier = {
				add = 2000
				divisions_in_state = {
					state = 610	
					size > 6
				}
			}
		}

		days_remove = 25

		complete_effect = {
			add_ideas = ENG_African_Brutality
		}

		remove_effect = {
			set_country_flag = ENG_Kenya_Pacified

			remove_ideas = ENG_African_Brutality

			add_equipment_to_stockpile = {
			    type = infantry_equipment
			    amount = -500
			    producer = ENG
			}
		}
	}

	PB_Pacify_Nigeria = {

		icon = generic_ignite_civil_war

		available = {
			tag = ENG

			ENG = {
				owns_state = 558
				controls_state = 558
				has_equipment = {
				    infantry_equipment_1 > 500
				}
			}

			divisions_in_state = {
				state = 558	
				size > 3
			}

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_African_Imperialism
				}
			}
		}

		visible = {
			tag = ENG

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_African_Imperialism
				}
			}
		}

		modifier = {
			stability_weekly = -0.01
		}

		highlight_states = {
			state = 558
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 0
			modifier = {
				add = 2000
				divisions_in_state = {
					state = 558	
					size > 6
				}
			}
		}

		days_remove = 25

		complete_effect = {
			add_ideas = ENG_African_Brutality
		}

		remove_effect = {
			set_country_flag = ENG_Nigeria_Pacified

			remove_ideas = ENG_African_Brutality

			add_equipment_to_stockpile = {
			    type = infantry_equipment
			    amount = -500
			    producer = ENG
			}
		}
	}

	PB_Pacify_Uganda = {

		icon = generic_ignite_civil_war

		available = {
			tag = ENG

			ENG = {
				owns_state = 548
				controls_state = 548
				has_equipment = {
				    infantry_equipment_1 > 500
				}
			}

			divisions_in_state = {
				state = 548	
				size > 3
			}

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_African_Imperialism
				}
			}
		}

		visible = {
			tag = ENG

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_African_Imperialism
				}
			}
		}

		modifier = {
			stability_weekly = -0.01
		}

		highlight_states = {
			state = 548
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 0
			modifier = {
				add = 2000
				divisions_in_state = {
					state = 548	
					size > 6
				}
			}
		}

		days_remove = 25

		complete_effect = {
			add_ideas = ENG_African_Brutality
		}

		remove_effect = {
			set_country_flag = ENG_Uganda_Pacified

			remove_effect = ENG_African_Brutality

			add_equipment_to_stockpile = {
			    type = infantry_equipment
			    amount = -500
			    producer = ENG
			}
		}
	}

	PB_Pacify_Tanganyika = {

		icon = generic_ignite_civil_war

		available = {
			tag = ENG

			ENG = {
				owns_state = 546
				controls_state = 546
				has_equipment = {
				    infantry_equipment_1 > 500
				}
			}

			divisions_in_state = {
				state = 546	
				size > 3
			}

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_African_Imperialism
				}
			}
		}

		visible = {
			tag = ENG

			hidden_trigger = {
				ENG = {
					has_country_flag = ENG_African_Imperialism
				}
			}
		}

		modifier = {
			stability_weekly = -0.01
		}

		highlight_states = {
			state = 548
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 0
			modifier = {
				add = 2000
				divisions_in_state = {
					state = 546	
					size > 6
				}
			}
		}

		days_remove = 25

		complete_effect = {
			add_ideas = ENG_African_Brutality
		}

		remove_effect = {
			set_country_flag = ENG_Tanganyika_Pacified

			remove_ideas = ENG_African_Brutality

			add_equipment_to_stockpile = {
			    type = infantry_equipment
			    amount = -500
			    producer = ENG
			}
		}
	}
}