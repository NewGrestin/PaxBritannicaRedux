generic_communist_coup = {

	generic_communist_propaganda = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = authsoc
				tag = ENG
				tag = AUS
				tag = DEN
			}
			authsoc > 0.2

		}

		visible = {
			always = yes
		}

		fire_only_once = no

		modifier = {
			authsoc_drift = 0.10
		}

		days_remove = 100
		days_re_enable = 200

		cost = 150

		complete_effect = {

		}
	}

	generic_approach_unions = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = authsoc
				tag = ENG
				tag = AUS
				tag = DEN
			}
			authsoc > 0.4

		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 100

		cost = 150

		complete_effect = {
			add_popularity = {
				ideology = authsoc popularity = 0.2
			}
		}
	}

	generic_communist_ally_military = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = authsoc
				tag = ENG
				tag = AUS
				tag = DEN
			}
			authsoc > 0.5

		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 100

		cost = 150

		complete_effect = {
			add_popularity = {
				ideology = authsoc popularity = 0.1
			}

			hidden_effect = {
				set_country_flag = Generic_Communist_Coup_Military_Support
			}
		}
	}

	generic_execute_communist_coup = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = authsoc
				tag = ENG
				tag = AUS
				tag = DEN
			}
			authsoc > 0.6
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 25

		cost = 150

		complete_effect = {
			hidden_effect = {
				if = {
					limit = {
						has_country_flag = Generic_Communist_Coup_Military_Support
					}
					country_event = { days = 26 id = political.7 }
				}

				if = {
					limit = {
						NOT = {
							has_country_flag = Generic_Communist_Coup_Military_Support
						}
					}
					country_event = { days = 26 id = political.8 }
				}
			}
		}
	}
}

