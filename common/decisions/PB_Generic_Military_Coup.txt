generic_military_coup = {

	generic_military_propaganda = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = authoritarianism
				tag = ENG
				tag = AUS
				tag = DEN
			}
			authoritarianism > 0.2

		}

		visible = {
			always = yes
		}

		fire_only_once = no

		modifier = {
			authoritarianism_drift = 0.10
		}

		days_remove = 100
		days_re_enable = 200

		cost = 150

		complete_effect = {

		}
	}

	generic_expand_clique = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = authoritarianism
				tag = ENG
				tag = AUS
				tag = DEN
			}
			authoritarianism > 0.4

		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 100

		cost = 150

		complete_effect = {
			add_popularity = {
				ideology = authoritarianism popularity = 0.2
			}
		}
	}

	generic_military_ally_military = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = authoritarianism
				tag = ENG
				tag = AUS
				tag = DEN
			}
			authoritarianism > 0.5

		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 100

		cost = 150

		complete_effect = {
			add_popularity = {
				ideology = authoritarianism popularity = 0.1
			}

			hidden_effect = {
				set_country_flag = Generic_Military_Coup_Military_Support
			}
		}
	}

	generic_execute_military_coup = {

		icon = generic_propaganda

		available = {
			NOT = {
				has_government = authoritarianism
				tag = ENG
				tag = AUS
				tag = DEN
			}
			authoritarianism > 0.6
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		days_remove = 25

		cost = 150

		complete_effect = {
			hidden_effect = {
				if = {
					limit = {
						has_country_flag = Generic_Military_Coup_Military_Support
					}
					country_event = { days = 26 id = political.3 }
				}

				if = {
					limit = {
						NOT = {
							has_country_flag = Generic_Military_Coup_Military_Support
						}
					}
					country_event = { days = 26 id = political.4 }
				}
			}
		}
	}
}

