CHI_Greater_China = {

	PB_CHI_Revanchist_Fervor = {

		icon = generic_civil_support

		available = {
			NOT = {
				has_idea = CHI_Revanchist_Fervor
			}
		}

		days_mission_timeout = 365

		activation = {
			has_idea = CHI_Revanchist_Fervor
		}

		visible = {
			
		}		

		modifier = {
			war_support_weekly = 0.01
		}

		fixed_random_seed = no

		fire_only_once = yes

		is_good = yes

		timeout_effect = {
			remove_idea = CHI_Revanchist_Fervor
		}
	}

	PB_CHI_Strike_at_Mongolia = {

		icon = generic_tank

		available = {
			tag = CHI
			MON = {
				exists = yes
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 25

		remove_effect = {
			create_wargoal = { type = annex_everything target = MON }
		}
	}

	PB_CHI_Assimilate_Mongolia = {

		icon = oppression

		available = {
			tag = CHI
			MON = {
				exists = no
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {
			stability_weekly = -0.01
		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 150

		cost = 100

		remove_effect = {
			every_state = {
    			limit = {
        			is_core_of = MON
        		}

    			CHI = {
        			add_state_core = PREV
    			}
    		}
		}
	}

	PB_CHI_Strike_at_Korea = {

		icon = generic_tank

		available = {
			tag = CHI
			KOR = {
				exists = yes
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 25

		remove_effect = {
			create_wargoal = { type = annex_everything target = KOR }
		}
	}

	PB_CHI_Assimilate_Korea = {

		icon = oppression

		available = {
			tag = CHI
			KOR = {
				exists = no
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {
			stability_weekly = -0.01
		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 150

		cost = 100

		remove_effect = {
			every_state = {
    			limit = {
        			is_core_of = KOR
        		}

    			CHI = {
        			add_state_core = PREV
    			}
    		}
		}
	}

	PB_CHI_Strike_at_Indochina = {

		icon = generic_tank

		available = {
			tag = CHI
			FID = {
				exists = yes
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 25

		remove_effect = {
			create_wargoal = { type = annex_everything target = FID }
		}
	}

	PB_CHI_Assimilate_Indochina = {

		icon = oppression

		available = {
			tag = CHI
			FID = {
				exists = no
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {
			stability_weekly = -0.01
		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 150

		cost = 100

		remove_effect = {
			every_state = {
    			limit = {
        			is_core_of = KOR
        		}

    			FID = {
        			add_state_core = PREV
    			}
    		}
		}
	}

	PB_CHI_End_Japan = {

		icon = generic_tank

		available = {
			tag = CHI
			MON = {
				exists = no
			}
			KOR = {
				exists = no
			}
			JAP = {
				exists = yes
			}
		}

		visible = {
			always = yes
		}

		fire_only_once = yes

		modifier = {

		}

		ai_will_do = {
			factor = 1000
		}

		days_remove = 25

		remove_effect = {
			create_wargoal = { type = annex_everything target = JAP }
		}
	}
}
