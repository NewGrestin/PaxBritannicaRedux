PB_Release_Puppet = {

	# BRITISH OCCUPATION
	PB_British_Occupation = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			NOT = {
				OR = {
					tag = ENG
					tag = OBR
				}
			}
			ROOT = {
				AND = {
					has_full_control_of_state = 131
					has_full_control_of_state = 130
					has_full_control_of_state = 132
					has_full_control_of_state = 129
					has_full_control_of_state = 128
					has_full_control_of_state = 125
					has_full_control_of_state = 126
					has_full_control_of_state = 338
					has_full_control_of_state = 127
					has_full_control_of_state = 128
					has_full_control_of_state = 123
				}
			}
			NOT = {
				has_global_flag = ENG_Collaborators_Installed
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				AND = {
					has_full_control_of_state = 131
					has_full_control_of_state = 130
					has_full_control_of_state = 132
					has_full_control_of_state = 129
					has_full_control_of_state = 128
					has_full_control_of_state = 125
					has_full_control_of_state = 126
					has_full_control_of_state = 338
					has_full_control_of_state = 127
					has_full_control_of_state = 128
					has_full_control_of_state = 123
				}
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.1 
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.1 
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# FRENCH OCCUPATION
	PB_French_Occupation = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			FRA = {
				exists = no
			}
			OFA = {
				exists = no
			}
			NOT = {
				OR = {
					tag = FRA
					tag = OFA
					AND = {
						tag = GER
						has_country_flag = PB_Manchester_Conference
					}
					AND = {
						tag = ENG
						has_country_flag = PB_Manchester_Conference
					}
					AND = {
						tag = AME
						has_country_flag = PB_Manchester_Conference
					}
				}
			}
			ROOT = {
				owns_state = 16
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				AND = {
					owns_state = 16
				}
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.2
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.2
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# GERMAN OCCUPATION - Rhineland
	PB_German_Occupation_Rhineland = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			RHH = {
				exists = no
			}
			NOT = {
				OR = {
					tag = GER
					tag = MGE
					tag = RHH
					tag = FRA
				}
			}
			ROOT = {
				owns_state = 51
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				AND = {
					owns_state = 51
				}
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.3
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.3
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# GERMAN OCCUPATION - Alsace
	PB_German_Occupation_Alsace = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			NOT = {
				OR = {
					tag = GER
					tag = MGE
					tag = RHH
					tag = FRA
				}
			}
			ROOT = {
				owns_state = 28
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				AND = {
					owns_state = 28
				}
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.4
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.4
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# GERMAN OCCUPATION - South Germany
	PB_German_Occupation_South_Germany = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			BAV = {
				exists = no
			}
			NOT = {
				OR = {
					tag = GER
					tag = MGE
					tag = FRA
					tag = BAV
					tag = RUS
				}
			}
			ROOT = {
				owns_state = 52
				owns_state = 4
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				AND = {
					owns_state = 52
					owns_state = 4
				}
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.5
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.5
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# GERMAN OCCUPATION - North Germany
	PB_German_Occupation_North_Germany = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			BRN = {
				exists = no
			}
			NOT = {
				OR = {
					tag = GER
					tag = MGE
					tag = FRA
					tag = BRN
					tag = RUS
				}
			}
			ROOT = {
				owns_state = 55
				owns_state = 64
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				AND = {
					owns_state = 55
					owns_state = 64
				}
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.6
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.6
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# GERMAN OCCUPATION - Bohemia
	PB_German_Occupation_Bohemia = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			CZE = {
				exists = no
			}
			NOT = {
				OR = {
					tag = GER
					tag = MGE
					tag = CZE
				}
			}
			ROOT = {
				owns_state = 9
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				AND = {
					owns_state = 9
				}
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.7
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.7
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# GERMAN OCCUPATION - Luxembourg
	PB_German_Occupation_Luxembourg = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			LUX = {
				exists = no
			}
			NOT = {
				OR = {
					tag = GER
					tag = MGE
					tag = LUX
				}
			}
			ROOT = {
				owns_state = 8
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				AND = {
					owns_state = 8
				}
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.8
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.8
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# ITALIAN OCCUPATION - North
	PB_Occupation_Italy_North = { 

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			ITA = {
				exists = no
			}
			NOT = {
				OR = {
					tag = ITA
					tag = SAV
					tag = TUS
					tag = LMB
					tag = PRM 
					tag = MOD
					tag = PAP
					tag = SRI
				}
			}
			ROOT = {
				owns_state = 832
				owns_state = 158
				owns_state = 831
				owns_state = 159
				owns_state = 161
				owns_state = 835
				owns_state = 834
				owns_state = 162
				owns_state = 157
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 832
				owns_state = 158
				owns_state = 831
				owns_state = 159
				owns_state = 161
				owns_state = 835
				owns_state = 834
				owns_state = 162
				owns_state = 157
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.9
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.9
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# ITALIAN OCCUPATION - South
	PB_Occupation_Italy_South = {

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			ITA = {
				exists = no
			}
			NOT = {
				OR = {
					tag = ITA
					tag = SAV
					tag = TUS
					tag = LMB
					tag = PRM 
					tag = MOD
					tag = PAP
					tag = SRI
				}
			}
			ROOT = {
				owns_state = 833
				owns_state = 117
				owns_state = 156
				owns_state = 115
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 833
				owns_state = 117
				owns_state = 156
				owns_state = 115
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.10
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.10
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# ITALIAN OCCUPATION - Rome
	PB_Occupation_Italy_Papacy = {

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			ITA = {
				exists = no
			}
			NOT = {
				OR = {
					tag = ITA
					tag = SAV
					tag = TUS
					tag = LMB
					tag = PRM 
					tag = MOD
					tag = PAP
					tag = SRI
				}
			}
			ROOT = {
				owns_state = 2
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 2
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.11
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.11
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# ITALIAN OCCUPATION - Venice
	PB_Occupation_Italy_Venice = {

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			ITA = {
				exists = no
			}
			NOT = {
				OR = {
					tag = ITA
					tag = SAV
					tag = TUS
					tag = LMB
					tag = PRM 
					tag = MOD
					tag = PAP
					tag = SRI
				}
			}
			ROOT = {
				owns_state = 160
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 160
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.12
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.12
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# ITALIAN OCCUPATION - Sardinia
	PB_Occupation_Italy_Sardinia = {

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			ITA = {
				exists = no
			}
			NOT = {
				OR = {
					tag = ITA
					tag = SAV
					tag = TUS
					tag = LMB
					tag = PRM 
					tag = MOD
					tag = PAP
					tag = SRI
				}
			}
			ROOT = {
				owns_state = 114
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 114
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.13
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.13
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# OCCUPATION OF SWITZERLAND
	PB_Occupation_Switzerland = {

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			SWI = {
				exists = no
			}
			NOT = {
				tag = SWI
			}
			ROOT = {
				owns_state = 151
				owns_state = 3
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 151
				owns_state = 3
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.14
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.14
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# OCCUPATION OF CATALONIA
	PB_Occupation_Catalonia = {

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			CAT = {
				exists = no
			}
			NOT = {
				OR = {
					tag = CAT
					tag = SPR
				}
			}
			ROOT = {
				owns_state = 165
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 165
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.15
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.15
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# OCCUPATION OF ANDALUSIA
	PB_Occupation_Andalusia = {

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			ADL = {
				exists = no
			}
			NOT = {
				OR = {
					tag = CAT
					tag = ADL
					tag = SPR
				}
			}
			ROOT = {
				owns_state = 169
				owns_state = 173
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 169
				owns_state = 173
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.16
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.16
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}

	# OCCUPATION OF SPAIN
	PB_Occupation_Spain = {

		icon = PB_Decision_Collaborators

		cost = 0

		ai_will_do = {
			factor = 100
		}

		activation = {
			SPR = {
				exists = no
			}
			NOT = {
				tag = SPR
			}
			ROOT = {
				owns_state = 41
			}
		}

		selectable_mission = yes
		days_mission_timeout = 70
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			NOT = {
				owns_state = 41
			}
		}

		complete_effect = {
			ROOT = {
				country_event = occupation.17
			}
		}

		timeout_effect = {
			ROOT = {
				country_event = occupation.17
			}
			add_political_power = -50
			add_stability = -0.05
			add_war_support = -0.05
		}
	}
}

