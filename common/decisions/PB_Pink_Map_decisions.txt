PB_PINK_The_Pink_Map = {

	ZIM_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = {
			original_tag = ZIM
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = ZIM_Tier_1_Formed } }
				num_of_controlled_states > 6
			}
		}
        cost = 20
		ai_will_do = {
			factor = 4000
		}
		fire_only_once = yes
		days_remove = 20
		modifier = {
       	 	civilian_factory_use = 1
    	}
		remove_effect = {
				set_cosmetic_tag = ZIM_1
				set_country_flag = ZIM_Tier_1_Formed
				every_owned_state = { 
					limit = {
						NOT = {
							is_core_of = ZIM
						}
					}
					add_core_of = ZIM
				}
				add_research_slot = 1
		}
	}

	ZAM_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = { 
			original_tag = ZAM 
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = ZAM_Tier_1_Formed } }
				num_of_controlled_states > 6
			}
		}
		cost = 20
		ai_will_do = { 
			factor = 4000 
		}
		fire_only_once = yes
		days_remove = 20
		modifier = { 
			civilian_factory_use = 1 
		}
		remove_effect = {
			set_cosmetic_tag = ZAM_1
			set_country_flag = ZAM_Tier_1_Formed
			every_owned_state = {
				limit = { NOT = { is_core_of = ZAM } }
				add_core_of = ZAM
			}
			add_research_slot = 1
		}
	}

		CAP_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = { 
			original_tag = CAP 
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = CAP_Tier_1_Formed } }
				num_of_controlled_states > 6
			}
		}
		cost = 20
		ai_will_do = { 
			factor = 4000 
		}
		fire_only_once = yes
		days_remove = 20
		modifier = { 
			civilian_factory_use = 1 
		}
		remove_effect = {
			set_cosmetic_tag = CAP_1
			set_country_flag = CAP_Tier_1_Formed
			every_owned_state = {
				limit = { NOT = { is_core_of = CAP } }
				add_core_of = CAP
			}
			add_research_slot = 1
		}
	}

	BAR_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = { 
			original_tag = BAR 
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = BAR_Tier_1_Formed } }
				num_of_controlled_states > 6
			}
		}
		cost = 20
		ai_will_do = { 
			factor = 4000 
		}
		fire_only_once = yes
		days_remove = 20
		modifier = { 
			civilian_factory_use = 1 
		}
		remove_effect = {
			set_cosmetic_tag = BAR_1
			set_country_flag = BAR_Tier_1_Formed
			every_owned_state = {
				limit = { NOT = { is_core_of = BAR } }
				add_core_of = BAR
			}
			add_research_slot = 1
		}
	}

	BAE_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = { 
			original_tag = BAE 
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = BAE_Tier_1_Formed } }
				num_of_controlled_states > 6
			}
		}
		cost = 20
		ai_will_do = { 
			factor = 4000 
		}
		fire_only_once = yes
		days_remove = 20
		modifier = { 
			civilian_factory_use = 1 
		}
		remove_effect = {
			set_cosmetic_tag = BAE_1
			set_country_flag = BAE_Tier_1_Formed
			every_owned_state = {
				limit = { NOT = { is_core_of = BAE } }
				add_core_of = BAE
			}
			add_research_slot = 1
		}
	}

	RRC_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = { 
			original_tag = RRC 
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = RRC_Tier_1_Formed } }
				num_of_controlled_states > 6
			}
		}
		cost = 20
		ai_will_do = { 
			factor = 4000 
		}
		fire_only_once = yes
		days_remove = 20
		modifier = { 
			civilian_factory_use = 1 
		}
		remove_effect = {
			set_cosmetic_tag = RRC_1
			set_country_flag = RRC_Tier_1_Formed
			every_owned_state = {
				limit = { NOT = { is_core_of = RRC } }
				add_core_of = RRC
			}
			add_research_slot = 1
		}
	}

	LFS_increase_tier = {
		icon = GFX_decision_generic_form_nation
		allowed = { 
			original_tag = LFS 
		}
		available = {
			if = {
				limit = { NOT = { has_country_flag = LFS_Tier_1_Formed } }
				num_of_controlled_states > 6
			}
		}
		cost = 20
		ai_will_do = { factor = 4000 }
		fire_only_once = yes
		days_remove = 20
		modifier = { civilian_factory_use = 1 }
		remove_effect = {
			set_cosmetic_tag = LFS_1
			set_country_flag = LFS_Tier_1_Formed
			every_owned_state = {
				limit = { NOT = { is_core_of = LFS } }
				add_core_of = LFS
			}
			add_research_slot = 1
		}
	}


	PB_attack_Pink_Map = {
		icon = GFX_decision_generic_operation
		cost = 20

		allowed = {
			OR = {
				original_tag = ZIM
				original_tag = ZAM
				original_tag = CAP
				original_tag = BAR
				original_tag = BAE
				original_tag = RRC
				original_tag = LFS
			}
		}
		available = {
			has_war = no

			hidden_trigger = {
				date > 1934.02.01
			}
			custom_trigger_tooltip = {
				tooltip = PNK_Recently_Declared_War_flag_tt
				NOT = {
					has_country_flag = {
						flag = PNK_Recently_Declared_War_flag
						days < 70
					}
				}
			}
			NOT = {
				has_country_flag = {
					flag = PNK_Recently_Declared_War_flag
					days < 70
				}
			}
    	}
		visible = {
			is_neighbor_of = FROM			
		}

		targets = { ZIM ZAM CAP BAR BAE RRC LFS }
    	targets_dynamic = yes
		war_with_target_on_remove = yes
		complete_effect = {
			set_country_flag = PNK_Recently_Declared_War_flag
		}
		remove_effect = {
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
		days_remove = 15
		cancel_trigger = {
			FROM = {
				OR = {
					exists = no
					has_war_with = ROOT
				}
			}
		}
	}
}