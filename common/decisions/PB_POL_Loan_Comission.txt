PB_POL_Loan_Comission = {

	PB_POL_Start_Kiedrons_Plan = {
		icon = generic_decision

		ai_will_do = {
			factor = 10
			
		}

		visible = {
			
		}

		available = {
			
		}

		modifier = {

		}

		complete_effect = {
			country_event = {
				id = pol.139
			}
		}

		fire_only_once = no

		days_remove = 365

		days_re_enable = 0

		remove_effect = {
			if = {
				limit = {
					has_idea = PB_POL_Kiedrons_Plan_Small
				}
				remove_ideas = PB_POL_Kiedrons_Plan_Small
				hidden_effect = {
					add_to_variable = {
						var = POL_LC_next_payment
						value = -20000
					}
				}
			}
			if = {
				limit = {
					has_idea = PB_POL_Kiedrons_Plan_Medium
				}
				remove_ideas = PB_POL_Kiedrons_Plan_Medium
				hidden_effect = {
					add_to_variable = {
						var = POL_LC_next_payment
						value = -40000
					}
				}
			}
			if = {
				limit = {
					has_idea = PB_POL_Kiedrons_Plan_Big
				}
				remove_ideas = PB_POL_Kiedrons_Plan_Big
				hidden_effect = {
					add_to_variable = {
						var = POL_LC_next_payment
						value = -60000
					}
				}
			}
		}
	}

	PB_POL_Dismiss_German_Debt = {
		icon = generic_decision

		ai_will_do = {
			factor = 10
			
		}

		visible = {
			
		}

		available = {
			has_war_with = GER
		}

		modifier = {

		}

		complete_effect = {
			hidden_effect = {
				subtract_from_variable = {
					var = POL_LC_debt_all
					value = POL_LC_debt_GER
				}
				set_variable = { var = POL_LC_debt_GER value = 0 }
			}
		}

		fire_only_once = yes

	}

	PB_POL_Dismiss_British_Debt = {
		icon = generic_decision

		ai_will_do = {
			factor = 10
			
		}

		visible = {
			
		}

		available = {
			has_war_with = ENG
		}

		modifier = {

		}

		complete_effect = {
			hidden_effect = {
				subtract_from_variable = {
					var = POL_LC_debt_all
					value = POL_LC_debt_UK
				}
				set_variable = { var = POL_LC_debt_UK value = 0 }
			}
		}

		fire_only_once = yes
	}

	PB_POL_Dismiss_French_Debt = {
		icon = generic_decision

		ai_will_do = {
			factor = 10
			
		}

		visible = {
			
		}

		available = {
			has_war_with = FRA
		}

		modifier = {

		}

		complete_effect = {
			hidden_effect = {
				subtract_from_variable = {
					var = POL_LC_debt_all
					value = POL_LC_debt_FRA
				}
				set_variable = { var = POL_LC_debt_FRA value = 0 }
			}
		}

		fire_only_once = yes
	}

	PB_POL_Dismiss_Russian_Debt = {
		icon = generic_decision

		ai_will_do = {
			factor = 10
			
		}

		visible = {
			
		}

		available = {
			has_war_with = RUS
		}

		modifier = {

		}

		complete_effect = {
			hidden_effect = {
				subtract_from_variable = {
					var = POL_LC_debt_all
					value = POL_LC_debt_RUS
				}
				set_variable = { var = POL_LC_debt_RUS value = 0 }
			}
		}

		fire_only_once = yes
	}

	PB_POL_Loan_Comission_Debug = {
		icon = generic_decision

		cost = 50
		ai_will_do = {
			factor = 10
			
		}

		visible = {
			
		}

		available = {
			
		}

		modifier = {
			political_power_cost = 0.1
		}

		fire_only_once = no

		days_remove = 25

		days_re_enable = 70

		remove_effect = {
			IF = {
				LIMIT = {
					NOT = {
						has_government = ni
					}
				}
				PB_POL_Decrease_Radicals_Influence = yes
			}
			ELSE = {
				PB_POL_Increse_Radicals_Influence = yes
			}
			IF = {
				LIMIT = {
					NOT = {
						has_government = rc
					}
				}
				PB_POL_Decrease_Namiestnik_Influence = yes
			}
			ELSE = {
				PB_POL_Increse_Namiestnik_Influence = yes
			}
			IF = {
				LIMIT = {
					NOT = {
						has_government = ds
					}
				}
				PB_POL_Decrease_Moderates_Influence = yes
			}
			ELSE = {
				PB_POL_Increse_Moderates_Influence = yes
			}
			IF = {
				LIMIT = {
					NOT = {
						has_government = cn
					}
				}
				PB_POL_Decrease_Conservatives_Influence = yes
			}
			ELSE = {
				PB_POL_Increse_Conservatives_Influence = yes
			}
			IF = {
				LIMIT = {
					NOT = {
						has_government = an
					}
				}
				PB_POL_Decrease_Industrialists_Influence = yes
			}
			ELSE = {
				PB_POL_Increse_Industralists_Influence = yes
			}
		}
	}

	POL_Monthly_Payment = {

		icon = GFX_decision_generic_civil_support
		
		activation = { 
			original_tag = POL
			OR = {
				has_completed_focus = PB_POL_Only_Eastern_Capital
				has_completed_focus = PB_POL_Mixed_Sources
			}          
		}

		available = {
			has_capitulated = yes
		}  

		selectable_mission = no

		is_good = no
		fire_only_once = no
				
		days_mission_timeout = 30

		timeout_effect = {
			hidden_effect = {
			subtract_from_variable = {
				var = POL_LC_current_money
				value = POL_LC_next_payment
			}
		}
			activate_mission  = POL_Monthly_Payment
		}
	}

}