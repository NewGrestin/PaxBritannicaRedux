PB_CHI_Civil_War = {

	PB_CHI_Civil_War_Incoming_Major_Famine = {

		icon = revolt

		available = {
			NOT = {
				has_idea = CHI_CIVWAR_minor_famine
			}
		}

		days_mission_timeout = 100

		activation = {
			has_idea = CHI_CIVWAR_minor_famine
		}

		visible = {
			
		}		

		fixed_random_seed = no

		fire_only_once = yes

		is_good = no

		timeout_effect = {
			swap_ideas = { remove_idea = CHI_CIVWAR_minor_famine add_idea = CHI_CIVWAR_major_famine }
		}
	}

	PB_CHI_Civil_War_Cholera_Outbreak = {

		icon = generic_prepare_civil_war

		available = {
			NOT = {
				has_idea = CHI_CIVWAR_major_famine
			}
		}

		days_mission_timeout = 200

		activation = {
			has_idea = CHI_CIVWAR_major_famine
		}

		visible = {
			
		}		

		modifier = {
			stability_factor = -0.10
			army_morale_factor = -0.1
		}

		fixed_random_seed = no

		fire_only_once = yes

		is_good = no

		timeout_effect = {

		}
	}

	PB_CHI_Civil_War_Emergency_Conscription = {

		icon = generic_civil_support

		available = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		visible = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		modifier = {

		}

		days_remove = 50

		cost = 100

		remove_effect = {
			add_ideas = CHI_CIVWAR_Emergency_Conscription
		}
	}

	PB_CHI_Civil_War_War_Economy = {

		icon = generic_civil_support

		available = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		visible = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		modifier = {

		}

		cost = 100

		complete_effect = {
			add_ideas = war_economy
		}
	}

	PB_CHI_Civil_War_Partisan_Warfare = {

		icon = generic_civil_support

		available = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		visible = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		modifier = {
			supply_consumption_factor = -0.20
			army_defence_factor = 0.1
		}

		cost = 150

		days_remove = 60

		complete_effect = {

		}
	}

	PB_CHI_Civil_War_Emegency_Armament_Program = {

		icon = generic_civil_support

		available = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		visible = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		modifier = {

		}

		cost = 150

		days_remove = 100

		complete_effect = {
			add_extra_state_shared_building_slots = 2
			random_owned_controlled_state = {
				add_building_construction = {
					type = arms_factory
					level = 2
					instant_build = yes
				}
			}
		}
	}

	PB_CHI_Civil_War_Women_in_Wartime = {
     	icon = generic_civil_support

		available = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		visible = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
		}

		fire_only_once = yes

     	days_remove = 25

    	cost = 150    

     	remove_effect = { #run effect when removed/finished
          	add_ideas = PB_CHI_CIVWAR_Wartime_Women
     	} 

    	ai_will_do = {
          	factor = 1000
     	}
	}

	PB_CHI_Civil_War_Fanatical_Resistance = {

		icon = generic_civil_support

		available = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
			surrender_progress > 0.7
		}

		visible = {
			OR = {
				tag = CHI
				tag = PRC
				tag = ROC
			}
			OR = {
				has_war_with = CHI
				has_war_with = PRC
				has_war_with = ROC
			}
			surrender_progress > 0.7
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		modifier = {
			stability_factor = -0.10
			army_defence_factor = 0.2
			consumer_goods_factor = -0.05
		}

		cost = 150

		days_remove = 60

		complete_effect = {

		}
	}
}