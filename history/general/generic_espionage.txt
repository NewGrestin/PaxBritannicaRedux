# For countries that doesnt have advisors defined at start

# Generic Espionage (only for LaR owners)
every_possible_country = {
	limit = {
		#add TAGs if needed
		is_dynamic_country = no
		NOT = { tag = XXD } #debug tag
	}

	generate_character = {
		token_base = generic_head_of_intelligence # token will be TAG_token_base
		advisor = {
			slot = special_minister
			traits = { head_of_intelligence }
			allowed = {
				has_dlc = "La Resistance"
			}
			available = {
				has_intelligence_agency = yes
			}
			ai_will_do = {
				factor = 1
				modifier = {
					factor = 0
					has_intelligence_agency = no
				}
			}
		}
		portraits = {
			army = { small = GFX_generic_head_of_intelligence }
		}
	}
	generic_head_of_intelligence = { set_character_flag = is_generic }
}
